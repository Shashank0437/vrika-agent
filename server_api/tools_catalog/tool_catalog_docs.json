{
  "ai_analyze_session": {
    "long_description": "CipherStrike workflow intelligence posts stored session artefacts to an LLM for summarisation, correlation, and remediation hints without re-running scanners.\n\nTypical NyxStrike output combines the model transcript with summaries of artefacts already stored for the cited session.\n\nUse this connector when repeating full scans across the workspace would duplicate load; defer to planners only when deterministic tooling has already exercised the corpus.\n\nLLM backends may persist prompts or outputs under provider policy\u2014strip secrets and scope identifiers before analysing.\n\nCipherStrike transmits the JSON envelope to `/api/intelligence/analyze-session`; NyxStrike performs final validation, then shells out or calls the bundled integration advertised for this catalogue id.",
    "safety": "Operate only inside authorized penetration tests or sanctioned lab networks. Respect stop conditions, lawful intercept rules, and internal change windows. Sensitive parameters are captured in CipherStrike tenant execution logs alongside NyxStrike response snippets \u2014 avoid pasting production secrets when alternatives exist. LLMs may retain pasted evidence\u2014sanitize transcripts before analysis.",
    "usage": "CipherStrike `ai_analyze_session` executes against `/api/intelligence/analyze-session` via POST JSON envelopes assembled in the Execute modal.\n\n1. Collect the `session_id` from the CipherStrike session you want reviewed\u2014no other payload is required.\n2. Ensure the session already contains the artefacts you expect the model to reason over; this route does not launch scanners.\n3. Review the returned narrative for sensitive names or credentials before sharing outside the assessment channel.\n4. If the model references missing context, re-run prerequisite tools and append their logs to the session before re-analysing.\n\nCipherStrike mechanics: send a JSON POST from the Execute modal\u2014the agent drops empty optional scalars before invoking NyxStrike.\nPrefer explicit catalogue fields (`target`, `url`, `wordlist`, etc.) before `additional_args`; extra tokens must still match the packaged wrapper behaviour and your engagement rules.\nPer-field catalogue hints (first sentence of modal help):\n- `session_id` (required): Server-side CipherStrike/NyxStrike session UUID/hash used to reload transcripts for LLM planners."
  },
  "airbase-ng": {
    "long_description": "airbase-ng fabricates soft access points impersonating BSSIDs/channel plans for sanctioned evil-twin playbook labs.\n\n802.11 harnesses emit captures, WPA hash material, rogue AP scaffolding, injector acknowledgements, and audit logs keyed to channels/BSSIDs.\n\nLaboratory rigs should dedicate monitor-mode interfaces plus isolated spectrum; tethered enterprise APs rarely tolerate injection homework.\n\nJamming neighbours or intercepting unauthorised traffic violates law\u2014operate strictly inside sanctioned RF enclosures and licensed bands.\n\nCipherStrike transmits the JSON envelope to `/api/tools/wifi_pentest/airbase_ng`; NyxStrike performs final validation, then shells out or calls the bundled integration advertised for this catalogue id.",
    "safety": "Operate only inside authorized penetration tests or sanctioned lab networks. Respect stop conditions, lawful intercept rules, and internal change windows. Sensitive parameters are captured in CipherStrike tenant execution logs alongside NyxStrike response snippets \u2014 avoid pasting production secrets when alternatives exist. Wireless probing and injection are lawful only on spectrum and networks you control by license or contract.",
    "usage": "CipherStrike `airbase-ng` executes against `/api/tools/wifi_pentest/airbase_ng` via POST JSON envelopes assembled in the Execute modal.\n\n1. Place adapters in monitor mode upstream when required (`airmon-ng` helper routes).\n2. Provide capture paths, ESSIDs/EAP configs, handshake targets, attack modes, regulatory paperwork references.\n3. Never point attack modes at civilian infrastructure\u2014hardware isolation only.\n4. Convert captures only within labs; distributing PMKIDs may violate telecom rules.\n\nCipherStrike mechanics: send a JSON POST from the Execute modal\u2014the agent drops empty optional scalars before invoking NyxStrike.\nPrefer explicit catalogue fields (`target`, `url`, `wordlist`, etc.) before `additional_args`; extra tokens must still match the packaged wrapper behaviour and your engagement rules.\nPer-field catalogue hints (first sentence of modal help):\n- `interface` (required): Network interface (`wlan0mon`, `eth0`) for Wi\u2011Fi or Responder jobs.\n- `essid` (required): SSID string for handshake correlation or rogue APs.\n- `channel` (optional): 802.11 channel for captures or rogue AP broadcasts.\n- `bssid` (optional): AP MAC filters for airodump/aireplay workflows.\n- `wpa_mode` (optional): Soft-AP cipher selection (`wpa2`, `wpa3`, \u2026)."
  },
  "aircrack-ng": {
    "long_description": "aircrack-ng leverages dictionary attacks plus statistical tests against WPA handshakes inside captures; optional BSSID filters reduce noise when multiple APs coexist.\n\n802.11 harnesses emit captures, WPA hash material, rogue AP scaffolding, injector acknowledgements, and audit logs keyed to channels/BSSIDs.\n\nLaboratory rigs should dedicate monitor-mode interfaces plus isolated spectrum; tethered enterprise APs rarely tolerate injection homework.\n\nJamming neighbours or intercepting unauthorised traffic violates law\u2014operate strictly inside sanctioned RF enclosures and licensed bands.\n\nCipherStrike transmits the JSON envelope to `/api/tools/password_cracking/aircrack_ng`; NyxStrike performs final validation, then shells out or calls the bundled integration advertised for this catalogue id.",
    "safety": "Operate only inside authorized penetration tests or sanctioned lab networks. Respect stop conditions, lawful intercept rules, and internal change windows. Sensitive parameters are captured in CipherStrike tenant execution logs alongside NyxStrike response snippets \u2014 avoid pasting production secrets when alternatives exist. Wireless probing and injection are lawful only on spectrum and networks you control by license or contract.",
    "usage": "CipherStrike `aircrack-ng` executes against `/api/tools/password_cracking/aircrack_ng` via POST JSON envelopes assembled in the Execute modal.\n\n1. Submit capture files passphrase lists ESSID/BSSID filters referencing aircrack/hcx tooling.\n2. Ensure captures contain complete four-way exchanges before cracking attempts waste GPU time.\n3. Document crack speeds and dictionaries for courtroom-ready notes.\n4. Decrypt PCAPs solely for evidence workflows under legal advice.\n\nCipherStrike mechanics: send a JSON POST from the Execute modal\u2014the agent drops empty optional scalars before invoking NyxStrike.\nPrefer explicit catalogue fields (`target`, `url`, `wordlist`, etc.) before `additional_args`; extra tokens must still match the packaged wrapper behaviour and your engagement rules.\nPer-field catalogue hints (first sentence of modal help):\n- `capture_files` (required): `.pcap` paths with handshake material for aircrack-ng.\n- `wordlist` (required): Agent-resident newline wordlist for content/DNS/password tooling.\n- `bssid` (optional): AP MAC filters for airodump/aireplay workflows."
  },
  "airdecap-ng": {
    "long_description": "airdecap-ng decrypts WEP/WPA packets once PSK/passphrases or WEP keys are known for offline evidence review.\n\n802.11 harnesses emit captures, WPA hash material, rogue AP scaffolding, injector acknowledgements, and audit logs keyed to channels/BSSIDs.\n\nLaboratory rigs should dedicate monitor-mode interfaces plus isolated spectrum; tethered enterprise APs rarely tolerate injection homework.\n\nJamming neighbours or intercepting unauthorised traffic violates law\u2014operate strictly inside sanctioned RF enclosures and licensed bands.\n\nCipherStrike transmits the JSON envelope to `/api/tools/wifi_pentest/airdecap_ng`; NyxStrike performs final validation, then shells out or calls the bundled integration advertised for this catalogue id.",
    "safety": "Operate only inside authorized penetration tests or sanctioned lab networks. Respect stop conditions, lawful intercept rules, and internal change windows. Sensitive parameters are captured in CipherStrike tenant execution logs alongside NyxStrike response snippets \u2014 avoid pasting production secrets when alternatives exist. Wireless probing and injection are lawful only on spectrum and networks you control by license or contract.",
    "usage": "CipherStrike `airdecap-ng` executes against `/api/tools/wifi_pentest/airdecap_ng` via POST JSON envelopes assembled in the Execute modal.\n\n1. Submit capture files passphrase lists ESSID/BSSID filters referencing aircrack/hcx tooling.\n2. Ensure captures contain complete four-way exchanges before cracking attempts waste GPU time.\n3. Document crack speeds and dictionaries for courtroom-ready notes.\n4. Decrypt PCAPs solely for evidence workflows under legal advice.\n\nCipherStrike mechanics: send a JSON POST from the Execute modal\u2014the agent drops empty optional scalars before invoking NyxStrike.\nPrefer explicit catalogue fields (`target`, `url`, `wordlist`, etc.) before `additional_args`; extra tokens must still match the packaged wrapper behaviour and your engagement rules.\nPer-field catalogue hints (first sentence of modal help):\n- `capture_file` (required): Single capture for airdecap-ng decrypt passes.\n- `password` (optional): Cleartext password for authenticated scans\u2014rotate if leaked into logs.\n- `wep_key` (optional): ASCII/hex WEP key for airdecap-ng decryption.\n- `bssid` (optional): AP MAC filters for airodump/aireplay workflows.\n- `essid` (optional): SSID string for handshake correlation or rogue APs."
  },
  "aireplay-ng": {
    "long_description": "aireplay-ng injects forged frames (`deauth`, `fakeauth`, ARP replay) with controlled burst counts respecting physical-layer risks.\n\n802.11 harnesses emit captures, WPA hash material, rogue AP scaffolding, injector acknowledgements, and audit logs keyed to channels/BSSIDs.\n\nLaboratory rigs should dedicate monitor-mode interfaces plus isolated spectrum; tethered enterprise APs rarely tolerate injection homework.\n\nJamming neighbours or intercepting unauthorised traffic violates law\u2014operate strictly inside sanctioned RF enclosures and licensed bands.\n\nCipherStrike transmits the JSON envelope to `/api/tools/wifi_pentest/aireplay_ng`; NyxStrike performs final validation, then shells out or calls the bundled integration advertised for this catalogue id.",
    "safety": "Operate only inside authorized penetration tests or sanctioned lab networks. Respect stop conditions, lawful intercept rules, and internal change windows. Sensitive parameters are captured in CipherStrike tenant execution logs alongside NyxStrike response snippets \u2014 avoid pasting production secrets when alternatives exist. Wireless probing and injection are lawful only on spectrum and networks you control by license or contract. Deauthentication floods disrupt legitimate users\u2014illegal outside consented RF cages.",
    "usage": "CipherStrike `aireplay-ng` executes against `/api/tools/wifi_pentest/aireplay_ng` via POST JSON envelopes assembled in the Execute modal.\n\n1. Identify interface channel BSSID/client MAC selections per monitor-mode checklist.\n2. Keep antennas disconnected from production SSIDs unless emulating sanctioned evil twins.\n3. Log injection counts to explain RF impacts.\n4. Pair `aireplay-ng` bursts with humane scheduling\u2014Wi-Fi disruptions alarm users instantly.\n\nCipherStrike mechanics: send a JSON POST from the Execute modal\u2014the agent drops empty optional scalars before invoking NyxStrike.\nPrefer explicit catalogue fields (`target`, `url`, `wordlist`, etc.) before `additional_args`; extra tokens must still match the packaged wrapper behaviour and your engagement rules.\nPer-field catalogue hints (first sentence of modal help):\n- `interface` (required): Network interface (`wlan0mon`, `eth0`) for Wi\u2011Fi or Responder jobs.\n- `attack_mode` (required): `aireplay-ng` attack name **or** hashcat `-a` attack id\u2014meaning depends on catalogue entry context.\n- `bssid` (optional): AP MAC filters for airodump/aireplay workflows.\n- `client_mac` (optional): Station MAC for directed deauthentication bursts.\n- `count` (optional): `aireplay-ng` packet burst counter."
  },
  "airmon-ng": {
    "long_description": "airmon-ng manages monitor-mode lifecycle for Aircrack suites (checks interfering processes and toggles vap interfaces).\n\n802.11 harnesses emit captures, WPA hash material, rogue AP scaffolding, injector acknowledgements, and audit logs keyed to channels/BSSIDs.\n\nLaboratory rigs should dedicate monitor-mode interfaces plus isolated spectrum; tethered enterprise APs rarely tolerate injection homework.\n\nJamming neighbours or intercepting unauthorised traffic violates law\u2014operate strictly inside sanctioned RF enclosures and licensed bands.\n\nCipherStrike transmits the JSON envelope to `/api/tools/wifi_pentest/airmon_ng`; NyxStrike performs final validation, then shells out or calls the bundled integration advertised for this catalogue id.",
    "safety": "Operate only inside authorized penetration tests or sanctioned lab networks. Respect stop conditions, lawful intercept rules, and internal change windows. Sensitive parameters are captured in CipherStrike tenant execution logs alongside NyxStrike response snippets \u2014 avoid pasting production secrets when alternatives exist. Wireless probing and injection are lawful only on spectrum and networks you control by license or contract.",
    "usage": "CipherStrike `airmon-ng` executes against `/api/tools/wifi_pentest/airmon_ng` via POST JSON envelopes assembled in the Execute modal.\n\n1. Identify interface channel BSSID/client MAC selections per monitor-mode checklist.\n2. Keep antennas disconnected from production SSIDs unless emulating sanctioned evil twins.\n3. Log injection counts to explain RF impacts.\n4. Pair `aireplay-ng` bursts with humane scheduling\u2014Wi-Fi disruptions alarm users instantly.\n\nCipherStrike mechanics: send a JSON POST from the Execute modal\u2014the agent drops empty optional scalars before invoking NyxStrike.\nPrefer explicit catalogue fields (`target`, `url`, `wordlist`, etc.) before `additional_args`; extra tokens must still match the packaged wrapper behaviour and your engagement rules.\nPer-field catalogue hints (first sentence of modal help):\n- `interface` (required): Network interface (`wlan0mon`, `eth0`) for Wi\u2011Fi or Responder jobs.\n- `action` (required): `airmon-ng` verb (`start`/`stop`) or CloudMapper subcommand name.\n- `channel` (optional): 802.11 channel for captures or rogue AP broadcasts."
  },
  "airodump-ng": {
    "long_description": "airodump-ng passively observes 802.11 beacons/data frames, pinning channels/BSSIDs/clients writing rolling PCAP prefixes.\n\n802.11 harnesses emit captures, WPA hash material, rogue AP scaffolding, injector acknowledgements, and audit logs keyed to channels/BSSIDs.\n\nLaboratory rigs should dedicate monitor-mode interfaces plus isolated spectrum; tethered enterprise APs rarely tolerate injection homework.\n\nJamming neighbours or intercepting unauthorised traffic violates law\u2014operate strictly inside sanctioned RF enclosures and licensed bands.\n\nCipherStrike transmits the JSON envelope to `/api/tools/wifi_pentest/airodump_ng`; NyxStrike performs final validation, then shells out or calls the bundled integration advertised for this catalogue id.",
    "safety": "Operate only inside authorized penetration tests or sanctioned lab networks. Respect stop conditions, lawful intercept rules, and internal change windows. Sensitive parameters are captured in CipherStrike tenant execution logs alongside NyxStrike response snippets \u2014 avoid pasting production secrets when alternatives exist. Wireless probing and injection are lawful only on spectrum and networks you control by license or contract.",
    "usage": "CipherStrike `airodump-ng` executes against `/api/tools/wifi_pentest/airodump_ng` via POST JSON envelopes assembled in the Execute modal.\n\n1. Identify interface channel BSSID/client MAC selections per monitor-mode checklist.\n2. Keep antennas disconnected from production SSIDs unless emulating sanctioned evil twins.\n3. Log injection counts to explain RF impacts.\n4. Pair `aireplay-ng` bursts with humane scheduling\u2014Wi-Fi disruptions alarm users instantly.\n\nCipherStrike mechanics: send a JSON POST from the Execute modal\u2014the agent drops empty optional scalars before invoking NyxStrike.\nPrefer explicit catalogue fields (`target`, `url`, `wordlist`, etc.) before `additional_args`; extra tokens must still match the packaged wrapper behaviour and your engagement rules.\nPer-field catalogue hints (first sentence of modal help):\n- `interface` (required): Network interface (`wlan0mon`, `eth0`) for Wi\u2011Fi or Responder jobs.\n- `output_prefix` (optional): `airodump-ng` `--write` basename for rolling captures.\n- `bssid` (optional): AP MAC filters for airodump/aireplay workflows.\n- `channel` (optional): 802.11 channel for captures or rogue AP broadcasts.\n- `essid` (optional): SSID string for handshake correlation or rogue APs."
  },
  "amass": {
    "long_description": "OWASP Amass orchestrates recursive DNS brute, API-backed OSINT, graph storage, ASN intel, wildcard detection, and scripted modules described in upstream user guides/config formats.\n\nResults are textual lists, JSON artefacts, archive URLs, or passive reconnaissance breadcrumbs suitable for ingestion into trackers.\n\nOSINT tooling should precede intrusive scanning wherever policy demands minimal touch; escalate to DNS brute or crawler stages only after validating scope wording.\n\nPassive APIs throttle aggressively and may log lookups under shared keys\u2014credential handling matters.\n\nCipherStrike transmits the JSON envelope to `/api/tools/amass`; NyxStrike performs final validation, then shells out or calls the bundled integration advertised for this catalogue id.",
    "safety": "Operate only inside authorized penetration tests or sanctioned lab networks. Respect stop conditions, lawful intercept rules, and internal change windows. Sensitive parameters are captured in CipherStrike tenant execution logs alongside NyxStrike response snippets \u2014 avoid pasting production secrets when alternatives exist.",
    "usage": "CipherStrike `amass` executes against `/api/tools/amass` via POST JSON envelopes assembled in the Execute modal.\n\n1. Configure Amass enums, wordlists, passive toggles, and output formats via JSON-first fields.\n2. Expect API quotas if passive sources authenticate\u2014supply keys cleanly.\n3. Merge graph outputs later with Maltego/BBot pipelines when authorised.\n4. Document every API credential rotation.\n\nCipherStrike mechanics: send a JSON POST from the Execute modal\u2014the agent drops empty optional scalars before invoking NyxStrike.\nPrefer explicit catalogue fields (`target`, `url`, `wordlist`, etc.) before `additional_args`; extra tokens must still match the packaged wrapper behaviour and your engagement rules.\nPer-field catalogue hints (first sentence of modal help):\n- `domain` (required): Authorised DNS apex fed to Amass/subfinder/GAU style OSINT connectors.\n- `mode` (optional): High-level tool mode (Amass `enum`, gobuster `dir`, shuffleDNS profile, etc.).\n- `additional_args` (optional): Shell tokens appended after mapped CLI arguments."
  },
  "analyze-target": {
    "long_description": "CipherStrike Intelligent Decision Engine compiles fingerprints, exposures, and heuristics for a scoped target to seed manual validation and chaining tasks.\n\nIntelligence routers combine analysis, chaining, profiling, orchestration manifests, AI-selected toolplans, and explanatory narratives about why certain jobs fire.\n\nSmart planners trade determinism for speed\u2014validate proposed fan-outs before unattended execution on sensitive ranges.\n\nAutomated chains still require human scope checks before touching regulated data classifications.\n\nCipherStrike transmits the JSON envelope to `/api/intelligence/analyze-target`; NyxStrike performs final validation, then shells out or calls the bundled integration advertised for this catalogue id.",
    "safety": "Operate only inside authorized penetration tests or sanctioned lab networks. Respect stop conditions, lawful intercept rules, and internal change windows. Sensitive parameters are captured in CipherStrike tenant execution logs alongside NyxStrike response snippets \u2014 avoid pasting production secrets when alternatives exist. Automated chaining still needs data-handling approvals for anything beyond public banners.",
    "usage": "CipherStrike `analyze-target` executes against `/api/intelligence/analyze-target` via POST JSON envelopes assembled in the Execute modal.\n\n1. Confirm the written scope authorises automated intelligence or orchestration against the supplied target.\n2. Populate `target` with the exact hostname, URL, or asset identifier your engagement letter lists\u2014no implicit wildcards.\n3. For `smart-scan`, tune `objective` and `max_tools` so the planner cannot fan out beyond your risk appetite; review the proposed fan-out in session logs before accepting unattended runs.\n4. `preview-attack-chain` avoids persistence\u2014use it first, then run `create-attack-chain` only when stored hypotheses are required.\n5. Interpret AI output as advisory: validate every recommended action with deterministic tooling and customer policies.\n`additional_args` is rarely used on these routes; prefer structured fields.\n\nCipherStrike mechanics: send a JSON POST from the Execute modal\u2014the agent drops empty optional scalars before invoking NyxStrike.\nPrefer explicit catalogue fields (`target`, `url`, `wordlist`, etc.) before `additional_args`; extra tokens must still match the packaged wrapper behaviour and your engagement rules.\nPer-field catalogue hints (first sentence of modal help):\n- `target` (required): Primary IPv4/IPv6/hostname/CIDR or bulk newline list routed into network scanners and RPC clients."
  },
  "anew": {
    "long_description": "anew streams STDIN uniqueness into optional output sinks\u2014canonical dedupe glue for massive URL lists.\n\nPipeline gadgets emit deduped URL streams, mutated query dictionaries, novelty markers consumed by fuzzers downstream.\n\nPair urltooling early in pipelines to cut noise feeding ffuf nuclei pipelines.\n\nMassive stdin lines stress memory filters\u2014chunk inputs when chaining shell pipes via agents.\n\nCipherStrike transmits the JSON envelope to `/api/tools/anew`; NyxStrike performs final validation, then shells out or calls the bundled integration advertised for this catalogue id.",
    "safety": "Operate only inside authorized penetration tests or sanctioned lab networks. Respect stop conditions, lawful intercept rules, and internal change windows. Sensitive parameters are captured in CipherStrike tenant execution logs alongside NyxStrike response snippets \u2014 avoid pasting production secrets when alternatives exist.",
    "usage": "CipherStrike `anew` executes against `/api/tools/anew` via POST JSON envelopes assembled in the Execute modal.\n\n1. Pipe newline URLs via `stdin` equivalent fields documented for qsreplace/anew/uro.\n2. Mark replacement tokens distinctly when prepping ffuf placeholders.\n3. Use `uro` to dedupe monstrous crawler outputs feeding nuclei templates.\n4. Document pipeline ordering for reproducibility.\n\nCipherStrike mechanics: send a JSON POST from the Execute modal\u2014the agent drops empty optional scalars before invoking NyxStrike.\nPrefer explicit catalogue fields (`target`, `url`, `wordlist`, etc.) before `additional_args`; extra tokens must still match the packaged wrapper behaviour and your engagement rules.\nPer-field catalogue hints (first sentence of modal help):\n- `input_data` (required): Line block deduped by anew-style utilities.\n- `output_file` (optional): Explicit output path for logs/PCAPNG/JSON when CLIs support `-o`.\n- `additional_args` (optional): Shell tokens appended after mapped CLI arguments."
  },
  "angr": {
    "long_description": "angr provides symbolic execution, symbolic states, CFG recovery, and interactive solving APIs over binaries with optional inline Python drivers.\n\nDebuggers/disassemblers/gadget hunters produce listings, scripted traces, annotated graphs, emulation logs, or CTF scaffolding.\n\nStatic analysis excels on offline artefacts; symbolic tooling demands compute and may path-explode on large binaries.\n\nMalware-grade samples may trigger host controls\u2014sandbox accordingly and sanitise artefacts before sharing externally.\n\nCipherStrike transmits the JSON envelope to `/api/tools/angr`; NyxStrike performs final validation, then shells out or calls the bundled integration advertised for this catalogue id.",
    "safety": "Operate only inside authorized penetration tests or sanctioned lab networks. Respect stop conditions, lawful intercept rules, and internal change windows. Sensitive parameters are captured in CipherStrike tenant execution logs alongside NyxStrike response snippets \u2014 avoid pasting production secrets when alternatives exist. Binary tooling may invoke malware safeguards\u2014sandbox untrusted specimens.",
    "usage": "CipherStrike `angr` executes against `/api/tools/angr` via POST JSON envelopes assembled in the Execute modal.\n\n1. Pass file paths macros scripts analysis toggles aligning with gdb/radare2/ghidra headless wrappers.\n2. Long analyses need `timeout` fields\u2014coordinate with infra.\n3. Store project directories on encrypted volumes.\n4. Symbolic backends may spin CPU\u2014quota jobs fairly.\n\nCipherStrike mechanics: send a JSON POST from the Execute modal\u2014the agent drops empty optional scalars before invoking NyxStrike.\nPrefer explicit catalogue fields (`target`, `url`, `wordlist`, etc.) before `additional_args`; extra tokens must still match the packaged wrapper behaviour and your engagement rules.\nPer-field catalogue hints (first sentence of modal help):\n- `binary` (required): Executable for radare2/ghidra/objdump/ropper workflows.\n- `script_content` (optional): Inline pwntools/angr code executed before harness launch.\n- `find_address` (optional): Hex goal for symbolic exploration.\n- `avoid_addresses` (optional): Hex addresses excluded from symbolic paths.\n- `analysis_type` (optional): angr strategy (`symbolic`, `CFGFast`, \u2026).\n- `additional_args` (optional): Shell tokens appended after mapped CLI arguments."
  },
  "api-schema-analyzer": {
    "long_description": "Static reviewer for Swagger/OpenAPI/GraphQL schemas spotting weak auth scopes, dangerously broad operations, SSRF-able callbacks, and data-exposure regressions prior to fuzzing.\n\nAPI tooling highlights schema quirks, undocumented parameters, malformed JWT behaviours, fuzz deltas, and property-test regressions referencing OpenAPI/GraphQL artefacts.\n\nSchema-first testers complement browser-only assessments by exercising machine contracts that never render in GUIs.\n\nGenerated traffic can cascade into billing or email workflows\u2014narrow operationIds and verbs before unattended fuzzing.\n\nCipherStrike transmits the JSON envelope to `/api/tools/api_schema_analyzer`; NyxStrike performs final validation, then shells out or calls the bundled integration advertised for this catalogue id.",
    "safety": "Operate only inside authorized penetration tests or sanctioned lab networks. Respect stop conditions, lawful intercept rules, and internal change windows. Sensitive parameters are captured in CipherStrike tenant execution logs alongside NyxStrike response snippets \u2014 avoid pasting production secrets when alternatives exist. API fuzzing can trigger costly side-effects (email, billing, writes)\u2014narrow scope before unattended runs.",
    "usage": "CipherStrike `api-schema-analyzer` executes against `/api/tools/api_schema_analyzer` via POST JSON envelopes assembled in the Execute modal.\n\n1. Supply `schema_url`/`swagger_url` plus parsers (`schema_type`).\n2. Static analysis surfaces risky operations\u2014not runtime bugs.\n3. Cross-check findings with penetration tests afterwards.\n4. Export JSON for downstream Schemathesis planning.\n\nCipherStrike mechanics: send a JSON POST from the Execute modal\u2014the agent drops empty optional scalars before invoking NyxStrike.\nPrefer explicit catalogue fields (`target`, `url`, `wordlist`, etc.) before `additional_args`; extra tokens must still match the packaged wrapper behaviour and your engagement rules.\nPer-field catalogue hints (first sentence of modal help):\n- `schema_url` (required): Remote OpenAPI/Swagger/GraphQL schema URL downloaded before static analysis.\n- `schema_type` (optional): Parser hint such as `\"openapi\"`, `\"swagger\"`, or `\"graphql\"`."
  },
  "api_fuzzer": {
    "long_description": "High-level REST fuzz mapper combining `base_url`, HTTP verbs, wordlists and explicit endpoint manifests for brute enumeration.\n\nAPI tooling highlights schema quirks, undocumented parameters, malformed JWT behaviours, fuzz deltas, and property-test regressions referencing OpenAPI/GraphQL artefacts.\n\nSchema-first testers complement browser-only assessments by exercising machine contracts that never render in GUIs.\n\nGenerated traffic can cascade into billing or email workflows\u2014narrow operationIds and verbs before unattended fuzzing.\n\nCipherStrike transmits the JSON envelope to `/api/tools/api_fuzzer`; NyxStrike performs final validation, then shells out or calls the bundled integration advertised for this catalogue id.",
    "safety": "Operate only inside authorized penetration tests or sanctioned lab networks. Respect stop conditions, lawful intercept rules, and internal change windows. Sensitive parameters are captured in CipherStrike tenant execution logs alongside NyxStrike response snippets \u2014 avoid pasting production secrets when alternatives exist. API fuzzing can trigger costly side-effects (email, billing, writes)\u2014narrow scope before unattended runs. REST fuzzing may hit destructive verbs\u2014blacklist admin routes first.",
    "usage": "CipherStrike `api_fuzzer` executes against `/api/tools/api_fuzzer` via POST JSON envelopes assembled in the Execute modal.\n\n1. Pair `base_url` with verbs wordlists endpoints lists rate controls.\n2. Treat discovered routes as tentative until authenticated testing proves reachability.\n3. Stop fuzzing destructive admin verbs unless explicitly chartered.\n4. Merge output with schema analyzers downstream.\n\nCipherStrike mechanics: send a JSON POST from the Execute modal\u2014the agent drops empty optional scalars before invoking NyxStrike.\nPrefer explicit catalogue fields (`target`, `url`, `wordlist`, etc.) before `additional_args`; extra tokens must still match the packaged wrapper behaviour and your engagement rules.\nPer-field catalogue hints (first sentence of modal help):\n- `base_url` (required): API root such as `https://api.target` that relative fuzz paths expand against.\n- `method` (optional): HTTP verb (`GET`, `POST`, \u2026) or tool-specific dispatcher (ffuf mode selection, HTTP-framework action cues, Arjun transport).\n- `wordlist` (optional): Agent-resident newline wordlist for content/DNS/password tooling.\n- `endpoints` (optional): Relative route fragments `api_fuzzer` appends to `base_url`."
  },
  "arjun": {
    "long_description": "Arjun heuristically enumerates latent HTTP GET/POST/JSON/XML parameters referencing wordlists `delay`, `threads`, stabilisation switches, plus passthrough fragments.\n\nOutput usually enumerates reachable paths, crawler graphs, fingerprints, robots hints, deduplicated URLs, or header snapshots.\n\nCombine directory bruteforce, spidering, and header probes sequentially: surface mapping before attempting injection payloads.\n\nMisconfigured crawlers crawl out of scope rapidly\u2014align seed URLs, blacklist regexes, and depth caps with authorised programs.\n\nCipherStrike transmits the JSON envelope to `/api/tools/arjun`; NyxStrike performs final validation, then shells out or calls the bundled integration advertised for this catalogue id.",
    "safety": "Operate only inside authorized penetration tests or sanctioned lab networks. Respect stop conditions, lawful intercept rules, and internal change windows. Sensitive parameters are captured in CipherStrike tenant execution logs alongside NyxStrike response snippets \u2014 avoid pasting production secrets when alternatives exist.",
    "usage": "CipherStrike `arjun` executes against `/api/tools/arjun` via POST JSON envelopes assembled in the Execute modal.\n\n1. Provide base URLs methods delay threads wordlists aligning with Arjun.\n2. Use stabilisation toggles when applications are noisy.\n3. Feed discoveries into authorised fuzz pipelines.\n4. Stop if application owners report instability.\n\nCipherStrike mechanics: send a JSON POST from the Execute modal\u2014the agent drops empty optional scalars before invoking NyxStrike.\nPrefer explicit catalogue fields (`target`, `url`, `wordlist`, etc.) before `additional_args`; extra tokens must still match the packaged wrapper behaviour and your engagement rules.\nPer-field catalogue hints (first sentence of modal help):\n- `url` (required): Fully qualified HTTP/S URL for crawlers, fuzzers, or vuln scanners (scheme required).\n- `method` (optional): HTTP verb (`GET`, `POST`, \u2026) or tool-specific dispatcher (ffuf mode selection, HTTP-framework action cues, Arjun transport).\n- `wordlist` (optional): Agent-resident newline wordlist for content/DNS/password tooling.\n- `delay` (optional): Millis or seconds between probes to reduce WAF trips (Arjun, fuzzers).\n- `threads` (optional): Concurrent workers for ffuf/arjun/sublist3r/feroxbuster HTTP jobs.\n- `stable` (optional): Arjun stabilises differential heuristics for noisy targets.\n- `additional_args` (optional): Shell tokens appended after mapped CLI arguments."
  },
  "arp-scan": {
    "long_description": "arp-scan issues ARP who-has probes on Ethernet segments\u2014ideal for authoritative L2 inventories but intrusive on noisy LAN fabrics.\n\nExpect port listings, traceroute-like hints, service banners, NIC inventories, handshake captures, or ARP inventories depending on tooling.\n\nFast SYN scanners suit huge ranges; balanced scanners add scripts and versioning; SMB-oriented tools deepen Windows visibility\u2014choose based on scope scale and covertness mandates.\n\nHigh packet rates overwhelm small networks\u2014rate-limit, subdivide subnets, and obtain written approval before touching neighbouring tenants.\n\nCipherStrike transmits the JSON envelope to `/api/tools/arp-scan`; NyxStrike performs final validation, then shells out or calls the bundled integration advertised for this catalogue id.",
    "safety": "Operate only inside authorized penetration tests or sanctioned lab networks. Respect stop conditions, lawful intercept rules, and internal change windows. Sensitive parameters are captured in CipherStrike tenant execution logs alongside NyxStrike response snippets \u2014 avoid pasting production secrets when alternatives exist. Port scanners and handshake grabbers alert defenders\u2014mirror scope IPs exactly as authorised.",
    "usage": "CipherStrike `arp-scan` executes against `/api/tools/arp-scan` via POST JSON envelopes assembled in the Execute modal.\n\n1. Select the correct `interface` on the same broadcast domain as your targets; ARP never crosses routers.\n2. Trim `target` ranges to what is authorised\u2014ARP sweeps touch every host on the LAN.\n3. Merge results with DHCP logs to spot rogue devices.\n4. Expect duplicate entries when laptops roam; deduplicate by MAC.\n\nCipherStrike mechanics: send a JSON POST from the Execute modal\u2014the agent drops empty optional scalars before invoking NyxStrike.\nPrefer explicit catalogue fields (`target`, `url`, `wordlist`, etc.) before `additional_args`; extra tokens must still match the packaged wrapper behaviour and your engagement rules.\nPer-field catalogue hints (first sentence of modal help):\n- `target` (required): Primary IPv4/IPv6/hostname/CIDR or bulk newline list routed into network scanners and RPC clients.\n- `additional_args` (optional): Shell tokens appended after mapped CLI arguments."
  },
  "assetfinder": {
    "long_description": "Tomnomnom\u2019s passive subdomain enumerator combining certificate transparency, reverse DNS scraping, and brute toggles respecting `only_subdomains` filters.\n\nResults are textual lists, JSON artefacts, archive URLs, or passive reconnaissance breadcrumbs suitable for ingestion into trackers.\n\nOSINT tooling should precede intrusive scanning wherever policy demands minimal touch; escalate to DNS brute or crawler stages only after validating scope wording.\n\nPassive APIs throttle aggressively and may log lookups under shared keys\u2014credential handling matters.\n\nCipherStrike transmits the JSON envelope to `/api/tools/assetfinder`; NyxStrike performs final validation, then shells out or calls the bundled integration advertised for this catalogue id.",
    "safety": "Operate only inside authorized penetration tests or sanctioned lab networks. Respect stop conditions, lawful intercept rules, and internal change windows. Sensitive parameters are captured in CipherStrike tenant execution logs alongside NyxStrike response snippets \u2014 avoid pasting production secrets when alternatives exist.",
    "usage": "CipherStrike `assetfinder` executes against `/api/tools/assetfinder` via POST JSON envelopes assembled in the Execute modal.\n\n1. Supply `domain` plus optional brute toggles referencing assetfinder quirks.\n2. Ideal for chaining into httpx/ffuf pipelines quickly.\n3. Expect compact stdout\u2014capture for diffing engagements.\n4. Avoid hammering scopes with brute flags enabled accidentally.\n\nCipherStrike mechanics: send a JSON POST from the Execute modal\u2014the agent drops empty optional scalars before invoking NyxStrike.\nPrefer explicit catalogue fields (`target`, `url`, `wordlist`, etc.) before `additional_args`; extra tokens must still match the packaged wrapper behaviour and your engagement rules.\nPer-field catalogue hints (first sentence of modal help):\n- `domain` (required): Authorised DNS apex fed to Amass/subfinder/GAU style OSINT connectors.\n- `only_subdomains` (optional): assetfinder prints hostnames without bare apex records.\n- `additional_args` (optional): Shell tokens appended after mapped CLI arguments."
  },
  "autopsy": {
    "long_description": "Autopsy/SleuthKit GUI workflows carve timelines, keyword indices, and extracted files from forensic disk images.\n\nForensic tooling streams carved files, timelines, entropy maps, PCAP decodes, volatility tables, SQLite extractions.\n\nForensic artefacts may contain HIPAA/PII regulated content\u2014encrypt exports immediately.\n\nResource-heavy jobs need disk-bound IO quotas; carve only targeted ranges when multi-terabyte images are mounted.\n\nCipherStrike transmits the JSON envelope to `/api/tools/binary_analysis/autopsy`; NyxStrike performs final validation, then shells out or calls the bundled integration advertised for this catalogue id.",
    "safety": "Operate only inside authorized penetration tests or sanctioned lab networks. Respect stop conditions, lawful intercept rules, and internal change windows. Sensitive parameters are captured in CipherStrike tenant execution logs alongside NyxStrike response snippets \u2014 avoid pasting production secrets when alternatives exist. Evidence images may contain regulated data\u2014encrypt at rest immediately after extraction.",
    "usage": "CipherStrike `autopsy` executes against `/api/tools/binary_analysis/autopsy` via POST JSON envelopes assembled in the Execute modal.\n\n1. Point `image_path`/`directory`/`case_name`/`project_name` fields at sanctioned evidence lockers.\n2. Long-running ingestion jobs need disk quotas\u2014coordinate with infra.\n3. Exported timelines may contain privileged communications restrict viewers.\n4. Chain SleuthKit outputs into Autopsy dashboards when routes split duties.\n\nCipherStrike mechanics: send a JSON POST from the Execute modal\u2014the agent drops empty optional scalars before invoking NyxStrike.\nPrefer explicit catalogue fields (`target`, `url`, `wordlist`, etc.) before `additional_args`; extra tokens must still match the packaged wrapper behaviour and your engagement rules.\nPer-field catalogue hints (first sentence of modal help):\n- `image_path` (required): Forensic disk image for Autopsy/sleuthkit ingestion.\n- `case_name` (optional): Autopsy logical case separating evidence SQLite stores.\n- `additional_args` (optional): Shell tokens appended after mapped CLI arguments."
  },
  "autorecon": {
    "long_description": "AutoRecon chains service discovery/port scans/scripted modules with pacing (`heartbeat`), timeout safeguards, configurable output dirs, and additional_args bridging custom modules.\n\nExpect port listings, traceroute-like hints, service banners, NIC inventories, handshake captures, or ARP inventories depending on tooling.\n\nFast SYN scanners suit huge ranges; balanced scanners add scripts and versioning; SMB-oriented tools deepen Windows visibility\u2014choose based on scope scale and covertness mandates.\n\nHigh packet rates overwhelm small networks\u2014rate-limit, subdivide subnets, and obtain written approval before touching neighbouring tenants.\n\nCipherStrike transmits the JSON envelope to `/api/tools/autorecon`; NyxStrike performs final validation, then shells out or calls the bundled integration advertised for this catalogue id.",
    "safety": "Operate only inside authorized penetration tests or sanctioned lab networks. Respect stop conditions, lawful intercept rules, and internal change windows. Sensitive parameters are captured in CipherStrike tenant execution logs alongside NyxStrike response snippets \u2014 avoid pasting production secrets when alternatives exist. Port scanners and handshake grabbers alert defenders\u2014mirror scope IPs exactly as authorised.",
    "usage": "CipherStrike `autorecon` executes against `/api/tools/autorecon` via POST JSON envelopes assembled in the Execute modal.\n\n1. Populate `targets`, `heartbeat`, timeouts, directories, modular toggles referencing AutoRecon packaging.\n2. Expect multi-hour timelines\u2014checkpoint `output_dir`.\n3. Use `additional_args` only for ancillary modules spelled out by runbooks.\n4. Verify each bundled tool inside AutoRecon is individually approved.\n\nCipherStrike mechanics: send a JSON POST from the Execute modal\u2014the agent drops empty optional scalars before invoking NyxStrike.\nPrefer explicit catalogue fields (`target`, `url`, `wordlist`, etc.) before `additional_args`; extra tokens must still match the packaged wrapper behaviour and your engagement rules.\nPer-field catalogue hints (first sentence of modal help):\n- `target` (required): Primary IPv4/IPv6/hostname/CIDR or bulk newline list routed into network scanners and RPC clients.\n- `output_dir` (optional): Directory root for AutoRecon, foremost, Scout exports.\n- `port_scans` (optional): Port-related control for `port_scans`; exact meaning follows the catalogue connector.\n- `service_scans` (optional): The workspace forwards `service_scans` in the CipherStrike POST body unchanged after stripping empty optional fields.\n- `heartbeat` (optional): AutoRecon status interval between internal tool stages.\n- `timeout` (optional): Seconds for socket/DNS/CLI overall timeouts.\n- `additional_args` (optional): Shell tokens appended after mapped CLI arguments."
  },
  "bbot": {
    "long_description": "BBot (Black Lantern Security) is a modular OSINT + attack-surface engine accepting rich YAML/JSON `parameters` seeds for safe pipeline automation.\n\nResults are textual lists, JSON artefacts, archive URLs, or passive reconnaissance breadcrumbs suitable for ingestion into trackers.\n\nOSINT tooling should precede intrusive scanning wherever policy demands minimal touch; escalate to DNS brute or crawler stages only after validating scope wording.\n\nPassive APIs throttle aggressively and may log lookups under shared keys\u2014credential handling matters.\n\nCipherStrike transmits the JSON envelope to `/api/bot/bbot`; NyxStrike performs final validation, then shells out or calls the bundled integration advertised for this catalogue id.",
    "safety": "Operate only inside authorized penetration tests or sanctioned lab networks. Respect stop conditions, lawful intercept rules, and internal change windows. Sensitive parameters are captured in CipherStrike tenant execution logs alongside NyxStrike response snippets \u2014 avoid pasting production secrets when alternatives exist.",
    "usage": "CipherStrike `bbot` executes against `/api/bot/bbot` via POST JSON envelopes assembled in the Execute modal.\n\n1. Hydrate YAML/JSON `parameters` seeds events modules flags per BBot contract.\n2. Module fan-out behaves like crawler swarms throttle aggressively.\n3. Archive graph-friendly exports for authorised analysts only.\n4. Validate API tokens per module before enabling.\n\nCipherStrike mechanics: send a JSON POST from the Execute modal\u2014the agent drops empty optional scalars before invoking NyxStrike.\nPrefer explicit catalogue fields (`target`, `url`, `wordlist`, etc.) before `additional_args`; extra tokens must still match the packaged wrapper behaviour and your engagement rules.\nPer-field catalogue hints (first sentence of modal help):\n- `target` (required): Primary IPv4/IPv6/hostname/CIDR or bulk newline list routed into network scanners and RPC clients.\n- `parameters` (required): BBot JSON/YAML selecting modules, safe flags, and seeds."
  },
  "bettercap": {
    "long_description": "bettercap is a Go-based MITM platform with modular caplets integrating Wi\u2011Fi/AP impersonation, BLE attacks, scripting, BLE/Wi\u2011Fi coexistence primitives on monitor interfaces.\n\nBettercap merges Wi-Fi BLE Ethernet MITM scripting caplets telemetry.\n\nBettercap contrasts with legacy aircrack toolchain by offering unified scripting albeit heavier dependencies.\n\nMITM tooling is adversarial emulate only sanctioned labs.\n\nCipherStrike transmits the JSON envelope to `/api/tools/wifi_pentest/bettercap_wifi`; NyxStrike performs final validation, then shells out or calls the bundled integration advertised for this catalogue id.",
    "safety": "Operate only inside authorized penetration tests or sanctioned lab networks. Respect stop conditions, lawful intercept rules, and internal change windows. Sensitive parameters are captured in CipherStrike tenant execution logs alongside NyxStrike response snippets \u2014 avoid pasting production secrets when alternatives exist. Wireless probing and injection are lawful only on spectrum and networks you control by license or contract. Transparent proxies intercept sensitive transactions\u2014use dedicated hardware labs only.",
    "usage": "CipherStrike `bettercap` executes against `/api/tools/wifi_pentest/bettercap_wifi` via POST JSON envelopes assembled in the Execute modal.\n\n1. Put the chosen `interface` into monitor/AP modes upstream if caplets assume promiscuity; load `caplet` payloads only after validating RF containment.\n2. Feed targets (BSSIDs/clients/IP ranges), proxy toggles, and script hooks expected by bundled bettercap integrations.\n3. Expect JSON/event streams documenting MITM primitives\u2014everything touched by transparent proxies generates legal exposure unless isolated.\n4. Compare against classic `aireplay-ng` workflows: bettercap unifies transports but introduces heavier dependence on agent drivers.\n\nCipherStrike mechanics: send a JSON POST from the Execute modal\u2014the agent drops empty optional scalars before invoking NyxStrike.\nPrefer explicit catalogue fields (`target`, `url`, `wordlist`, etc.) before `additional_args`; extra tokens must still match the packaged wrapper behaviour and your engagement rules.\nPer-field catalogue hints (first sentence of modal help):\n- `interface` (required): Network interface (`wlan0mon`, `eth0`) for Wi\u2011Fi or Responder jobs.\n- `mode` (optional): High-level tool mode (Amass `enum`, gobuster `dir`, shuffleDNS profile, etc.).\n- `target_bssid` (optional): 802.11 BSSID narrowed for Bettercap Wi\u2011Fi tooling.\n- `caplet` (optional): Bettercap `.cap` macro loaded after interface setup."
  },
  "binwalk": {
    "long_description": "binwalk signature-scans blobs for filesystems/firmware manifests and optionally extracts cascaded components for reverse engineering pipelines.\n\nDebuggers/disassemblers/gadget hunters produce listings, scripted traces, annotated graphs, emulation logs, or CTF scaffolding.\n\nStatic analysis excels on offline artefacts; symbolic tooling demands compute and may path-explode on large binaries.\n\nMalware-grade samples may trigger host controls\u2014sandbox accordingly and sanitise artefacts before sharing externally.\n\nCipherStrike transmits the JSON envelope to `/api/tools/binwalk`; NyxStrike performs final validation, then shells out or calls the bundled integration advertised for this catalogue id.",
    "safety": "Operate only inside authorized penetration tests or sanctioned lab networks. Respect stop conditions, lawful intercept rules, and internal change windows. Sensitive parameters are captured in CipherStrike tenant execution logs alongside NyxStrike response snippets \u2014 avoid pasting production secrets when alternatives exist. Binary tooling may invoke malware safeguards\u2014sandbox untrusted specimens.",
    "usage": "CipherStrike `binwalk` executes against `/api/tools/binwalk` via POST JSON envelopes assembled in the Execute modal.\n\n1. Point `input_file`/`image_path`/`output_dir` fields at sanctioned evidence stores.\n2. Choose carving signatures conservatively\u2014broad signatures balloon storage.\n3. Keep chain-of-custody notes for every exported file.\n4. Some carvers overwrite slack space\u2014snapshot images first.\n\nCipherStrike mechanics: send a JSON POST from the Execute modal\u2014the agent drops empty optional scalars before invoking NyxStrike.\nPrefer explicit catalogue fields (`target`, `url`, `wordlist`, etc.) before `additional_args`; extra tokens must still match the packaged wrapper behaviour and your engagement rules.\nPer-field catalogue hints (first sentence of modal help):\n- `file` (required): Binary/firmware specimen for binwalk/strings/checksec.\n- `additional_args` (optional): Shell tokens appended after mapped CLI arguments."
  },
  "bulk_extractor": {
    "long_description": "bulk_extractor parallel-scans forensic images extracting emails URLs credit cards keyed by selectable scanner modules.\n\nForensic tooling streams carved files, timelines, entropy maps, PCAP decodes, volatility tables, SQLite extractions.\n\nForensic artefacts may contain HIPAA/PII regulated content\u2014encrypt exports immediately.\n\nResource-heavy jobs need disk-bound IO quotas; carve only targeted ranges when multi-terabyte images are mounted.\n\nCipherStrike transmits the JSON envelope to `/api/tools/bulk_extractor`; NyxStrike performs final validation, then shells out or calls the bundled integration advertised for this catalogue id.",
    "safety": "Operate only inside authorized penetration tests or sanctioned lab networks. Respect stop conditions, lawful intercept rules, and internal change windows. Sensitive parameters are captured in CipherStrike tenant execution logs alongside NyxStrike response snippets \u2014 avoid pasting production secrets when alternatives exist. Evidence images may contain regulated data\u2014encrypt at rest immediately after extraction.",
    "usage": "CipherStrike `bulk_extractor` executes against `/api/tools/bulk_extractor` via POST JSON envelopes assembled in the Execute modal.\n\n1. Point `input_file`/`image_path`/`output_dir` fields at sanctioned evidence stores.\n2. Choose carving signatures conservatively\u2014broad signatures balloon storage.\n3. Keep chain-of-custody notes for every exported file.\n4. Some carvers overwrite slack space\u2014snapshot images first.\n\nCipherStrike mechanics: send a JSON POST from the Execute modal\u2014the agent drops empty optional scalars before invoking NyxStrike.\nPrefer explicit catalogue fields (`target`, `url`, `wordlist`, etc.) before `additional_args`; extra tokens must still match the packaged wrapper behaviour and your engagement rules.\nPer-field catalogue hints (first sentence of modal help):\n- `input_file` (required): Evidence blob for foremost/scalpel/testdisk carvers.\n- `output_dir` (optional): Directory root for AutoRecon, foremost, Scout exports.\n- `scanners` (optional): The workspace forwards `scanners` in the CipherStrike POST body unchanged after stripping empty optional fields.\n- `additional_args` (optional): Shell tokens appended after mapped CLI arguments."
  },
  "burpsuite": {
    "long_description": "CipherStrike proxies to a packaged Burp-like automation fa\u00e7ade performing scoped spidering/passive-active scans with concurrency caps (`max_pages`).\n\nOutput usually enumerates reachable paths, crawler graphs, fingerprints, robots hints, deduplicated URLs, or header snapshots.\n\nCombine directory bruteforce, spidering, and header probes sequentially: surface mapping before attempting injection payloads.\n\nMisconfigured crawlers crawl out of scope rapidly\u2014align seed URLs, blacklist regexes, and depth caps with authorised programs.\n\nCipherStrike transmits the JSON envelope to `/api/tools/burpsuite-alternative`; NyxStrike performs final validation, then shells out or calls the bundled integration advertised for this catalogue id.",
    "safety": "Operate only inside authorized penetration tests or sanctioned lab networks. Respect stop conditions, lawful intercept rules, and internal change windows. Sensitive parameters are captured in CipherStrike tenant execution logs alongside NyxStrike response snippets \u2014 avoid pasting production secrets when alternatives exist. Active scanning can modify business data\u2014scope forms and database seeds explicitly.",
    "usage": "CipherStrike `burpsuite` executes against `/api/tools/burpsuite-alternative` via POST JSON envelopes assembled in the Execute modal.\n\n1. Configure `target`, API listener keys, concurrency pages scan policies per Burp/ZAP automation fa\u00e7ade.\n2. Headless scanners still mutate state validate CSRF safeguards.\n3. Export reports into encrypted evidence stores promptly.\n4. Provide unique API keys per engagement.\n\nCipherStrike mechanics: send a JSON POST from the Execute modal\u2014the agent drops empty optional scalars before invoking NyxStrike.\nPrefer explicit catalogue fields (`target`, `url`, `wordlist`, etc.) before `additional_args`; extra tokens must still match the packaged wrapper behaviour and your engagement rules.\nPer-field catalogue hints (first sentence of modal help):\n- `target` (required): Primary IPv4/IPv6/hostname/CIDR or bulk newline list routed into network scanners and RPC clients.\n- `scan_type` (optional): Connector-specific profile: Nmap flags, Trivy scan class, Burp macro, or ZAP policy name.\n- `headless` (optional): Drive Burp-alternative or ZAP automation without a desktop session.\n- `max_depth` (optional): Recursive crawl depth for packaged web spiders.\n- `max_pages` (optional): Burp-style spider page cap."
  },
  "checkov": {
    "long_description": "Bridgecrew Checkov statically analyses IaC repos (Terraform, CloudFormation, K8s) with granular control ids and output SARIF integrations.\n\nCloud posture connectors emit JSON/HTML/SARIF findings enumerating IAM sprawl, open buckets, CIS deviations, stale keys, and attack-path narratives.\n\nCloud scans require least-privilege API roles scoped to enumerated accounts\u2014not root keys embedded in plaintext JSON long term.\n\nProvider APIs log every call; minimise repeated broad scans and purge credentials promptly after tabletop windows.\n\nCipherStrike transmits the JSON envelope to `/api/tools/checkov`; NyxStrike performs final validation, then shells out or calls the bundled integration advertised for this catalogue id.",
    "safety": "Operate only inside authorized penetration tests or sanctioned lab networks. Respect stop conditions, lawful intercept rules, and internal change windows. Sensitive parameters are captured in CipherStrike tenant execution logs alongside NyxStrike response snippets \u2014 avoid pasting production secrets when alternatives exist. Cloud API tokens authenticate broad actions\u2014purge keys after tabletop exercises.",
    "usage": "CipherStrike `checkov` executes against `/api/tools/checkov` via POST JSON envelopes assembled in the Execute modal.\n\n1. Provide kubeconfig paths image refs benchmark ids directories matching Trivy/Checkov/kube-bench/Clair/Falco wrappers.\n2. Align severities/report formats with SOC ingestion pipelines.\n3. Remediation tips require owner teams\u2014don't auto-patch.\n4. Some scans mutate nothing; others exec inside clusters\u2014confirm blast radius.\n\nCipherStrike mechanics: send a JSON POST from the Execute modal\u2014the agent drops empty optional scalars before invoking NyxStrike.\nPrefer explicit catalogue fields (`target`, `url`, `wordlist`, etc.) before `additional_args`; extra tokens must still match the packaged wrapper behaviour and your engagement rules.\nPer-field catalogue hints (first sentence of modal help):\n- `directory` (optional): Repository root for Checkov or broad filesystem Trivy scans.\n- `framework` (optional): The workspace forwards `framework` in the CipherStrike POST body unchanged after stripping empty optional fields.\n- `check` (optional): The workspace forwards `check` in the CipherStrike POST body unchanged after stripping empty optional fields.\n- `skip_check` (optional): The workspace forwards `skip_check` in the CipherStrike POST body unchanged after stripping empty optional fields.\n- `output_format` (optional): Structured report format (`json`, `xml`, `sarif`, `html`).\n- `additional_args` (optional): Shell tokens appended after mapped CLI arguments."
  },
  "checksec": {
    "long_description": "checksec (PaX/grsecurity heritage) parses ELF security mitigations (`NX`, `RELRO`, `PIE`, `Canary`, fortify).\n\nDebuggers/disassemblers/gadget hunters produce listings, scripted traces, annotated graphs, emulation logs, or CTF scaffolding.\n\nStatic analysis excels on offline artefacts; symbolic tooling demands compute and may path-explode on large binaries.\n\nMalware-grade samples may trigger host controls\u2014sandbox accordingly and sanitise artefacts before sharing externally.\n\nCipherStrike transmits the JSON envelope to `/api/tools/checksec`; NyxStrike performs final validation, then shells out or calls the bundled integration advertised for this catalogue id.",
    "safety": "Operate only inside authorized penetration tests or sanctioned lab networks. Respect stop conditions, lawful intercept rules, and internal change windows. Sensitive parameters are captured in CipherStrike tenant execution logs alongside NyxStrike response snippets \u2014 avoid pasting production secrets when alternatives exist. Binary tooling may invoke malware safeguards\u2014sandbox untrusted specimens.",
    "usage": "CipherStrike `checksec` executes against `/api/tools/checksec` via POST JSON envelopes assembled in the Execute modal.\n\n1. Provide `binary`, `file_path`, or CLI-equivalent selectors for quick static insight.\n2. Use triage utilities before heavyweight Ghidra/autopsy timelines.\n3. Outputs are succinct\u2014combine with checklist reporting.\n4. Malware artefacts remain hazardous\u2014sandbox hosts accordingly.\n\nCipherStrike mechanics: send a JSON POST from the Execute modal\u2014the agent drops empty optional scalars before invoking NyxStrike.\nPrefer explicit catalogue fields (`target`, `url`, `wordlist`, etc.) before `additional_args`; extra tokens must still match the packaged wrapper behaviour and your engagement rules.\nPer-field catalogue hints (first sentence of modal help):\n- `file` (required): Binary/firmware specimen for binwalk/strings/checksec.\n- `additional_args` (optional): Shell tokens appended after mapped CLI arguments."
  },
  "clair": {
    "long_description": "Clair static-scans layered container images referencing vulnerability databases/APIs configurable via YAML `config` blobs.\n\nCloud posture connectors emit JSON/HTML/SARIF findings enumerating IAM sprawl, open buckets, CIS deviations, stale keys, and attack-path narratives.\n\nCloud scans require least-privilege API roles scoped to enumerated accounts\u2014not root keys embedded in plaintext JSON long term.\n\nProvider APIs log every call; minimise repeated broad scans and purge credentials promptly after tabletop windows.\n\nCipherStrike transmits the JSON envelope to `/api/tools/clair`; NyxStrike performs final validation, then shells out or calls the bundled integration advertised for this catalogue id.",
    "safety": "Operate only inside authorized penetration tests or sanctioned lab networks. Respect stop conditions, lawful intercept rules, and internal change windows. Sensitive parameters are captured in CipherStrike tenant execution logs alongside NyxStrike response snippets \u2014 avoid pasting production secrets when alternatives exist. Cloud API tokens authenticate broad actions\u2014purge keys after tabletop exercises.",
    "usage": "CipherStrike `clair` executes against `/api/tools/clair` via POST JSON envelopes assembled in the Execute modal.\n\n1. Provide kubeconfig paths image refs benchmark ids directories matching Trivy/Checkov/kube-bench/Clair/Falco wrappers.\n2. Align severities/report formats with SOC ingestion pipelines.\n3. Remediation tips require owner teams\u2014don't auto-patch.\n4. Some scans mutate nothing; others exec inside clusters\u2014confirm blast radius.\n\nCipherStrike mechanics: send a JSON POST from the Execute modal\u2014the agent drops empty optional scalars before invoking NyxStrike.\nPrefer explicit catalogue fields (`target`, `url`, `wordlist`, etc.) before `additional_args`; extra tokens must still match the packaged wrapper behaviour and your engagement rules.\nPer-field catalogue hints (first sentence of modal help):\n- `image` (required): OCI image ref (`repo:tag@sha256:\u2026`) for Clair/Trivy.\n- `config` (optional): cloudmapper account metadata or secondary service configuration path.\n- `output_format` (optional): Structured report format (`json`, `xml`, `sarif`, `html`).\n- `additional_args` (optional): Shell tokens appended after mapped CLI arguments."
  },
  "cloudmapper": {
    "long_description": "Cloudmapper visualises IAM/network relationships in AWS accounts (prepare/webserver/reporting actions) to spot privilege sprawl.\n\nCloud posture connectors emit JSON/HTML/SARIF findings enumerating IAM sprawl, open buckets, CIS deviations, stale keys, and attack-path narratives.\n\nCloud scans require least-privilege API roles scoped to enumerated accounts\u2014not root keys embedded in plaintext JSON long term.\n\nProvider APIs log every call; minimise repeated broad scans and purge credentials promptly after tabletop windows.\n\nCipherStrike transmits the JSON envelope to `/api/tools/cloudmapper`; NyxStrike performs final validation, then shells out or calls the bundled integration advertised for this catalogue id.",
    "safety": "Operate only inside authorized penetration tests or sanctioned lab networks. Respect stop conditions, lawful intercept rules, and internal change windows. Sensitive parameters are captured in CipherStrike tenant execution logs alongside NyxStrike response snippets \u2014 avoid pasting production secrets when alternatives exist. Cloud API tokens authenticate broad actions\u2014purge keys after tabletop exercises.",
    "usage": "CipherStrike `cloudmapper` executes against `/api/tools/cloudmapper` via POST JSON envelopes assembled in the Execute modal.\n\n1. Populate cloud profile/session fields provider flags services directories per scout/pacu/cloudmapper wrappers.\n2. Expect large IAM graphs\u2014narrow services first.\n3. Export HTML/JSON responsibly; URLs may contain account identifiers.\n4. Rotate cloud keys after engagements.\n\nCipherStrike mechanics: send a JSON POST from the Execute modal\u2014the agent drops empty optional scalars before invoking NyxStrike.\nPrefer explicit catalogue fields (`target`, `url`, `wordlist`, etc.) before `additional_args`; extra tokens must still match the packaged wrapper behaviour and your engagement rules.\nPer-field catalogue hints (first sentence of modal help):\n- `account` (required): CloudMapper/Prowler pseudo-account alias for mapping AWS tenancy graphs.\n- `action` (required): `airmon-ng` verb (`start`/`stop`) or CloudMapper subcommand name.\n- `config` (optional): cloudmapper account metadata or secondary service configuration path.\n- `additional_args` (optional): Shell tokens appended after mapped CLI arguments."
  },
  "commix": {
    "long_description": "commix hunts OS command injection and shell interaction vectors via staged injection levels and tamper primitives similar to sqlmap philosophies.\n\nMetasploit/payload harnesses expose sessions, staged shellcode artefacts, staged listeners, meterpreter transcripts, and failure diagnostics.\n\nExploitation tooling is for labs with containment; maintain VM snapshots because modules may patch or crash unstable services.\n\nPayload generation can trip AV egress controls\u2014isolate build hosts and watermark binaries for traceability.\n\nCipherStrike transmits the JSON envelope to `/api/tools/exploitation/commix`; NyxStrike performs final validation, then shells out or calls the bundled integration advertised for this catalogue id.",
    "safety": "Operate only inside authorized penetration tests or sanctioned lab networks. Respect stop conditions, lawful intercept rules, and internal change windows. Sensitive parameters are captured in CipherStrike tenant execution logs alongside NyxStrike response snippets \u2014 avoid pasting production secrets when alternatives exist. Weaponised modules may crash services\u2014snapshot lab VMs and segregate egress before launching. Command injection success may yield remote shells\u2014contain egress immediately.",
    "usage": "CipherStrike `commix` executes against `/api/tools/exploitation/commix` via POST JSON envelopes assembled in the Execute modal.\n\n1. Specify injection points, transports, and tamper tiers via mapped fields.\n2. Expect operating-system level effects\u2014sandbox the target app.\n3. Capture command outputs but avoid destructive switches (`shutdown`, etc.).\n4. Combine with SSRF testers when chaining into internal services.\n\nCipherStrike mechanics: send a JSON POST from the Execute modal\u2014the agent drops empty optional scalars before invoking NyxStrike.\nPrefer explicit catalogue fields (`target`, `url`, `wordlist`, etc.) before `additional_args`; extra tokens must still match the packaged wrapper behaviour and your engagement rules.\nPer-field catalogue hints (first sentence of modal help):\n- `url` (required): Fully qualified HTTP/S URL for crawlers, fuzzers, or vuln scanners (scheme required).\n- `level` (optional): Commix injection depth, ophcrack gadget level, or similar integer tier.\n- `additional_args` (optional): Shell tokens appended after mapped CLI arguments."
  },
  "create-attack-chain": {
    "long_description": "Planner service that derives ordered attack hypotheses from analysed signals; preview mode avoids persistence while creation mode persists structured chains for assisted operations.\n\nIntelligence routers combine analysis, chaining, profiling, orchestration manifests, AI-selected toolplans, and explanatory narratives about why certain jobs fire.\n\nSmart planners trade determinism for speed\u2014validate proposed fan-outs before unattended execution on sensitive ranges.\n\nAutomated chains still require human scope checks before touching regulated data classifications.\n\nCipherStrike transmits the JSON envelope to `/api/intelligence/create-attack-chain`; NyxStrike performs final validation, then shells out or calls the bundled integration advertised for this catalogue id.",
    "safety": "Operate only inside authorized penetration tests or sanctioned lab networks. Respect stop conditions, lawful intercept rules, and internal change windows. Sensitive parameters are captured in CipherStrike tenant execution logs alongside NyxStrike response snippets \u2014 avoid pasting production secrets when alternatives exist. Automated chaining still needs data-handling approvals for anything beyond public banners.",
    "usage": "CipherStrike `create-attack-chain` executes against `/api/intelligence/create-attack-chain` via POST JSON envelopes assembled in the Execute modal.\n\n1. Confirm the written scope authorises automated intelligence or orchestration against the supplied target.\n2. Populate `target` with the exact hostname, URL, or asset identifier your engagement letter lists\u2014no implicit wildcards.\n3. For `smart-scan`, tune `objective` and `max_tools` so the planner cannot fan out beyond your risk appetite; review the proposed fan-out in session logs before accepting unattended runs.\n4. `preview-attack-chain` avoids persistence\u2014use it first, then run `create-attack-chain` only when stored hypotheses are required.\n5. Interpret AI output as advisory: validate every recommended action with deterministic tooling and customer policies.\n`additional_args` is rarely used on these routes; prefer structured fields.\n\nCipherStrike mechanics: send a JSON POST from the Execute modal\u2014the agent drops empty optional scalars before invoking NyxStrike.\nPrefer explicit catalogue fields (`target`, `url`, `wordlist`, etc.) before `additional_args`; extra tokens must still match the packaged wrapper behaviour and your engagement rules.\nPer-field catalogue hints (first sentence of modal help):\n- `target` (required): Primary IPv4/IPv6/hostname/CIDR or bulk newline list routed into network scanners and RPC clients.\n- `objective` (optional): Planner hint for smart-scan/attack-chain heuristics (`comprehensive`, `stealth`)."
  },
  "dalfox": {
    "long_description": "DalFox focuses on reflected/stored XSS by mining parameters, injecting polyglot payloads, and optionally pairing with collaborator servers for blind XSS.\n\nDalFox emits reflected stored blind XSS correlations optional collaborator callbacks mined parameters.\n\nPair DalFox with crawler-derived parameters before generic XSS sprays.\n\nStored XSS payloads can harm real users sanitise QA tenants.\n\nCipherStrike transmits the JSON envelope to `/api/tools/dalfox`; NyxStrike performs final validation, then shells out or calls the bundled integration advertised for this catalogue id.",
    "safety": "Operate only inside authorized penetration tests or sanctioned lab networks. Respect stop conditions, lawful intercept rules, and internal change windows. Sensitive parameters are captured in CipherStrike tenant execution logs alongside NyxStrike response snippets \u2014 avoid pasting production secrets when alternatives exist.",
    "usage": "CipherStrike `dalfox` executes against `/api/tools/dalfox` via POST JSON envelopes assembled in the Execute modal.\n\n1. Supply target URLs/parameters and optional crawler hooks/colaborator URLs.\n2. Start with benign payloads verifying reflection, then escalate using prepared polyglots.\n3. Capture proofs without impacting real users\u2014use labs or seeded accounts.\n4. Integrate DalFox/XSSer output with ticketing for developer reproduction.\n\nCipherStrike mechanics: send a JSON POST from the Execute modal\u2014the agent drops empty optional scalars before invoking NyxStrike.\nPrefer explicit catalogue fields (`target`, `url`, `wordlist`, etc.) before `additional_args`; extra tokens must still match the packaged wrapper behaviour and your engagement rules.\nPer-field catalogue hints (first sentence of modal help):\n- `url` (required): Fully qualified HTTP/S URL for crawlers, fuzzers, or vuln scanners (scheme required).\n- `blind` (optional): Route XSS testing through out-of-band/collaborator channels for blind sinks.\n- `additional_args` (optional): Shell tokens appended after mapped CLI arguments."
  },
  "dig": {
    "long_description": "Uses ISC BIND dig semantics (`+short` style) for deterministic DNS answers across multiple RR types configurable via `record_types`.\n\nResults are textual lists, JSON artefacts, archive URLs, or passive reconnaissance breadcrumbs suitable for ingestion into trackers.\n\nOSINT tooling should precede intrusive scanning wherever policy demands minimal touch; escalate to DNS brute or crawler stages only after validating scope wording.\n\nPassive APIs throttle aggressively and may log lookups under shared keys\u2014credential handling matters.\n\nCipherStrike transmits the JSON envelope to `/api/tools/dig`; NyxStrike performs final validation, then shells out or calls the bundled integration advertised for this catalogue id.",
    "safety": "Operate only inside authorized penetration tests or sanctioned lab networks. Respect stop conditions, lawful intercept rules, and internal change windows. Sensitive parameters are captured in CipherStrike tenant execution logs alongside NyxStrike response snippets \u2014 avoid pasting production secrets when alternatives exist.",
    "usage": "CipherStrike `dig` executes against `/api/tools/dig` via POST JSON envelopes assembled in the Execute modal.\n\n1. Enter `target` plus `record_types` lists; responses should mirror dig semantics.\n2. Use this for split-horizon troubleshooting before firing MassDNS workloads.\n3. Log resolver used if `additional_args` pins `@server` values.\n4. Large AXFR attempts belong in chartered heavy DNS jobs\u2014not here unless exposed.\n\nCipherStrike mechanics: send a JSON POST from the Execute modal\u2014the agent drops empty optional scalars before invoking NyxStrike.\nPrefer explicit catalogue fields (`target`, `url`, `wordlist`, etc.) before `additional_args`; extra tokens must still match the packaged wrapper behaviour and your engagement rules.\nPer-field catalogue hints (first sentence of modal help):\n- `target` (required): Primary IPv4/IPv6/hostname/CIDR or bulk newline list routed into network scanners and RPC clients.\n- `record_types` (optional): dig driver issues these RR types sequentially against `target`.\n- `timeout` (optional): Seconds for socket/DNS/CLI overall timeouts."
  },
  "dirb": {
    "long_description": "DIRB brute-forces web paths usingclassic wordlists and optional fine-grained HTTP codes mirroring circa-2000 scanners still common in engagements.\n\nCore utilities return concise stdout suited for piping into scanners or parsers (DNS answers, registrar text, binaries, transports).\n\nReserve essential connectors for repeatable plumbing when higher-level arsenals obscure parameters you must control explicitly.\n\nEven ancillary utilities honour the same egress rules\u2014do not widen targets implicitly via glob inputs.\n\nCipherStrike transmits the JSON envelope to `/api/tools/dirb`; NyxStrike performs final validation, then shells out or calls the bundled integration advertised for this catalogue id.",
    "safety": "Operate only inside authorized penetration tests or sanctioned lab networks. Respect stop conditions, lawful intercept rules, and internal change windows. Sensitive parameters are captured in CipherStrike tenant execution logs alongside NyxStrike response snippets \u2014 avoid pasting production secrets when alternatives exist.",
    "usage": "CipherStrike `dirb` executes against `/api/tools/dirb` via POST JSON envelopes assembled in the Execute modal.\n\n1. Provide `url` or `target` with scheme, optional `wordlist`, and HTTP tuning knobs (`threads`, `extensions`, `status_codes`).\n2. Start with a small wordlist to validate scope, then scale\u2014large runs resemble DDoS traffic.\n3. Compare HTTP status, response length, and redirect chains; manual confirmation kills false positives.\n4. `additional_args` should only carry flags missing from the JSON mapper.\n\nCipherStrike mechanics: send a JSON POST from the Execute modal\u2014the agent drops empty optional scalars before invoking NyxStrike.\nPrefer explicit catalogue fields (`target`, `url`, `wordlist`, etc.) before `additional_args`; extra tokens must still match the packaged wrapper behaviour and your engagement rules.\nPer-field catalogue hints (first sentence of modal help):\n- `url` (required): Fully qualified HTTP/S URL for crawlers, fuzzers, or vuln scanners (scheme required).\n- `wordlist` (optional): Agent-resident newline wordlist for content/DNS/password tooling.\n- `additional_args` (optional): Shell tokens appended after mapped CLI arguments."
  },
  "dirsearch": {
    "long_description": "dirsearch multithreads extension-aware directory busting using advanced filters, blacklist rules, recursion, and customised wordlists favoured in modern web assessments.\n\nOutput usually enumerates reachable paths, crawler graphs, fingerprints, robots hints, deduplicated URLs, or header snapshots.\n\nCombine directory bruteforce, spidering, and header probes sequentially: surface mapping before attempting injection payloads.\n\nMisconfigured crawlers crawl out of scope rapidly\u2014align seed URLs, blacklist regexes, and depth caps with authorised programs.\n\nCipherStrike transmits the JSON envelope to `/api/tools/dirsearch`; NyxStrike performs final validation, then shells out or calls the bundled integration advertised for this catalogue id.",
    "safety": "Operate only inside authorized penetration tests or sanctioned lab networks. Respect stop conditions, lawful intercept rules, and internal change windows. Sensitive parameters are captured in CipherStrike tenant execution logs alongside NyxStrike response snippets \u2014 avoid pasting production secrets when alternatives exist.",
    "usage": "CipherStrike `dirsearch` executes against `/api/tools/dirsearch` via POST JSON envelopes assembled in the Execute modal.\n\n1. Supply base URL, extensions, recursion, thread, and filter options mirroring dirsearch semantics.\n2. Seed wordlists appropriate to the technology stack (API vs static site).\n3. Watch for WAF captcha responses\u2014back off when HTTP 403 patterns repeat.\n4. Export hits to spreadsheets for manual verification.\n\nCipherStrike mechanics: send a JSON POST from the Execute modal\u2014the agent drops empty optional scalars before invoking NyxStrike.\nPrefer explicit catalogue fields (`target`, `url`, `wordlist`, etc.) before `additional_args`; extra tokens must still match the packaged wrapper behaviour and your engagement rules.\nPer-field catalogue hints (first sentence of modal help):\n- `url` (required): Fully qualified HTTP/S URL for crawlers, fuzzers, or vuln scanners (scheme required).\n- `additional_args` (optional): Shell tokens appended after mapped CLI arguments."
  },
  "dnsenum": {
    "long_description": "dnsenum threads zone transfers, brute-force guesses, wildcard detection, reverse lookups, and WHOIS-guided pivoting reminiscent of Perl-era DNS auditors.\n\nResults are textual lists, JSON artefacts, archive URLs, or passive reconnaissance breadcrumbs suitable for ingestion into trackers.\n\nOSINT tooling should precede intrusive scanning wherever policy demands minimal touch; escalate to DNS brute or crawler stages only after validating scope wording.\n\nPassive APIs throttle aggressively and may log lookups under shared keys\u2014credential handling matters.\n\nCipherStrike transmits the JSON envelope to `/api/tools/dnsenum`; NyxStrike performs final validation, then shells out or calls the bundled integration advertised for this catalogue id.",
    "safety": "Operate only inside authorized penetration tests or sanctioned lab networks. Respect stop conditions, lawful intercept rules, and internal change windows. Sensitive parameters are captured in CipherStrike tenant execution logs alongside NyxStrike response snippets \u2014 avoid pasting production secrets when alternatives exist.",
    "usage": "CipherStrike `dnsenum` executes against `/api/tools/dnsenum` via POST JSON envelopes assembled in the Execute modal.\n\n1. Supply domain lists, brute toggles, and recursion depth aligning with fierce/dnsenum templates.\n2. Attempt zone transfers only against assets you administer.\n3. Log wildcard detections\u2014they influence brute noise.\n4. Pair with Resolver logs for anomalies.\n\nCipherStrike mechanics: send a JSON POST from the Execute modal\u2014the agent drops empty optional scalars before invoking NyxStrike.\nPrefer explicit catalogue fields (`target`, `url`, `wordlist`, etc.) before `additional_args`; extra tokens must still match the packaged wrapper behaviour and your engagement rules.\nPer-field catalogue hints (first sentence of modal help):\n- `domain` (required): Authorised DNS apex fed to Amass/subfinder/GAU style OSINT connectors.\n- `additional_args` (optional): Shell tokens appended after mapped CLI arguments."
  },
  "docker-bench-security": {
    "long_description": "Docker bench implements CIS Docker host guidance checks selectively via `checks`/`exclude` lists.\n\nCloud posture connectors emit JSON/HTML/SARIF findings enumerating IAM sprawl, open buckets, CIS deviations, stale keys, and attack-path narratives.\n\nCloud scans require least-privilege API roles scoped to enumerated accounts\u2014not root keys embedded in plaintext JSON long term.\n\nProvider APIs log every call; minimise repeated broad scans and purge credentials promptly after tabletop windows.\n\nCipherStrike transmits the JSON envelope to `/api/tools/docker-bench-security`; NyxStrike performs final validation, then shells out or calls the bundled integration advertised for this catalogue id.",
    "safety": "Operate only inside authorized penetration tests or sanctioned lab networks. Respect stop conditions, lawful intercept rules, and internal change windows. Sensitive parameters are captured in CipherStrike tenant execution logs alongside NyxStrike response snippets \u2014 avoid pasting production secrets when alternatives exist. Cloud API tokens authenticate broad actions\u2014purge keys after tabletop exercises.",
    "usage": "CipherStrike `docker-bench-security` executes against `/api/tools/docker-bench-security` via POST JSON envelopes assembled in the Execute modal.\n\n1. Provide kubeconfig paths image refs benchmark ids directories matching Trivy/Checkov/kube-bench/Clair/Falco wrappers.\n2. Align severities/report formats with SOC ingestion pipelines.\n3. Remediation tips require owner teams\u2014don't auto-patch.\n4. Some scans mutate nothing; others exec inside clusters\u2014confirm blast radius.\n\nCipherStrike mechanics: send a JSON POST from the Execute modal\u2014the agent drops empty optional scalars before invoking NyxStrike.\nPrefer explicit catalogue fields (`target`, `url`, `wordlist`, etc.) before `additional_args`; extra tokens must still match the packaged wrapper behaviour and your engagement rules.\nPer-field catalogue hints (first sentence of modal help):\n- `checks` (optional): CSV benchmark control ids (docker-bench), Prowler check ids, **or** Schemathesis check names\u2014depends on route.\n- `exclude` (optional): The workspace forwards `exclude` in the CipherStrike POST body unchanged after stripping empty optional fields.\n- `output_file` (optional): Explicit output path for logs/PCAPNG/JSON when CLIs support `-o`.\n- `additional_args` (optional): Shell tokens appended after mapped CLI arguments."
  },
  "dotdotpwn": {
    "long_description": "DotDotPwn traverses traversal fuzzing payloads across protocols (HTTP/FTP/etc.) referencing historic canonical traversal patterns for identifying path normalisation flaws.\n\nOutput usually enumerates reachable paths, crawler graphs, fingerprints, robots hints, deduplicated URLs, or header snapshots.\n\nCombine directory bruteforce, spidering, and header probes sequentially: surface mapping before attempting injection payloads.\n\nMisconfigured crawlers crawl out of scope rapidly\u2014align seed URLs, blacklist regexes, and depth caps with authorised programs.\n\nCipherStrike transmits the JSON envelope to `/api/tools/dotdotpwn`; NyxStrike performs final validation, then shells out or calls the bundled integration advertised for this catalogue id.",
    "safety": "Operate only inside authorized penetration tests or sanctioned lab networks. Respect stop conditions, lawful intercept rules, and internal change windows. Sensitive parameters are captured in CipherStrike tenant execution logs alongside NyxStrike response snippets \u2014 avoid pasting production secrets when alternatives exist.",
    "usage": "CipherStrike `dotdotpwn` executes against `/api/tools/dotdotpwn` via POST JSON envelopes assembled in the Execute modal.\n\n1. Provide protocol-specific targets (HTTP, FTP, etc.) per the packaged dotdotpwn profile.\n2. Choose module and depth options conservatively\u2014traversal payloads may read sensitive files.\n3. Stop immediately when evidence proves impact; export logs for remediation teams.\n4. Do not aim traversal templates at unmanaged third-party estates.\n\nCipherStrike mechanics: send a JSON POST from the Execute modal\u2014the agent drops empty optional scalars before invoking NyxStrike.\nPrefer explicit catalogue fields (`target`, `url`, `wordlist`, etc.) before `additional_args`; extra tokens must still match the packaged wrapper behaviour and your engagement rules.\nPer-field catalogue hints (first sentence of modal help):\n- `target` (required): Primary IPv4/IPv6/hostname/CIDR or bulk newline list routed into network scanners and RPC clients.\n- `additional_args` (optional): Shell tokens appended after mapped CLI arguments."
  },
  "eaphammer": {
    "long_description": "EAPHammer stages rogue WPA/WPA2-Enterprise portals, credential relays, karma attacks, captive-portal payloads, negotiation attacks, and cert plumbing for controlled WPA-EAP lab assessments.\n\n802.11 harnesses emit captures, WPA hash material, rogue AP scaffolding, injector acknowledgements, and audit logs keyed to channels/BSSIDs.\n\nLaboratory rigs should dedicate monitor-mode interfaces plus isolated spectrum; tethered enterprise APs rarely tolerate injection homework.\n\nJamming neighbours or intercepting unauthorised traffic violates law\u2014operate strictly inside sanctioned RF enclosures and licensed bands.\n\nCipherStrike transmits the JSON envelope to `/api/tools/wifi_pentest/eaphammer`; NyxStrike performs final validation, then shells out or calls the bundled integration advertised for this catalogue id.",
    "safety": "Operate only inside authorized penetration tests or sanctioned lab networks. Respect stop conditions, lawful intercept rules, and internal change windows. Sensitive parameters are captured in CipherStrike tenant execution logs alongside NyxStrike response snippets \u2014 avoid pasting production secrets when alternatives exist. Wireless probing and injection are lawful only on spectrum and networks you control by license or contract. Enterprise downgrades impact corporate Wi-Fi\u2014only attempt with written spectrum control.",
    "usage": "CipherStrike `eaphammer` executes against `/api/tools/wifi_pentest/eaphammer` via POST JSON envelopes assembled in the Execute modal.\n\n1. Place adapters in monitor mode upstream when required (`airmon-ng` helper routes).\n2. Provide capture paths, ESSIDs/EAP configs, handshake targets, attack modes, regulatory paperwork references.\n3. Never point attack modes at civilian infrastructure\u2014hardware isolation only.\n4. Convert captures only within labs; distributing PMKIDs may violate telecom rules.\n\nCipherStrike mechanics: send a JSON POST from the Execute modal\u2014the agent drops empty optional scalars before invoking NyxStrike.\nPrefer explicit catalogue fields (`target`, `url`, `wordlist`, etc.) before `additional_args`; extra tokens must still match the packaged wrapper behaviour and your engagement rules.\nPer-field catalogue hints (first sentence of modal help):\n- `interface` (required): Network interface (`wlan0mon`, `eth0`) for Wi\u2011Fi or Responder jobs.\n- `essid` (required): SSID string for handshake correlation or rogue APs.\n- `channel` (optional): 802.11 channel for captures or rogue AP broadcasts.\n- `auth_mode` (optional): EAPHammer soft-AP authentication profile (OPEN, WPA2-PSK, enterprise).\n- `attack_type` (optional): MDK4/EAPHammer attack choreography identifier.\n- `negotiate` (optional): EAPHammer/downgrade knobs affecting WPA-enterprise negotiation behaviours.\n- `cert_path` (optional): Agent filesystem path for `cert_path` consumed by the connector."
  },
  "enum4linux": {
    "long_description": "Classic Perl aggregator around Samba tooling that enumerates SMB/RPC users, shares, groups, transports, policies, and brute-force-able RIDs analogous to legacy Windows enum.exe output.\n\nLegacy enum4linux lumps rpcclient net users smbclient heuristics into Perl-era tables.\n\nPrefer enum4linux-ng unless you deliberately reproduce classical script output for regressions.\n\nNull sessions rarely succeed on modern domains\u2014supply credentialed context when mandated.\n\nCipherStrike transmits the JSON envelope to `/api/tools/enum4linux`; NyxStrike performs final validation, then shells out or calls the bundled integration advertised for this catalogue id.",
    "safety": "Operate only inside authorized penetration tests or sanctioned lab networks. Respect stop conditions, lawful intercept rules, and internal change windows. Sensitive parameters are captured in CipherStrike tenant execution logs alongside NyxStrike response snippets \u2014 avoid pasting production secrets when alternatives exist. Port scanners and handshake grabbers alert defenders\u2014mirror scope IPs exactly as authorised.",
    "usage": "CipherStrike `enum4linux` executes against `/api/tools/enum4linux` via POST JSON envelopes assembled in the Execute modal.\n\n1. Supply `target` for the Windows/Samba host and optional credentials if null sessions are disabled.\n2. Enable verb-style toggles for users, shares, or policy dumps as exposed by the JSON schema.\n3. Expect noisy RPC chatter\u2014run during approved windows and capture stdout for reporting.\n4. Pair output with `enum4linux-ng` or `nxc` when modern controls block legacy behaviours.\n\nCipherStrike mechanics: send a JSON POST from the Execute modal\u2014the agent drops empty optional scalars before invoking NyxStrike.\nPrefer explicit catalogue fields (`target`, `url`, `wordlist`, etc.) before `additional_args`; extra tokens must still match the packaged wrapper behaviour and your engagement rules.\nPer-field catalogue hints (first sentence of modal help):\n- `target` (required): Primary IPv4/IPv6/hostname/CIDR or bulk newline list routed into network scanners and RPC clients.\n- `additional_args` (optional): Shell tokens appended after mapped CLI arguments."
  },
  "enum4linux-ng": {
    "long_description": "enum4linux-ng modernises Rid cycling, Kerberos interplay, regex-friendly JSON summaries, and granular toggles covering users, shares, groups, policies, etc.\n\nenum4linux-ng emits JSON-friendly summaries Rid cycles Kerberos aware toggles granular share maps.\n\nDefault to NG for greener assessments; fallback to legacy enum4linux only when parsers expect historical formats.\n\nAggressive Rid cycling trips detections throttle consciously.\n\nCipherStrike transmits the JSON envelope to `/api/tools/enum4linux-ng`; NyxStrike performs final validation, then shells out or calls the bundled integration advertised for this catalogue id.",
    "safety": "Operate only inside authorized penetration tests or sanctioned lab networks. Respect stop conditions, lawful intercept rules, and internal change windows. Sensitive parameters are captured in CipherStrike tenant execution logs alongside NyxStrike response snippets \u2014 avoid pasting production secrets when alternatives exist. Port scanners and handshake grabbers alert defenders\u2014mirror scope IPs exactly as authorised.",
    "usage": "CipherStrike `enum4linux-ng` executes against `/api/tools/enum4linux-ng` via POST JSON envelopes assembled in the Execute modal.\n\n1. Provide `target`, optional creds, and Kerberos-related toggles exactly as the wrapper documents.\n2. Prefer JSON mode when available for downstream parsing; fall back to classic text when comparing with historic reports.\n3. Validate sensitive share paths manually before attempting write tests elsewhere.\n4. Use `additional_args` only for niche switches after reproducing them manually on a lab DC.\n\nCipherStrike mechanics: send a JSON POST from the Execute modal\u2014the agent drops empty optional scalars before invoking NyxStrike.\nPrefer explicit catalogue fields (`target`, `url`, `wordlist`, etc.) before `additional_args`; extra tokens must still match the packaged wrapper behaviour and your engagement rules.\nPer-field catalogue hints (first sentence of modal help):\n- `target` (required): Primary IPv4/IPv6/hostname/CIDR or bulk newline list routed into network scanners and RPC clients.\n- `username` (optional): Username for SMB/LDAP/WinRM clients and login brute modules.\n- `password` (optional): Cleartext password for authenticated scans\u2014rotate if leaked into logs.\n- `domain` (optional): Authorised DNS apex fed to Amass/subfinder/GAU style OSINT connectors.\n- `shares` (optional): The workspace forwards `shares` in the CipherStrike POST body unchanged after stripping empty optional fields.\n- `users` (optional): The workspace forwards `users` in the CipherStrike POST body unchanged after stripping empty optional fields.\n- `groups` (optional): The workspace forwards `groups` in the CipherStrike POST body unchanged after stripping empty optional fields.\n- `policy` (optional): The workspace forwards `policy` in the CipherStrike POST body unchanged after stripping empty optional fields.\n- `additional_args` (optional): Shell tokens appended after mapped CLI arguments."
  },
  "evil-winrm": {
    "long_description": "Evil-WinRM supplies Ruby-based WinRM shells with pass-the-hash, logging, AMSI-ish toggles, and script execution aides.\n\nExpect port listings, traceroute-like hints, service banners, NIC inventories, handshake captures, or ARP inventories depending on tooling.\n\nFast SYN scanners suit huge ranges; balanced scanners add scripts and versioning; SMB-oriented tools deepen Windows visibility\u2014choose based on scope scale and covertness mandates.\n\nHigh packet rates overwhelm small networks\u2014rate-limit, subdivide subnets, and obtain written approval before touching neighbouring tenants.\n\nCipherStrike transmits the JSON envelope to `/api/tools/evil_winrm`; NyxStrike performs final validation, then shells out or calls the bundled integration advertised for this catalogue id.",
    "safety": "Operate only inside authorized penetration tests or sanctioned lab networks. Respect stop conditions, lawful intercept rules, and internal change windows. Sensitive parameters are captured in CipherStrike tenant execution logs alongside NyxStrike response snippets \u2014 avoid pasting production secrets when alternatives exist. Port scanners and handshake grabbers alert defenders\u2014mirror scope IPs exactly as authorised. Interactive shells alter target state\u2014use evidence-friendly logging.",
    "usage": "CipherStrike `evil-winrm` executes against `/api/tools/evil_winrm` via POST JSON envelopes assembled in the Execute modal.\n\n1. Populate `username`, optional `password`/`hash`, `target_ip`, TLS toggles referencing Evil-WinRM mapping.\n2. Interactive shells behave like SSH\u2014everything logs server-side.\n3. Upload pathways may trip AV document binaries.\n4. Close sessions politely to flush transcripts.\n\nCipherStrike mechanics: send a JSON POST from the Execute modal\u2014the agent drops empty optional scalars before invoking NyxStrike.\nPrefer explicit catalogue fields (`target`, `url`, `wordlist`, etc.) before `additional_args`; extra tokens must still match the packaged wrapper behaviour and your engagement rules.\nPer-field catalogue hints (first sentence of modal help):\n- `target` (required): Primary IPv4/IPv6/hostname/CIDR or bulk newline list routed into network scanners and RPC clients.\n- `username` (required): Username for SMB/LDAP/WinRM clients and login brute modules.\n- `password` (optional): Cleartext password for authenticated scans\u2014rotate if leaked into logs.\n- `hash` (optional): Verifier string for Evil-WinRM / NetExec pass-the-hash transports.\n- `additional_args` (optional): Shell tokens appended after mapped CLI arguments."
  },
  "exiftool": {
    "long_description": "exiftool reads/writes EXIF/IPTC/XMP tags across multimedia formats aiding metadata leak reviews.\n\nForensic tooling streams carved files, timelines, entropy maps, PCAP decodes, volatility tables, SQLite extractions.\n\nForensic artefacts may contain HIPAA/PII regulated content\u2014encrypt exports immediately.\n\nResource-heavy jobs need disk-bound IO quotas; carve only targeted ranges when multi-terabyte images are mounted.\n\nCipherStrike transmits the JSON envelope to `/api/tools/exiftool`; NyxStrike performs final validation, then shells out or calls the bundled integration advertised for this catalogue id.",
    "safety": "Operate only inside authorized penetration tests or sanctioned lab networks. Respect stop conditions, lawful intercept rules, and internal change windows. Sensitive parameters are captured in CipherStrike tenant execution logs alongside NyxStrike response snippets \u2014 avoid pasting production secrets when alternatives exist. Evidence images may contain regulated data\u2014encrypt at rest immediately after extraction.",
    "usage": "CipherStrike `exiftool` executes against `/api/tools/exiftool` via POST JSON envelopes assembled in the Execute modal.\n\n1. Submit `file_path`, tag selections, recursion toggles aligning with ExifTool wrappers.\n2. Avoid writing GPS metadata creep into leaked evidence.\n3. Use read-only semantics unless charter allows mutation.\n4. Keep backups prior to rewriting metadata.\n\nCipherStrike mechanics: send a JSON POST from the Execute modal\u2014the agent drops empty optional scalars before invoking NyxStrike.\nPrefer explicit catalogue fields (`target`, `url`, `wordlist`, etc.) before `additional_args`; extra tokens must still match the packaged wrapper behaviour and your engagement rules.\nPer-field catalogue hints (first sentence of modal help):\n- `file_path` (required): Readable path for xxd/exiftool style utilities.\n- `output_format` (optional): Structured report format (`json`, `xml`, `sarif`, `html`).\n- `tags` (optional): exiftool tag projection or scanner tag filters.\n- `additional_args` (optional): Shell tokens appended after mapped CLI arguments."
  },
  "falco": {
    "long_description": "Falco hooks syscalls emitting JSON/SHELL alerts for container breakout signals with optional ephemeral capture windows (`duration`).\n\nCloud posture connectors emit JSON/HTML/SARIF findings enumerating IAM sprawl, open buckets, CIS deviations, stale keys, and attack-path narratives.\n\nCloud scans require least-privilege API roles scoped to enumerated accounts\u2014not root keys embedded in plaintext JSON long term.\n\nProvider APIs log every call; minimise repeated broad scans and purge credentials promptly after tabletop windows.\n\nCipherStrike transmits the JSON envelope to `/api/tools/falco`; NyxStrike performs final validation, then shells out or calls the bundled integration advertised for this catalogue id.",
    "safety": "Operate only inside authorized penetration tests or sanctioned lab networks. Respect stop conditions, lawful intercept rules, and internal change windows. Sensitive parameters are captured in CipherStrike tenant execution logs alongside NyxStrike response snippets \u2014 avoid pasting production secrets when alternatives exist. Cloud API tokens authenticate broad actions\u2014purge keys after tabletop exercises.",
    "usage": "CipherStrike `falco` executes against `/api/tools/falco` via POST JSON envelopes assembled in the Execute modal.\n\n1. Provide kubeconfig paths image refs benchmark ids directories matching Trivy/Checkov/kube-bench/Clair/Falco wrappers.\n2. Align severities/report formats with SOC ingestion pipelines.\n3. Remediation tips require owner teams\u2014don't auto-patch.\n4. Some scans mutate nothing; others exec inside clusters\u2014confirm blast radius.\n\nCipherStrike mechanics: send a JSON POST from the Execute modal\u2014the agent drops empty optional scalars before invoking NyxStrike.\nPrefer explicit catalogue fields (`target`, `url`, `wordlist`, etc.) before `additional_args`; extra tokens must still match the packaged wrapper behaviour and your engagement rules.\nPer-field catalogue hints (first sentence of modal help):\n- `config_file` (optional): Alternate YAML for Falco, Clair, or service-specific configs.\n- `rules_file` (optional): Supplemental Falco rule pack path.\n- `output_format` (optional): Structured report format (`json`, `xml`, `sarif`, `html`).\n- `duration` (optional): Runtime cap for Responder poison loops or Falco capture windows.\n- `additional_args` (optional): Shell tokens appended after mapped CLI arguments."
  },
  "feroxbuster": {
    "long_description": "feroxbuster is a recursive Rust content discovery crawler that honours depth semantics, concurrency controls, and response heuristics without manual script glue.\n\nFeroxbuster streams recursive crawler aware responses depth aware heuristics.\n\nPrefer feroxbuster over gobuster ffuf combos when recursion depth concurrency must stay unified.\n\nRecursive mis-scoping enumerates unintended vhosts blacklist explicitly.\n\nCipherStrike transmits the JSON envelope to `/api/tools/feroxbuster`; NyxStrike performs final validation, then shells out or calls the bundled integration advertised for this catalogue id.",
    "safety": "Operate only inside authorized penetration tests or sanctioned lab networks. Respect stop conditions, lawful intercept rules, and internal change windows. Sensitive parameters are captured in CipherStrike tenant execution logs alongside NyxStrike response snippets \u2014 avoid pasting production secrets when alternatives exist.",
    "usage": "CipherStrike `feroxbuster` executes against `/api/tools/feroxbuster` via POST JSON envelopes assembled in the Execute modal.\n\n1. Seed with the highest privileged base URL allowed, then tune `depth`, `threads`, and status filters.\n2. Monitor disk usage\u2014recursive crawlers fan out quickly on content-heavy apps.\n3. De-duplicate against `uro`/`anew` pipelines before feeding nuclei.\n4. Validate out-of-scope link filters before launching overnight jobs.\n\nCipherStrike mechanics: send a JSON POST from the Execute modal\u2014the agent drops empty optional scalars before invoking NyxStrike.\nPrefer explicit catalogue fields (`target`, `url`, `wordlist`, etc.) before `additional_args`; extra tokens must still match the packaged wrapper behaviour and your engagement rules.\nPer-field catalogue hints (first sentence of modal help):\n- `url` (required): Fully qualified HTTP/S URL for crawlers, fuzzers, or vuln scanners (scheme required).\n- `wordlist` (optional): Agent-resident newline wordlist for content/DNS/password tooling.\n- `threads` (optional): Concurrent workers for ffuf/arjun/sublist3r/feroxbuster HTTP jobs.\n- `additional_args` (optional): Shell tokens appended after mapped CLI arguments."
  },
  "ffuf": {
    "long_description": "ffuf (Fuzz Faster U Fool) substitutes the `FUZZ` keyword across verbs, routes, headers, and bodies enabling multi-slot fuzz workflows.\n\nffuf outputs tabular fuzz results keyed by fuzz slots across directions, statuses, sizes, redirects, and complementary metadata when matchers are tuned.\n\nffuf excels at simultaneous verb header route body fuzz nucleation versus gobuster simplicity.\n\nHigh concurrency trips WAFs align threads delay.\n\nCipherStrike transmits the JSON envelope to `/api/tools/ffuf`; NyxStrike performs final validation, then shells out or calls the bundled integration advertised for this catalogue id.",
    "safety": "Operate only inside authorized penetration tests or sanctioned lab networks. Respect stop conditions, lawful intercept rules, and internal change windows. Sensitive parameters are captured in CipherStrike tenant execution logs alongside NyxStrike response snippets \u2014 avoid pasting production secrets when alternatives exist.",
    "usage": "CipherStrike `ffuf` executes against `/api/tools/ffuf` via POST JSON envelopes assembled in the Execute modal.\n\n1. Define the `FUZZ`/`FFUF` keyword placement in `url`, headers, or data fields per this connector\u2019s template.\n2. Load wordlists and configure matchers/filters to ignore static 404 pages.\n3. Keep concurrency aligned with WAF rate limits; use `delay` options when present.\n4. Log successful fuzz slots for replay in Burp or ZAP if deeper manual testing is required.\n\nCipherStrike mechanics: send a JSON POST from the Execute modal\u2014the agent drops empty optional scalars before invoking NyxStrike.\nPrefer explicit catalogue fields (`target`, `url`, `wordlist`, etc.) before `additional_args`; extra tokens must still match the packaged wrapper behaviour and your engagement rules.\nPer-field catalogue hints (first sentence of modal help):\n- `url` (required): Fully qualified HTTP/S URL for crawlers, fuzzers, or vuln scanners (scheme required).\n- `wordlist` (optional): Agent-resident newline wordlist for content/DNS/password tooling.\n- `mode` (optional): High-level tool mode (Amass `enum`, gobuster `dir`, shuffleDNS profile, etc.).\n- `match_codes` (optional): The workspace forwards `match_codes` in the CipherStrike POST body unchanged after stripping empty optional fields.\n- `additional_args` (optional): Shell tokens appended after mapped CLI arguments."
  },
  "fierce": {
    "long_description": "fierce automates recursive DNS brute with zone-transfer attempts, traversal options, and heuristic domain walking described in canonical DNS penetration references.\n\nResults are textual lists, JSON artefacts, archive URLs, or passive reconnaissance breadcrumbs suitable for ingestion into trackers.\n\nOSINT tooling should precede intrusive scanning wherever policy demands minimal touch; escalate to DNS brute or crawler stages only after validating scope wording.\n\nPassive APIs throttle aggressively and may log lookups under shared keys\u2014credential handling matters.\n\nCipherStrike transmits the JSON envelope to `/api/tools/fierce`; NyxStrike performs final validation, then shells out or calls the bundled integration advertised for this catalogue id.",
    "safety": "Operate only inside authorized penetration tests or sanctioned lab networks. Respect stop conditions, lawful intercept rules, and internal change windows. Sensitive parameters are captured in CipherStrike tenant execution logs alongside NyxStrike response snippets \u2014 avoid pasting production secrets when alternatives exist.",
    "usage": "CipherStrike `fierce` executes against `/api/tools/fierce` via POST JSON envelopes assembled in the Execute modal.\n\n1. Supply domain lists, brute toggles, and recursion depth aligning with fierce/dnsenum templates.\n2. Attempt zone transfers only against assets you administer.\n3. Log wildcard detections\u2014they influence brute noise.\n4. Pair with Resolver logs for anomalies.\n\nCipherStrike mechanics: send a JSON POST from the Execute modal\u2014the agent drops empty optional scalars before invoking NyxStrike.\nPrefer explicit catalogue fields (`target`, `url`, `wordlist`, etc.) before `additional_args`; extra tokens must still match the packaged wrapper behaviour and your engagement rules.\nPer-field catalogue hints (first sentence of modal help):\n- `domain` (required): Authorised DNS apex fed to Amass/subfinder/GAU style OSINT connectors.\n- `additional_args` (optional): Shell tokens appended after mapped CLI arguments."
  },
  "file": {
    "long_description": "libmagic `file` fingerprints binary types/magic descriptors ahead of toolchain-specific unpacking.\n\nForensic tooling streams carved files, timelines, entropy maps, PCAP decodes, volatility tables, SQLite extractions.\n\nForensic artefacts may contain HIPAA/PII regulated content\u2014encrypt exports immediately.\n\nResource-heavy jobs need disk-bound IO quotas; carve only targeted ranges when multi-terabyte images are mounted.\n\nCipherStrike transmits the JSON envelope to `/api/tools/file_type`; NyxStrike performs final validation, then shells out or calls the bundled integration advertised for this catalogue id.",
    "safety": "Operate only inside authorized penetration tests or sanctioned lab networks. Respect stop conditions, lawful intercept rules, and internal change windows. Sensitive parameters are captured in CipherStrike tenant execution logs alongside NyxStrike response snippets \u2014 avoid pasting production secrets when alternatives exist. Evidence images may contain regulated data\u2014encrypt at rest immediately after extraction.",
    "usage": "CipherStrike `file` executes against `/api/tools/file_type` via POST JSON envelopes assembled in the Execute modal.\n\n1. Provide `binary`, `file_path`, or CLI-equivalent selectors for quick static insight.\n2. Use triage utilities before heavyweight Ghidra/autopsy timelines.\n3. Outputs are succinct\u2014combine with checklist reporting.\n4. Malware artefacts remain hazardous\u2014sandbox hosts accordingly.\n\nCipherStrike mechanics: send a JSON POST from the Execute modal\u2014the agent drops empty optional scalars before invoking NyxStrike.\nPrefer explicit catalogue fields (`target`, `url`, `wordlist`, etc.) before `additional_args`; extra tokens must still match the packaged wrapper behaviour and your engagement rules.\nPer-field catalogue hints (first sentence of modal help):\n- `file_path` (required): Readable path for xxd/exiftool style utilities.\n- `additional_args` (optional): Shell tokens appended after mapped CLI arguments."
  },
  "foremost": {
    "long_description": "foremost carves predefined file types via header/footer heuristics from raw images akin to DD dumps.\n\nForensic tooling streams carved files, timelines, entropy maps, PCAP decodes, volatility tables, SQLite extractions.\n\nForensic artefacts may contain HIPAA/PII regulated content\u2014encrypt exports immediately.\n\nResource-heavy jobs need disk-bound IO quotas; carve only targeted ranges when multi-terabyte images are mounted.\n\nCipherStrike transmits the JSON envelope to `/api/tools/foremost`; NyxStrike performs final validation, then shells out or calls the bundled integration advertised for this catalogue id.",
    "safety": "Operate only inside authorized penetration tests or sanctioned lab networks. Respect stop conditions, lawful intercept rules, and internal change windows. Sensitive parameters are captured in CipherStrike tenant execution logs alongside NyxStrike response snippets \u2014 avoid pasting production secrets when alternatives exist. Evidence images may contain regulated data\u2014encrypt at rest immediately after extraction.",
    "usage": "CipherStrike `foremost` executes against `/api/tools/foremost` via POST JSON envelopes assembled in the Execute modal.\n\n1. Point `input_file`/`image_path`/`output_dir` fields at sanctioned evidence stores.\n2. Choose carving signatures conservatively\u2014broad signatures balloon storage.\n3. Keep chain-of-custody notes for every exported file.\n4. Some carvers overwrite slack space\u2014snapshot images first.\n\nCipherStrike mechanics: send a JSON POST from the Execute modal\u2014the agent drops empty optional scalars before invoking NyxStrike.\nPrefer explicit catalogue fields (`target`, `url`, `wordlist`, etc.) before `additional_args`; extra tokens must still match the packaged wrapper behaviour and your engagement rules.\nPer-field catalogue hints (first sentence of modal help):\n- `input_file` (required): Evidence blob for foremost/scalpel/testdisk carvers.\n- `output_dir` (optional): Directory root for AutoRecon, foremost, Scout exports.\n- `file_types` (optional): The workspace forwards `file_types` in the CipherStrike POST body unchanged after stripping empty optional fields.\n- `additional_args` (optional): Shell tokens appended after mapped CLI arguments."
  },
  "gau": {
    "long_description": "getallurls (tomnomnom/link discovery) merges AlienVault Open Threat Exchange, FDNS, Archive, Common Crawl snapshots for historical URL inventories.\n\nResults are textual lists, JSON artefacts, archive URLs, or passive reconnaissance breadcrumbs suitable for ingestion into trackers.\n\nOSINT tooling should precede intrusive scanning wherever policy demands minimal touch; escalate to DNS brute or crawler stages only after validating scope wording.\n\nPassive APIs throttle aggressively and may log lookups under shared keys\u2014credential handling matters.\n\nCipherStrike transmits the JSON envelope to `/api/tools/gau`; NyxStrike performs final validation, then shells out or calls the bundled integration advertised for this catalogue id.",
    "safety": "Operate only inside authorized penetration tests or sanctioned lab networks. Respect stop conditions, lawful intercept rules, and internal change windows. Sensitive parameters are captured in CipherStrike tenant execution logs alongside NyxStrike response snippets \u2014 avoid pasting production secrets when alternatives exist.",
    "usage": "CipherStrike `gau` executes against `/api/tools/gau` via POST JSON envelopes assembled in the Execute modal.\n\n1. Provide `domains`/`domain`/`input` equivalents per GAU/WaybackURL connector mapping.\n2. Filter archives to scope hostnames programmatically afterwards.\n3. Archives may expose retired credentials\u2014handle carefully.\n4. Rate-limit concurrency to obey remote archive policies.\n\nCipherStrike mechanics: send a JSON POST from the Execute modal\u2014the agent drops empty optional scalars before invoking NyxStrike.\nPrefer explicit catalogue fields (`target`, `url`, `wordlist`, etc.) before `additional_args`; extra tokens must still match the packaged wrapper behaviour and your engagement rules.\nPer-field catalogue hints (first sentence of modal help):\n- `domain` (required): Authorised DNS apex fed to Amass/subfinder/GAU style OSINT connectors.\n- `additional_args` (optional): Shell tokens appended after mapped CLI arguments."
  },
  "gdb": {
    "long_description": "GNU gdb debugs userland binaries with command macros, optional command files, and scripted breakpoint automation.\n\nDebuggers/disassemblers/gadget hunters produce listings, scripted traces, annotated graphs, emulation logs, or CTF scaffolding.\n\nStatic analysis excels on offline artefacts; symbolic tooling demands compute and may path-explode on large binaries.\n\nMalware-grade samples may trigger host controls\u2014sandbox accordingly and sanitise artefacts before sharing externally.\n\nCipherStrike transmits the JSON envelope to `/api/tools/gdb`; NyxStrike performs final validation, then shells out or calls the bundled integration advertised for this catalogue id.",
    "safety": "Operate only inside authorized penetration tests or sanctioned lab networks. Respect stop conditions, lawful intercept rules, and internal change windows. Sensitive parameters are captured in CipherStrike tenant execution logs alongside NyxStrike response snippets \u2014 avoid pasting production secrets when alternatives exist. Binary tooling may invoke malware safeguards\u2014sandbox untrusted specimens.",
    "usage": "CipherStrike `gdb` executes against `/api/tools/gdb` via POST JSON envelopes assembled in the Execute modal.\n\n1. Pass file paths macros scripts analysis toggles aligning with gdb/radare2/ghidra headless wrappers.\n2. Long analyses need `timeout` fields\u2014coordinate with infra.\n3. Store project directories on encrypted volumes.\n4. Symbolic backends may spin CPU\u2014quota jobs fairly.\n\nCipherStrike mechanics: send a JSON POST from the Execute modal\u2014the agent drops empty optional scalars before invoking NyxStrike.\nPrefer explicit catalogue fields (`target`, `url`, `wordlist`, etc.) before `additional_args`; extra tokens must still match the packaged wrapper behaviour and your engagement rules.\nPer-field catalogue hints (first sentence of modal help):\n- `binary` (required): Executable for radare2/ghidra/objdump/ropper workflows.\n- `commands` (optional): Macro batch (`rpcclient` or `r2 -c`) replayed verbatim.\n- `script_file` (optional): External automation script for Ghidra/GDB post-processing.\n- `additional_args` (optional): Shell tokens appended after mapped CLI arguments."
  },
  "ghidra": {
    "long_description": "Ghidra headless mode imports specimens, runs analysis passes, optional scripts, and exports project artefacts for CI-scale reverse engineering.\n\nDebuggers/disassemblers/gadget hunters produce listings, scripted traces, annotated graphs, emulation logs, or CTF scaffolding.\n\nStatic analysis excels on offline artefacts; symbolic tooling demands compute and may path-explode on large binaries.\n\nMalware-grade samples may trigger host controls\u2014sandbox accordingly and sanitise artefacts before sharing externally.\n\nCipherStrike transmits the JSON envelope to `/api/tools/ghidra`; NyxStrike performs final validation, then shells out or calls the bundled integration advertised for this catalogue id.",
    "safety": "Operate only inside authorized penetration tests or sanctioned lab networks. Respect stop conditions, lawful intercept rules, and internal change windows. Sensitive parameters are captured in CipherStrike tenant execution logs alongside NyxStrike response snippets \u2014 avoid pasting production secrets when alternatives exist. Binary tooling may invoke malware safeguards\u2014sandbox untrusted specimens.",
    "usage": "CipherStrike `ghidra` executes against `/api/tools/ghidra` via POST JSON envelopes assembled in the Execute modal.\n\n1. Pass file paths macros scripts analysis toggles aligning with gdb/radare2/ghidra headless wrappers.\n2. Long analyses need `timeout` fields\u2014coordinate with infra.\n3. Store project directories on encrypted volumes.\n4. Symbolic backends may spin CPU\u2014quota jobs fairly.\n\nCipherStrike mechanics: send a JSON POST from the Execute modal\u2014the agent drops empty optional scalars before invoking NyxStrike.\nPrefer explicit catalogue fields (`target`, `url`, `wordlist`, etc.) before `additional_args`; extra tokens must still match the packaged wrapper behaviour and your engagement rules.\nPer-field catalogue hints (first sentence of modal help):\n- `binary` (required): Executable for radare2/ghidra/objdump/ropper workflows.\n- `project_name` (optional): Ghidra project directory for headless reversing jobs.\n- `script_file` (optional): External automation script for Ghidra/GDB post-processing.\n- `analysis_timeout` (optional): Maximum Ghidra headless analysis seconds before abort.\n- `output_format` (optional): Structured report format (`json`, `xml`, `sarif`, `html`).\n- `additional_args` (optional): Shell tokens appended after mapped CLI arguments."
  },
  "gobuster": {
    "long_description": "Gobuster (OJ Reeves) brute-forces directory, DNS, and virtual-host names at high concurrency using deterministic HTTP status/size heuristics.\n\nCore utilities return concise stdout suited for piping into scanners or parsers (DNS answers, registrar text, binaries, transports).\n\nReserve essential connectors for repeatable plumbing when higher-level arsenals obscure parameters you must control explicitly.\n\nEven ancillary utilities honour the same egress rules\u2014do not widen targets implicitly via glob inputs.\n\nCipherStrike transmits the JSON envelope to `/api/tools/gobuster`; NyxStrike performs final validation, then shells out or calls the bundled integration advertised for this catalogue id.",
    "safety": "Operate only inside authorized penetration tests or sanctioned lab networks. Respect stop conditions, lawful intercept rules, and internal change windows. Sensitive parameters are captured in CipherStrike tenant execution logs alongside NyxStrike response snippets \u2014 avoid pasting production secrets when alternatives exist.",
    "usage": "CipherStrike `gobuster` executes against `/api/tools/gobuster` via POST JSON envelopes assembled in the Execute modal.\n\n1. Provide `url` or `target` with scheme, optional `wordlist`, and HTTP tuning knobs (`threads`, `extensions`, `status_codes`).\n2. Start with a small wordlist to validate scope, then scale\u2014large runs resemble DDoS traffic.\n3. Compare HTTP status, response length, and redirect chains; manual confirmation kills false positives.\n4. `additional_args` should only carry flags missing from the JSON mapper.\n\nCipherStrike mechanics: send a JSON POST from the Execute modal\u2014the agent drops empty optional scalars before invoking NyxStrike.\nPrefer explicit catalogue fields (`target`, `url`, `wordlist`, etc.) before `additional_args`; extra tokens must still match the packaged wrapper behaviour and your engagement rules.\nPer-field catalogue hints (first sentence of modal help):\n- `url` (required): Fully qualified HTTP/S URL for crawlers, fuzzers, or vuln scanners (scheme required).\n- `mode` (optional): High-level tool mode (Amass `enum`, gobuster `dir`, shuffleDNS profile, etc.).\n- `wordlist` (optional): Agent-resident newline wordlist for content/DNS/password tooling.\n- `additional_args` (optional): Shell tokens appended after mapped CLI arguments."
  },
  "gospider": {
    "long_description": "GoSpider walks sites, parses robots.txt and sitemap.xml, leverages Burp/OSINT feeders, proxies, concurrency controls, and optional JS sources for sprawling endpoint discovery jobs.\n\nOutput usually enumerates reachable paths, crawler graphs, fingerprints, robots hints, deduplicated URLs, or header snapshots.\n\nCombine directory bruteforce, spidering, and header probes sequentially: surface mapping before attempting injection payloads.\n\nMisconfigured crawlers crawl out of scope rapidly\u2014align seed URLs, blacklist regexes, and depth caps with authorised programs.\n\nCipherStrike transmits the JSON envelope to `/api/tools/gospider`; NyxStrike performs final validation, then shells out or calls the bundled integration advertised for this catalogue id.",
    "safety": "Operate only inside authorized penetration tests or sanctioned lab networks. Respect stop conditions, lawful intercept rules, and internal change windows. Sensitive parameters are captured in CipherStrike tenant execution logs alongside NyxStrike response snippets \u2014 avoid pasting production secrets when alternatives exist.",
    "usage": "CipherStrike `gospider` executes against `/api/tools/gospider` via POST JSON envelopes assembled in the Execute modal.\n\n1. Provide one or more seed URLs and optional proxy, cookie, or depth controls.\n2. Enable JavaScript parsing only when headless components are permitted\u2014more CPU, more scope risk.\n3. Capture stdout URL streams into your evidence store; they anchor later parameter mining.\n4. Use `additional_args` for crawler-specific flags absent from first-class fields.\n\nCipherStrike mechanics: send a JSON POST from the Execute modal\u2014the agent drops empty optional scalars before invoking NyxStrike.\nPrefer explicit catalogue fields (`target`, `url`, `wordlist`, etc.) before `additional_args`; extra tokens must still match the packaged wrapper behaviour and your engagement rules.\nPer-field catalogue hints (first sentence of modal help):\n- `site` (optional): Single gospider seed URL.\n- `sites` (optional): Multiple gospider seeds.\n- `proxy` (optional): HTTP/S proxy URL for gospider or other HTTP utilities.\n- `output` (optional): Primary output sink some CLIs require (`-o`, `--output`).\n- `user_agent` (optional): Custom User-Agent for spiders and REST fuzzers.\n- `cookie` (optional): Session cookie string for authenticated gospider crawls.\n- `headers` (optional): HTTP headers as JSON map or newline text for replay utilities.\n- `burp` (optional): Burp collaborator/proxy integration hint for auxiliary crawlers.\n- `blacklist` (optional): Regex/glob denylist trimming spider scope.\n- `threads` (optional): Concurrent workers for ffuf/arjun/sublist3r/feroxbuster HTTP jobs.\n- `concurrent` (optional): gospider concurrent fetch budget per depth tier.\n- `depth` (optional): hakrawler/paramspider/gospider crawl depth.\n- `delay` (optional): Millis or seconds between probes to reduce WAF trips (Arjun, fuzzers).\n- `random_delay` (optional): gospider random delay between worker requests.\n- `timeout` (optional): Seconds for socket/DNS/CLI overall timeouts.\n- `sitemap` (optional): Pull sitemap-derived URLs during crawls.\n- `robots` (optional): Respect or parse robots.txt during crawls.\n- `other_source` (optional): Enable optional OSINT feeds in gospider.\n- _(and 7 more optional keys \u2014 see catalogue defaults in the Execute modal)._"
  },
  "graphql-scanner": {
    "long_description": "Dedicated GraphQL assessor probing introspection, depth-based DoS motifs, abusive mutations, and insecure resolver exposure within scope.\n\nStructured runs usually report vulnerable parameters, collaborator callbacks, payloads attempted, timings, tamper artefacts, and shell evidence when enabled.\n\nAutomation accelerates repeatable bug classes yet still requires manual corroboration; pair with benign accounts and isolated databases.\n\nExploit payloads can corrupt data stores or escalate privileges\u2014replay only artefacts provisioned explicitly for drills.\n\nCipherStrike transmits the JSON envelope to `/api/tools/graphql_scanner`; NyxStrike performs final validation, then shells out or calls the bundled integration advertised for this catalogue id.",
    "safety": "Operate only inside authorized penetration tests or sanctioned lab networks. Respect stop conditions, lawful intercept rules, and internal change windows. Sensitive parameters are captured in CipherStrike tenant execution logs alongside NyxStrike response snippets \u2014 avoid pasting production secrets when alternatives exist. Active payloads may corrupt data stores\u2014replay only seeded test accounts created for the drill.",
    "usage": "CipherStrike `graphql-scanner` executes against `/api/tools/graphql_scanner` via POST JSON envelopes assembled in the Execute modal.\n\n1. Provide `graphql_url`, depth/query toggles introspection booleans authorised by schema owners.\n2. Watch for unstable nested queries\u2014they can degrade services even in labs.\n3. Pair results with JWT analyser tooling when hybrid APIs exist.\n4. Record query text for remediation teams verbatim.\n\nCipherStrike mechanics: send a JSON POST from the Execute modal\u2014the agent drops empty optional scalars before invoking NyxStrike.\nPrefer explicit catalogue fields (`target`, `url`, `wordlist`, etc.) before `additional_args`; extra tokens must still match the packaged wrapper behaviour and your engagement rules.\nPer-field catalogue hints (first sentence of modal help):\n- `endpoint` (required): HTTP GraphQL/OpenAPI endpoint evaluated by schema scanners (not the NyxStrike route string).\n- `introspection` (optional): GraphQL scanners may run introspection-assisted checks.\n- `query_depth` (optional): GraphQL nested query depth limit for scanners.\n- `mutations` (optional): GraphQL scanners include mutation-focused test cases."
  },
  "hakrawler": {
    "long_description": "hakrawler rapidly enumerates reachable links/endpoints respecting depth/forms/robots/sitemap/optional Wayback supplementation.\n\nOutput usually enumerates reachable paths, crawler graphs, fingerprints, robots hints, deduplicated URLs, or header snapshots.\n\nCombine directory bruteforce, spidering, and header probes sequentially: surface mapping before attempting injection payloads.\n\nMisconfigured crawlers crawl out of scope rapidly\u2014align seed URLs, blacklist regexes, and depth caps with authorised programs.\n\nCipherStrike transmits the JSON envelope to `/api/tools/hakrawler`; NyxStrike performs final validation, then shells out or calls the bundled integration advertised for this catalogue id.",
    "safety": "Operate only inside authorized penetration tests or sanctioned lab networks. Respect stop conditions, lawful intercept rules, and internal change windows. Sensitive parameters are captured in CipherStrike tenant execution logs alongside NyxStrike response snippets \u2014 avoid pasting production secrets when alternatives exist.",
    "usage": "CipherStrike `hakrawler` executes against `/api/tools/hakrawler` via POST JSON envelopes assembled in the Execute modal.\n\n1. Seed URLs depth forms robots toggles akin to hakrawler semantics.\n2. Ideal for breadth-first inventories before katana depths.\n3. Limit forms parsing when destructive POST risk exists.\n4. Merge with archival sources when optional Wayback flags exist.\n\nCipherStrike mechanics: send a JSON POST from the Execute modal\u2014the agent drops empty optional scalars before invoking NyxStrike.\nPrefer explicit catalogue fields (`target`, `url`, `wordlist`, etc.) before `additional_args`; extra tokens must still match the packaged wrapper behaviour and your engagement rules.\nPer-field catalogue hints (first sentence of modal help):\n- `url` (required): Fully qualified HTTP/S URL for crawlers, fuzzers, or vuln scanners (scheme required).\n- `depth` (optional): hakrawler/paramspider/gospider crawl depth.\n- `forms` (optional): hakrawler enumerates HTML forms in addition to anchors.\n- `robots` (optional): Respect or parse robots.txt during crawls.\n- `sitemap` (optional): Pull sitemap-derived URLs during crawls.\n- `wayback` (optional): hakrawler can fold Wayback-derived URLs into the crawl frontier.\n- `additional_args` (optional): Shell tokens appended after mapped CLI arguments."
  },
  "hashcat": {
    "long_description": "hashcat is the predominant GPU-assisted hash recovery suite supporting hundreds of modes (straight, combinators, masks, hybrid rules) with tuning flags for workstation thermals.\n\nCore utilities return concise stdout suited for piping into scanners or parsers (DNS answers, registrar text, binaries, transports).\n\nReserve essential connectors for repeatable plumbing when higher-level arsenals obscure parameters you must control explicitly.\n\nEven ancillary utilities honour the same egress rules\u2014do not widen targets implicitly via glob inputs.\n\nCipherStrike transmits the JSON envelope to `/api/tools/hashcat`; NyxStrike performs final validation, then shells out or calls the bundled integration advertised for this catalogue id.",
    "safety": "Operate only inside authorized penetration tests or sanctioned lab networks. Respect stop conditions, lawful intercept rules, and internal change windows. Sensitive parameters are captured in CipherStrike tenant execution logs alongside NyxStrike response snippets \u2014 avoid pasting production secrets when alternatives exist.",
    "usage": "CipherStrike `hashcat` executes against `/api/tools/hashcat` via POST JSON envelopes assembled in the Execute modal.\n\n1. Supply `hash_file` paths and accurate `hash_type`/`attack_mode` equivalents per hashcat nomenclature.\n2. Attach wordlists/rules/masks staged on the agent\u2014GPU jobs still need CPU preparation.\n3. Monitor thermals/power; unattended cracking can trip breakers.\n4. Document cracked passwords under evidence handling guidelines.\n\nCipherStrike mechanics: send a JSON POST from the Execute modal\u2014the agent drops empty optional scalars before invoking NyxStrike.\nPrefer explicit catalogue fields (`target`, `url`, `wordlist`, etc.) before `additional_args`; extra tokens must still match the packaged wrapper behaviour and your engagement rules.\nPer-field catalogue hints (first sentence of modal help):\n- `hash_file` (required): Line-delimited hashes for John/hashcat/ophcrack.\n- `hash_type` (required): Hashcat mode id matching `hash_file` format (`22000`, `1000`, \u2026).\n- `attack_mode` (optional): `aireplay-ng` attack name **or** hashcat `-a` attack id\u2014meaning depends on catalogue entry context.\n- `wordlist` (optional): Agent-resident newline wordlist for content/DNS/password tooling.\n- `mask` (optional): Hashcat mask syntax (`?d?d?d?d?d?d`).\n- `additional_args` (optional): Shell tokens appended after mapped CLI arguments."
  },
  "hashcat-utils": {
    "long_description": "hashcat-utils bundle specialised CPU helpers (candidate generators, cutters) prepping inputs for downstream hash cracking.\n\nOutput enumerates guessed credentials, cracked hashes, cracked handshakes, or rule-engine statistics keyed to service modules.\n\nCredential attacks belong after softer enumeration proves plausible usernames/services; brute force last to avoid cascading lockouts.\n\nAlways enforce attempt ceilings, alerting hooks, and service-owner approval\u2014plaintext secrets land in bastion transcripts.\n\nCipherStrike transmits the JSON envelope to `/api/tools/hashcat`; NyxStrike performs final validation, then shells out or calls the bundled integration advertised for this catalogue id.",
    "safety": "Operate only inside authorized penetration tests or sanctioned lab networks. Respect stop conditions, lawful intercept rules, and internal change windows. Sensitive parameters are captured in CipherStrike tenant execution logs alongside NyxStrike response snippets \u2014 avoid pasting production secrets when alternatives exist. Credential guesses can trigger lockouts; coordinate with SOC and honour throttling mandates.",
    "usage": "CipherStrike `hashcat-utils` executes against `/api/tools/hashcat` via POST JSON envelopes assembled in the Execute modal.\n\n1. Identify formats with hashid-style helpers before importing into John-compatible modes.\n2. Provide hash files or SAM extracts as required; rainbow tables demand local `tables_dir` mounts.\n3. Expect long runtimes\u2014snapshot progress files.\n4. When cracking succeeds, escalate to credential rotation\u2014not operational reuse.\n\nCipherStrike mechanics: send a JSON POST from the Execute modal\u2014the agent drops empty optional scalars before invoking NyxStrike.\nPrefer explicit catalogue fields (`target`, `url`, `wordlist`, etc.) before `additional_args`; extra tokens must still match the packaged wrapper behaviour and your engagement rules.\nPer-field catalogue hints (first sentence of modal help):\n- `hash_file` (required): Line-delimited hashes for John/hashcat/ophcrack.\n- `hash_type` (required): Hashcat mode id matching `hash_file` format (`22000`, `1000`, \u2026).\n- `attack_mode` (optional): `aireplay-ng` attack name **or** hashcat `-a` attack id\u2014meaning depends on catalogue entry context.\n- `wordlist` (optional): Agent-resident newline wordlist for content/DNS/password tooling.\n- `additional_args` (optional): Shell tokens appended after mapped CLI arguments."
  },
  "hashid": {
    "long_description": "hashid heuristically scores hash formats to suggest candidate algorithms before feeding hashcat/john pipelines.\n\nOutput enumerates guessed credentials, cracked hashes, cracked handshakes, or rule-engine statistics keyed to service modules.\n\nCredential attacks belong after softer enumeration proves plausible usernames/services; brute force last to avoid cascading lockouts.\n\nAlways enforce attempt ceilings, alerting hooks, and service-owner approval\u2014plaintext secrets land in bastion transcripts.\n\nCipherStrike transmits the JSON envelope to `/api/tools/password_cracking/hashid`; NyxStrike performs final validation, then shells out or calls the bundled integration advertised for this catalogue id.",
    "safety": "Operate only inside authorized penetration tests or sanctioned lab networks. Respect stop conditions, lawful intercept rules, and internal change windows. Sensitive parameters are captured in CipherStrike tenant execution logs alongside NyxStrike response snippets \u2014 avoid pasting production secrets when alternatives exist. Credential guesses can trigger lockouts; coordinate with SOC and honour throttling mandates.",
    "usage": "CipherStrike `hashid` executes against `/api/tools/password_cracking/hashid` via POST JSON envelopes assembled in the Execute modal.\n\n1. Identify formats with hashid-style helpers before importing into John-compatible modes.\n2. Provide hash files or SAM extracts as required; rainbow tables demand local `tables_dir` mounts.\n3. Expect long runtimes\u2014snapshot progress files.\n4. When cracking succeeds, escalate to credential rotation\u2014not operational reuse.\n\nCipherStrike mechanics: send a JSON POST from the Execute modal\u2014the agent drops empty optional scalars before invoking NyxStrike.\nPrefer explicit catalogue fields (`target`, `url`, `wordlist`, etc.) before `additional_args`; extra tokens must still match the packaged wrapper behaviour and your engagement rules.\nPer-field catalogue hints (first sentence of modal help):\n- `hash` (required): Verifier string for Evil-WinRM / NetExec pass-the-hash transports.\n- `additional_args` (optional): Shell tokens appended after mapped CLI arguments."
  },
  "hashpump": {
    "long_description": "hashpump automates MD5/SHA length-extension forgeries crafting signatures over attacker-controlled suffixes (`append_data`).\n\nForensic tooling streams carved files, timelines, entropy maps, PCAP decodes, volatility tables, SQLite extractions.\n\nForensic artefacts may contain HIPAA/PII regulated content\u2014encrypt exports immediately.\n\nResource-heavy jobs need disk-bound IO quotas; carve only targeted ranges when multi-terabyte images are mounted.\n\nCipherStrike transmits the JSON envelope to `/api/tools/hashpump`; NyxStrike performs final validation, then shells out or calls the bundled integration advertised for this catalogue id.",
    "safety": "Operate only inside authorized penetration tests or sanctioned lab networks. Respect stop conditions, lawful intercept rules, and internal change windows. Sensitive parameters are captured in CipherStrike tenant execution logs alongside NyxStrike response snippets \u2014 avoid pasting production secrets when alternatives exist. Evidence images may contain regulated data\u2014encrypt at rest immediately after extraction.",
    "usage": "CipherStrike `hashpump` executes against `/api/tools/hashpump` via POST JSON envelopes assembled in the Execute modal.\n\n1. Specify secret length original message append bytes hash algorithm via mapped fields.\n2. Validate forged signatures offline before replaying downstream.\n3. Pair with cryptographic reviews\u2014mistakes degrade trust narratives.\n4. Document assumptions about padding oracles explicitly.\n\nCipherStrike mechanics: send a JSON POST from the Execute modal\u2014the agent drops empty optional scalars before invoking NyxStrike.\nPrefer explicit catalogue fields (`target`, `url`, `wordlist`, etc.) before `additional_args`; extra tokens must still match the packaged wrapper behaviour and your engagement rules.\nPer-field catalogue hints (first sentence of modal help):\n- `signature` (required): The workspace forwards `signature` in the CipherStrike POST body unchanged after stripping empty optional fields.\n- `data` (required): sqlmap POST body, Commix injection context, or hashpump cleartext segment.\n- `key_length` (required): The workspace forwards `key_length` in the CipherStrike POST body unchanged after stripping empty optional fields.\n- `append_data` (required): Suffix bytes appended inside hashpump length-extension forgeries.\n- `additional_args` (optional): Shell tokens appended after mapped CLI arguments."
  },
  "hcxdumptool": {
    "long_description": "hcxdumptool captures PMKID/EAPOL frames from modern chipsets with filter lists, driver-specific attack bitmaps and PCAPNG exporters aligned with hcxtools ecosystems.\n\n802.11 harnesses emit captures, WPA hash material, rogue AP scaffolding, injector acknowledgements, and audit logs keyed to channels/BSSIDs.\n\nLaboratory rigs should dedicate monitor-mode interfaces plus isolated spectrum; tethered enterprise APs rarely tolerate injection homework.\n\nJamming neighbours or intercepting unauthorised traffic violates law\u2014operate strictly inside sanctioned RF enclosures and licensed bands.\n\nCipherStrike transmits the JSON envelope to `/api/tools/wifi_pentest/hcxdumptool`; NyxStrike performs final validation, then shells out or calls the bundled integration advertised for this catalogue id.",
    "safety": "Operate only inside authorized penetration tests or sanctioned lab networks. Respect stop conditions, lawful intercept rules, and internal change windows. Sensitive parameters are captured in CipherStrike tenant execution logs alongside NyxStrike response snippets \u2014 avoid pasting production secrets when alternatives exist. Wireless probing and injection are lawful only on spectrum and networks you control by license or contract.",
    "usage": "CipherStrike `hcxdumptool` executes against `/api/tools/wifi_pentest/hcxdumptool` via POST JSON envelopes assembled in the Execute modal.\n\n1. Place adapters in monitor mode upstream when required (`airmon-ng` helper routes).\n2. Provide capture paths, ESSIDs/EAP configs, handshake targets, attack modes, regulatory paperwork references.\n3. Never point attack modes at civilian infrastructure\u2014hardware isolation only.\n4. Convert captures only within labs; distributing PMKIDs may violate telecom rules.\n\nCipherStrike mechanics: send a JSON POST from the Execute modal\u2014the agent drops empty optional scalars before invoking NyxStrike.\nPrefer explicit catalogue fields (`target`, `url`, `wordlist`, etc.) before `additional_args`; extra tokens must still match the packaged wrapper behaviour and your engagement rules.\nPer-field catalogue hints (first sentence of modal help):\n- `interface` (required): Network interface (`wlan0mon`, `eth0`) for Wi\u2011Fi or Responder jobs.\n- `output_file` (optional): Explicit output path for logs/PCAPNG/JSON when CLIs support `-o`.\n- `target_bssid` (optional): 802.11 BSSID narrowed for Bettercap Wi\u2011Fi tooling.\n- `duration` (optional): Runtime cap for Responder poison loops or Falco capture windows.\n- `additional_args` (optional): Shell tokens appended after mapped CLI arguments."
  },
  "hcxpcapngtool": {
    "long_description": "hcxpcapngtool converts captures into hash formats consumable by hashcat/john WPA cracking workflows.\n\n802.11 harnesses emit captures, WPA hash material, rogue AP scaffolding, injector acknowledgements, and audit logs keyed to channels/BSSIDs.\n\nLaboratory rigs should dedicate monitor-mode interfaces plus isolated spectrum; tethered enterprise APs rarely tolerate injection homework.\n\nJamming neighbours or intercepting unauthorised traffic violates law\u2014operate strictly inside sanctioned RF enclosures and licensed bands.\n\nCipherStrike transmits the JSON envelope to `/api/tools/wifi_pentest/hcxpcapngtool`; NyxStrike performs final validation, then shells out or calls the bundled integration advertised for this catalogue id.",
    "safety": "Operate only inside authorized penetration tests or sanctioned lab networks. Respect stop conditions, lawful intercept rules, and internal change windows. Sensitive parameters are captured in CipherStrike tenant execution logs alongside NyxStrike response snippets \u2014 avoid pasting production secrets when alternatives exist. Wireless probing and injection are lawful only on spectrum and networks you control by license or contract.",
    "usage": "CipherStrike `hcxpcapngtool` executes against `/api/tools/wifi_pentest/hcxpcapngtool` via POST JSON envelopes assembled in the Execute modal.\n\n1. Place adapters in monitor mode upstream when required (`airmon-ng` helper routes).\n2. Provide capture paths, ESSIDs/EAP configs, handshake targets, attack modes, regulatory paperwork references.\n3. Never point attack modes at civilian infrastructure\u2014hardware isolation only.\n4. Convert captures only within labs; distributing PMKIDs may violate telecom rules.\n\nCipherStrike mechanics: send a JSON POST from the Execute modal\u2014the agent drops empty optional scalars before invoking NyxStrike.\nPrefer explicit catalogue fields (`target`, `url`, `wordlist`, etc.) before `additional_args`; extra tokens must still match the packaged wrapper behaviour and your engagement rules.\nPer-field catalogue hints (first sentence of modal help):\n- `input_file` (required): Evidence blob for foremost/scalpel/testdisk carvers.\n- `output_file` (required): Explicit output path for logs/PCAPNG/JSON when CLIs support `-o`."
  },
  "http-framework": {
    "long_description": "HTTP-framework route mimics scripted curl/httpie ergonomics exposing verbs, payloads, cookie jars, replay actions for manual assertions.\n\nHTTP framework connector mirrors curl ergonomics verbs bodies cookies replay chains.\n\nUseful for bespoke asserts before scripting python requests pipelines.\n\nManual verbs can mutate production data beware non-idempotent POSTs.\n\nCipherStrike transmits the JSON envelope to `/api/tools/http-framework`; NyxStrike performs final validation, then shells out or calls the bundled integration advertised for this catalogue id.",
    "safety": "Operate only inside authorized penetration tests or sanctioned lab networks. Respect stop conditions, lawful intercept rules, and internal change windows. Sensitive parameters are captured in CipherStrike tenant execution logs alongside NyxStrike response snippets \u2014 avoid pasting production secrets when alternatives exist. API fuzzing can trigger costly side-effects (email, billing, writes)\u2014narrow scope before unattended runs.",
    "usage": "CipherStrike `http-framework` executes against `/api/tools/http-framework` via POST JSON envelopes assembled in the Execute modal.\n\n1. Choose HTTP verbs headers bodies cookies proxies replay toggles enumerated for this route.\n2. Ideal ad hoc verifier before scripting pipelines.\n3. Manual replays hitting POST endpoints may write data throttle accordingly.\n4. Document every transactional ID touched.\n\nCipherStrike mechanics: send a JSON POST from the Execute modal\u2014the agent drops empty optional scalars before invoking NyxStrike.\nPrefer explicit catalogue fields (`target`, `url`, `wordlist`, etc.) before `additional_args`; extra tokens must still match the packaged wrapper behaviour and your engagement rules.\nPer-field catalogue hints (first sentence of modal help):\n- `url` (required): Fully qualified HTTP/S URL for crawlers, fuzzers, or vuln scanners (scheme required).\n- `method` (optional): HTTP verb (`GET`, `POST`, \u2026) or tool-specific dispatcher (ffuf mode selection, HTTP-framework action cues, Arjun transport).\n- `data` (optional): sqlmap POST body, Commix injection context, or hashpump cleartext segment.\n- `headers` (optional): HTTP headers as JSON map or newline text for replay utilities.\n- `action` (optional): `airmon-ng` verb (`start`/`stop`) or CloudMapper subcommand name.\n- `cookies` (optional): Cookie jar string for manual HTTP replays."
  },
  "http-headers": {
    "long_description": "Lightweight HTTPS/HTTP HEAD fetcher akin to curl\u2019s `-sI`, surfacing CSP/HSTS/feature-policy signals for quick header reviews.\n\nOutput usually enumerates reachable paths, crawler graphs, fingerprints, robots hints, deduplicated URLs, or header snapshots.\n\nCombine directory bruteforce, spidering, and header probes sequentially: surface mapping before attempting injection payloads.\n\nMisconfigured crawlers crawl out of scope rapidly\u2014align seed URLs, blacklist regexes, and depth caps with authorised programs.\n\nCipherStrike transmits the JSON envelope to `/api/tools/http-headers`; NyxStrike performs final validation, then shells out or calls the bundled integration advertised for this catalogue id.",
    "safety": "Operate only inside authorized penetration tests or sanctioned lab networks. Respect stop conditions, lawful intercept rules, and internal change windows. Sensitive parameters are captured in CipherStrike tenant execution logs alongside NyxStrike response snippets \u2014 avoid pasting production secrets when alternatives exist.",
    "usage": "CipherStrike `http-headers` executes against `/api/tools/http-headers` via POST JSON envelopes assembled in the Execute modal.\n\n1. Provide hostnames/toggle `https` booleans depending on TLS requirements.\n2. Review CSP/HSTS/Feature-Policy quickly before deeper scanners.\n3. Follow internal data-handling guidelines when headers reveal internal IPs.\n4. Do not automate against third-party SaaS absent permission.\n\nCipherStrike mechanics: send a JSON POST from the Execute modal\u2014the agent drops empty optional scalars before invoking NyxStrike.\nPrefer explicit catalogue fields (`target`, `url`, `wordlist`, etc.) before `additional_args`; extra tokens must still match the packaged wrapper behaviour and your engagement rules.\nPer-field catalogue hints (first sentence of modal help):\n- `target` (required): Primary IPv4/IPv6/hostname/CIDR or bulk newline list routed into network scanners and RPC clients.\n- `https` (optional): http-headers helper uses TLS when probing raw hostnames.\n- `follow_redirects` (optional): HTTP helpers follow 3xx chains\u2014watch for open-redirect scope creep.\n- `timeout` (optional): Seconds for socket/DNS/CLI overall timeouts."
  },
  "httpx": {
    "long_description": "httpx is ProjectDiscovery\u2019s fast HTTP probing engine that fingerprints live hosts and optional technologies/status/title artefacts.\n\nOutput usually enumerates reachable paths, crawler graphs, fingerprints, robots hints, deduplicated URLs, or header snapshots.\n\nCombine directory bruteforce, spidering, and header probes sequentially: surface mapping before attempting injection payloads.\n\nMisconfigured crawlers crawl out of scope rapidly\u2014align seed URLs, blacklist regexes, and depth caps with authorised programs.\n\nCipherStrike transmits the JSON envelope to `/api/tools/httpx`; NyxStrike performs final validation, then shells out or calls the bundled integration advertised for this catalogue id.",
    "safety": "Operate only inside authorized penetration tests or sanctioned lab networks. Respect stop conditions, lawful intercept rules, and internal change windows. Sensitive parameters are captured in CipherStrike tenant execution logs alongside NyxStrike response snippets \u2014 avoid pasting production secrets when alternatives exist.",
    "usage": "CipherStrike `httpx` executes against `/api/tools/httpx` via POST JSON envelopes assembled in the Execute modal.\n\n1. Feed newline files or lists via `target`/`url` fields as defined in the registry entry.\n2. Toggle technology detection, title extraction, and status flags to match reporting needs.\n3. Treat non-responses as dead paths\u2014follow up with ping/TCP checks if UDP-only services matter.\n4. Batch huge inputs to keep memory predictable on the agent.\n\nCipherStrike mechanics: send a JSON POST from the Execute modal\u2014the agent drops empty optional scalars before invoking NyxStrike.\nPrefer explicit catalogue fields (`target`, `url`, `wordlist`, etc.) before `additional_args`; extra tokens must still match the packaged wrapper behaviour and your engagement rules.\nPer-field catalogue hints (first sentence of modal help):\n- `target` (required): Primary IPv4/IPv6/hostname/CIDR or bulk newline list routed into network scanners and RPC clients.\n- `probe` (optional): httpx runs liveness/TLS fingerprint passes.\n- `tech_detect` (optional): httpx enriches results with technology fingerprints.\n- `status_code` (optional): httpx prints HTTP status codes in output tables.\n- `title` (optional): httpx captures HTML `<title>` tags alongside probes.\n- `additional_args` (optional): Shell tokens appended after mapped CLI arguments."
  },
  "hurl": {
    "long_description": "hurl (Tom Hudson) exposes encoding gymnastics\u2014decode/encode payloads across hex/url/base64 and related transforms supporting web abuse testing.\n\nPipeline gadgets emit deduped URL streams, mutated query dictionaries, novelty markers consumed by fuzzers downstream.\n\nPair urltooling early in pipelines to cut noise feeding ffuf nuclei pipelines.\n\nMassive stdin lines stress memory filters\u2014chunk inputs when chaining shell pipes via agents.\n\nCipherStrike transmits the JSON envelope to `/api/tools/data_processing/hurl`; NyxStrike performs final validation, then shells out or calls the bundled integration advertised for this catalogue id.",
    "safety": "Operate only inside authorized penetration tests or sanctioned lab networks. Respect stop conditions, lawful intercept rules, and internal change windows. Sensitive parameters are captured in CipherStrike tenant execution logs alongside NyxStrike response snippets \u2014 avoid pasting production secrets when alternatives exist.",
    "usage": "CipherStrike `hurl` executes against `/api/tools/data_processing/hurl` via POST JSON envelopes assembled in the Execute modal.\n\n1. Provide the `input` blob plus the transform mode expected by the hurl-style wrapper.\n2. Use this tool for payload preparation\u2014not for delivering exploits directly.\n3. Validate outputs manually when encodings chain (e.g., hex then URL).\n4. Avoid secrets in shared sessions; encoders log inputs verbatim.\n\nCipherStrike mechanics: send a JSON POST from the Execute modal\u2014the agent drops empty optional scalars before invoking NyxStrike.\nPrefer explicit catalogue fields (`target`, `url`, `wordlist`, etc.) before `additional_args`; extra tokens must still match the packaged wrapper behaviour and your engagement rules.\nPer-field catalogue hints (first sentence of modal help):\n- `input` (required): Generic text blob for waymore/hurl data transforms.\n- `mode` (optional): High-level tool mode (Amass `enum`, gobuster `dir`, shuffleDNS profile, etc.).\n- `suppress` (optional): hurl-style helpers hide decorative stdout.\n- `additional_args` (optional): Shell tokens appended after mapped CLI arguments."
  },
  "hydra": {
    "long_description": "Hydra is the parallel login cracker spanning dozens of services (SMB, SSH, HTTP forms, databases) combining dictionary, brute, and configurable module-specific options.\n\nHydra prints cracked credentials per-module attempt counts and handshake diagnostics.\n\nHydra excels at multiprotocol brute forcing versus Medusa emphasis or Patator extensibility.\n\nParallel threads may lock accounts maintain delay flags.\n\nCipherStrike transmits the JSON envelope to `/api/tools/hydra`; NyxStrike performs final validation, then shells out or calls the bundled integration advertised for this catalogue id.",
    "safety": "Operate only inside authorized penetration tests or sanctioned lab networks. Respect stop conditions, lawful intercept rules, and internal change windows. Sensitive parameters are captured in CipherStrike tenant execution logs alongside NyxStrike response snippets \u2014 avoid pasting production secrets when alternatives exist. Parallel login attempts may trip global lockouts\u2014coordinate account policies and cooldowns.",
    "usage": "CipherStrike `hydra` executes against `/api/tools/hydra` via POST JSON envelopes assembled in the Execute modal.\n\n1. Provide `target`, `username`/`username_file`, `password_file`, and protocol/service selectors.\n2. Apply `threads`, `delay`, and attempt caps mandated by SOC.\n3. Stop on first success unless horizontal movement is chartered\u2014credential stuffing may violate policy.\n4. Archive cracked creds securely; rotate them after testing.\n\nCipherStrike mechanics: send a JSON POST from the Execute modal\u2014the agent drops empty optional scalars before invoking NyxStrike.\nPrefer explicit catalogue fields (`target`, `url`, `wordlist`, etc.) before `additional_args`; extra tokens must still match the packaged wrapper behaviour and your engagement rules.\nPer-field catalogue hints (first sentence of modal help):\n- `target` (required): Primary IPv4/IPv6/hostname/CIDR or bulk newline list routed into network scanners and RPC clients.\n- `service` (required): The workspace forwards `service` in the CipherStrike POST body unchanged after stripping empty optional fields.\n- `username` (optional): Username for SMB/LDAP/WinRM clients and login brute modules.\n- `username_file` (optional): Username list for credential spraying modules.\n- `password` (optional): Cleartext password for authenticated scans\u2014rotate if leaked into logs.\n- `password_file` (optional): Dictionary file for Hydra/Medusa/Patator.\n- `additional_args` (optional): Shell tokens appended after mapped CLI arguments."
  },
  "impacket-ad-enum": {
    "long_description": "Curated Impacket fa\u00e7ade for enumeration scripts (`GetNPUsers`, `FindDelegation`, `lookupsid`, etc.) exposing DC IP, hashes, AES keys, Kerberos booleans and guarded `extra_*` escapes.\n\nAD helpers export user lists, trusts, constrained delegations, policy summaries, responder captures, SMB maps, LDAP dumps.\n\nUse dedicated assessment domain accounts wherever possible\u2014these emits land in Defender and SIEM timelines.\n\nCredential material in transit may enable domain-wide compromise\u2014treat transcripts as collateral damage.\n\nCipherStrike transmits the JSON envelope to `/api/tool/active_directory/impacket`; NyxStrike performs final validation, then shells out or calls the bundled integration advertised for this catalogue id.",
    "safety": "Operate only inside authorized penetration tests or sanctioned lab networks. Respect stop conditions, lawful intercept rules, and internal change windows. Sensitive parameters are captured in CipherStrike tenant execution logs alongside NyxStrike response snippets \u2014 avoid pasting production secrets when alternatives exist. Domain queries leave authentication telemetry\u2014prefer dedicated assessment accounts.",
    "usage": "CipherStrike `impacket-ad-enum` executes against `/api/tool/active_directory/impacket` via POST JSON envelopes assembled in the Execute modal.\n\n1. Pick the enumeration preset via `script` or module fields and include `dc_ip`, credentials, hashes, or AES keys as required.\n2. Toggle Kerberos-aware booleans deliberately\u2014wrong combinations leak NT hash quirks.\n3. Expect large object dumps\u2014redirect to encrypted storage.\n4. Pair results with SOC to avoid surprise lockouts.\n\nCipherStrike mechanics: send a JSON POST from the Execute modal\u2014the agent drops empty optional scalars before invoking NyxStrike.\nPrefer explicit catalogue fields (`target`, `url`, `wordlist`, etc.) before `additional_args`; extra tokens must still match the packaged wrapper behaviour and your engagement rules.\nPer-field catalogue hints (first sentence of modal help):\n- `script` (required): Impacket script basename (`GetNPUsers.py`, `secretsdump.py`).\n- `target` (required): Primary IPv4/IPv6/hostname/CIDR or bulk newline list routed into network scanners and RPC clients.\n- `dc_ip` (optional): Domain controller IPv4 when DNS is untrusted or split-horizon breaks auth.\n- `username` (optional): Username for SMB/LDAP/WinRM clients and login brute modules.\n- `password` (optional): Cleartext password for authenticated scans\u2014rotate if leaked into logs.\n- `hashes` (optional): Impacket `-hashes LMHASH:NTHASH` material or equivalent for Kerberos/NTLM hops.\n- `kerberos` (optional): Impacket connectors use Kerberos instead of classical NTLM only.\n- `no_pass` (optional): Permit credential-less binds when upstream scripts expect that sentinel.\n- `aes_key` (optional): Hex AES256 key for Kerberos `-aesKey` flows when passwords are unavailable.\n- `debug` (optional): Emit verbose traces (Impacket, HTTP utilities); may leak secrets into logs.\n- `extra_options` (optional): Leading dash-options for Impacket scripts lacking structured fields.\n- `extra_args` (optional): Trailing CLI tokens after mapped arguments for niche switches."
  },
  "impacket-remote-exec": {
    "long_description": "Curated fa\u00e7ade for lateral movement transports (`wmiexec`, `psexec`, `atexec`) with command strings, UNC shares, shells, hashes, AES keys and impersonation safeguards.\n\nImpacket-style transports return command stdout, SID dumps, ticketing warnings, DPAPI extracts, or benign RPC listings depending on wrappers.\n\nRemote execution primitives differ in OPSEC footprints\u2014prioritise WinRM auditing accounts over noisy SMB relays when defenders mandate stealth.\n\nKerberos materially depends on reachable DCs accurate clocks and careful hash handling.\n\nCipherStrike transmits the JSON envelope to `/api/tool/active_directory/impacket`; NyxStrike performs final validation, then shells out or calls the bundled integration advertised for this catalogue id.",
    "safety": "Operate only inside authorized penetration tests or sanctioned lab networks. Respect stop conditions, lawful intercept rules, and internal change windows. Sensitive parameters are captured in CipherStrike tenant execution logs alongside NyxStrike response snippets \u2014 avoid pasting production secrets when alternatives exist. Impacket-style executions alter security event logs\u2014isolate jump hosts and minimise shared credentials. Remote execution is high severity; record every command and preserve chain-of-custody.",
    "usage": "CipherStrike `impacket-remote-exec` executes against `/api/tool/active_directory/impacket` via POST JSON envelopes assembled in the Execute modal.\n\n1. Identify the transport (`wmiexec`, `psexec`, etc.) plus `command` strings reviewed by peers.\n2. Provide share paths shells and credential bundles per template\u2014executions alter event logs drastically.\n3. Capture stdout/stderr for evidence; beware commands that reboot hosts.\n4. Escalations require explicit approval in writing.\n\nCipherStrike mechanics: send a JSON POST from the Execute modal\u2014the agent drops empty optional scalars before invoking NyxStrike.\nPrefer explicit catalogue fields (`target`, `url`, `wordlist`, etc.) before `additional_args`; extra tokens must still match the packaged wrapper behaviour and your engagement rules.\nPer-field catalogue hints (first sentence of modal help):\n- `script` (required): Impacket script basename (`GetNPUsers.py`, `secretsdump.py`).\n- `target` (required): Primary IPv4/IPv6/hostname/CIDR or bulk newline list routed into network scanners and RPC clients.\n- `command` (optional): Escaped command string for psexec/wmiexec/smbexec-style runners.\n- `username` (optional): Username for SMB/LDAP/WinRM clients and login brute modules.\n- `password` (optional): Cleartext password for authenticated scans\u2014rotate if leaked into logs.\n- `domain` (optional): Authorised DNS apex fed to Amass/subfinder/GAU style OSINT connectors.\n- `hashes` (optional): Impacket `-hashes LMHASH:NTHASH` material or equivalent for Kerberos/NTLM hops.\n- `kerberos` (optional): Impacket connectors use Kerberos instead of classical NTLM only.\n- `no_pass` (optional): Permit credential-less binds when upstream scripts expect that sentinel.\n- `aes_key` (optional): Hex AES256 key for Kerberos `-aesKey` flows when passwords are unavailable.\n- `share` (optional): SMB share (`C$`, `ADMIN$`) for remote execution transports.\n- `shell_type` (optional): Remote shell interpreter (`cmd`, `powershell`) some Impacket scripts honour.\n- `debug` (optional): Emit verbose traces (Impacket, HTTP utilities); may leak secrets into logs.\n- `extra_options` (optional): Leading dash-options for Impacket scripts lacking structured fields.\n- `extra_args` (optional): Trailing CLI tokens after mapped arguments for niche switches.\n- `use_recovery` (optional): Impacket enables Kerberos session recovery quirks."
  },
  "impacket-scripts": {
    "long_description": "Thin wrapper invoking arbitrary SecureAuth/impacket utilities with script names plus target/options strings mirroring upstream CLI ergonomics.\n\nAD helpers export user lists, trusts, constrained delegations, policy summaries, responder captures, SMB maps, LDAP dumps.\n\nUse dedicated assessment domain accounts wherever possible\u2014these emits land in Defender and SIEM timelines.\n\nCredential material in transit may enable domain-wide compromise\u2014treat transcripts as collateral damage.\n\nCipherStrike transmits the JSON envelope to `/api/tool/active_directory/impacket`; NyxStrike performs final validation, then shells out or calls the bundled integration advertised for this catalogue id.",
    "safety": "Operate only inside authorized penetration tests or sanctioned lab networks. Respect stop conditions, lawful intercept rules, and internal change windows. Sensitive parameters are captured in CipherStrike tenant execution logs alongside NyxStrike response snippets \u2014 avoid pasting production secrets when alternatives exist. Domain queries leave authentication telemetry\u2014prefer dedicated assessment accounts.",
    "usage": "CipherStrike `impacket-scripts` executes against `/api/tool/active_directory/impacket` via POST JSON envelopes assembled in the Execute modal.\n\n1. Set `script` to the upstream Impacket filename and marshal arguments via structured plus `extra_args` tail.\n2. Run `impacket-spec` first when unsure how parameters map.\n3. Log every authenticated bind; Kerberos anomalies usually mean skewed clocks or wrong SPN formatting.\n4. Never mix production credentials across tenants.\n\nCipherStrike mechanics: send a JSON POST from the Execute modal\u2014the agent drops empty optional scalars before invoking NyxStrike.\nPrefer explicit catalogue fields (`target`, `url`, `wordlist`, etc.) before `additional_args`; extra tokens must still match the packaged wrapper behaviour and your engagement rules.\nPer-field catalogue hints (first sentence of modal help):\n- `script` (required): Impacket script basename (`GetNPUsers.py`, `secretsdump.py`).\n- `target` (required): Primary IPv4/IPv6/hostname/CIDR or bulk newline list routed into network scanners and RPC clients.\n- `options` (optional): JSON map of Metasploit `set` assignments for scripted console use."
  },
  "impacket-spec": {
    "long_description": "Inspection helper retrieving argument specifications for Impacket scripts so planners can hydrate JSON payloads safely prior to invocation.\n\nAD helpers export user lists, trusts, constrained delegations, policy summaries, responder captures, SMB maps, LDAP dumps.\n\nUse dedicated assessment domain accounts wherever possible\u2014these emits land in Defender and SIEM timelines.\n\nCredential material in transit may enable domain-wide compromise\u2014treat transcripts as collateral damage.\n\nCipherStrike transmits the JSON envelope to `/api/tool/active_directory/impacket/spec`; NyxStrike performs final validation, then shells out or calls the bundled integration advertised for this catalogue id.",
    "safety": "Operate only inside authorized penetration tests or sanctioned lab networks. Respect stop conditions, lawful intercept rules, and internal change windows. Sensitive parameters are captured in CipherStrike tenant execution logs alongside NyxStrike response snippets \u2014 avoid pasting production secrets when alternatives exist. Domain queries leave authentication telemetry\u2014prefer dedicated assessment accounts.",
    "usage": "CipherStrike `impacket-spec` executes against `/api/tool/active_directory/impacket/spec` via POST JSON envelopes assembled in the Execute modal.\n\n1. Populate `script` plus minimal dummy targets to retrieve help text programmatically.\n2. Use responses to hydrate safer JSON later\u2014do not blindly paste greasy shell fragments.\n3. Cache outputs inside runbooks so operators understand mandatory switches.\n4. This route should never touch production without read-only intentions.\n\nCipherStrike mechanics: send a JSON POST from the Execute modal\u2014the agent drops empty optional scalars before invoking NyxStrike.\nPrefer explicit catalogue fields (`target`, `url`, `wordlist`, etc.) before `additional_args`; extra tokens must still match the packaged wrapper behaviour and your engagement rules.\nPer-field catalogue hints (first sentence of modal help):\n- `script` (required): Impacket script basename (`GetNPUsers.py`, `secretsdump.py`)."
  },
  "interactsh": {
    "long_description": "Interactsh (ProjectDiscovery) issues correlation tokens for out-of-band detection of SSRF/DNS/blind SSRF behaviours via hosted or self-hosted brokers.\n\nInteractsh correlates collaborator DNS HTTP interactions with seeded tokens tying blind sinks.\n\nUse after mapping injection points needing out-of-band confirmation.\n\nSelf-hosted relays require infra approval.\n\nCipherStrike transmits the JSON envelope to `/api/tools/web_scan/interactsh`; NyxStrike performs final validation, then shells out or calls the bundled integration advertised for this catalogue id.",
    "safety": "Operate only inside authorized penetration tests or sanctioned lab networks. Respect stop conditions, lawful intercept rules, and internal change windows. Sensitive parameters are captured in CipherStrike tenant execution logs alongside NyxStrike response snippets \u2014 avoid pasting production secrets when alternatives exist. Broad web scanners overwhelm fragile apps\u2014coordinate throttles before production-facing tests.",
    "usage": "CipherStrike `interactsh` executes against `/api/tools/web_scan/interactsh` via POST JSON envelopes assembled in the Execute modal.\n\n1. Configure `server`, `token`, and polling budget `n` per your Interactsh deployment.\n2. Embed the issued correlation ID into blind XSS, SSRF, or deserialization gadgets.\n3. Monitor callback timelines; absence of hits is inconclusive if egress filters block traffic.\n4. Self-host brokers when customer policy forbids third-party callbacks.\n\nCipherStrike mechanics: send a JSON POST from the Execute modal\u2014the agent drops empty optional scalars before invoking NyxStrike.\nPrefer explicit catalogue fields (`target`, `url`, `wordlist`, etc.) before `additional_args`; extra tokens must still match the packaged wrapper behaviour and your engagement rules.\nPer-field catalogue hints (first sentence of modal help):\n- `server` (optional): Interactsh or collaborator base URL polled for DNS/HTTP blind-callback payloads.\n- `token` (optional): Interactsh session token tying out-of-band events to your run.\n- `n` (optional): `interactsh` poll/request budget controlling how many callback checks occur before stopping.\n- `poll_interval` (optional): Spiderfoot/long-job polling cadence when exposed.\n- `timeout` (optional): Seconds for socket/DNS/CLI overall timeouts.\n- `additional_args` (optional): Shell tokens appended after mapped CLI arguments."
  },
  "jaeles": {
    "long_description": "Jaeles is a YAML-signature oriented web vuln hunter emphasising customised templates and baseline diffing akin to miniature nuclei clones.\n\nOutput usually enumerates reachable paths, crawler graphs, fingerprints, robots hints, deduplicated URLs, or header snapshots.\n\nCombine directory bruteforce, spidering, and header probes sequentially: surface mapping before attempting injection payloads.\n\nMisconfigured crawlers crawl out of scope rapidly\u2014align seed URLs, blacklist regexes, and depth caps with authorised programs.\n\nCipherStrike transmits the JSON envelope to `/api/tools/jaeles`; NyxStrike performs final validation, then shells out or calls the bundled integration advertised for this catalogue id.",
    "safety": "Operate only inside authorized penetration tests or sanctioned lab networks. Respect stop conditions, lawful intercept rules, and internal change windows. Sensitive parameters are captured in CipherStrike tenant execution logs alongside NyxStrike response snippets \u2014 avoid pasting production secrets when alternatives exist.",
    "usage": "CipherStrike `jaeles` executes against `/api/tools/jaeles` via POST JSON envelopes assembled in the Execute modal.\n\n1. Load custom YAML signatures or curated paths via structured selectors.\n2. Align concurrency with defensive monitoring; Jaeles can be thunderous.\n3. Validate positives manually\u2014templates vary in quality.\n4. Version-control bespoke signatures to trace assessment provenance.\n\nCipherStrike mechanics: send a JSON POST from the Execute modal\u2014the agent drops empty optional scalars before invoking NyxStrike.\nPrefer explicit catalogue fields (`target`, `url`, `wordlist`, etc.) before `additional_args`; extra tokens must still match the packaged wrapper behaviour and your engagement rules.\nPer-field catalogue hints (first sentence of modal help):\n- `url` (required): Fully qualified HTTP/S URL for crawlers, fuzzers, or vuln scanners (scheme required).\n- `additional_args` (optional): Shell tokens appended after mapped CLI arguments."
  },
  "john": {
    "long_description": "John the Ripper is the canonical CPU-centric password security auditor with `-format` selectors, incremental modes, external filters, and incremental GPU helpers via forks.\n\nCore utilities return concise stdout suited for piping into scanners or parsers (DNS answers, registrar text, binaries, transports).\n\nReserve essential connectors for repeatable plumbing when higher-level arsenals obscure parameters you must control explicitly.\n\nEven ancillary utilities honour the same egress rules\u2014do not widen targets implicitly via glob inputs.\n\nCipherStrike transmits the JSON envelope to `/api/tools/john`; NyxStrike performs final validation, then shells out or calls the bundled integration advertised for this catalogue id.",
    "safety": "Operate only inside authorized penetration tests or sanctioned lab networks. Respect stop conditions, lawful intercept rules, and internal change windows. Sensitive parameters are captured in CipherStrike tenant execution logs alongside NyxStrike response snippets \u2014 avoid pasting production secrets when alternatives exist.",
    "usage": "CipherStrike `john` executes against `/api/tools/john` via POST JSON envelopes assembled in the Execute modal.\n\n1. Identify formats with hashid-style helpers before importing into John-compatible modes.\n2. Provide hash files or SAM extracts as required; rainbow tables demand local `tables_dir` mounts.\n3. Expect long runtimes\u2014snapshot progress files.\n4. When cracking succeeds, escalate to credential rotation\u2014not operational reuse.\n\nCipherStrike mechanics: send a JSON POST from the Execute modal\u2014the agent drops empty optional scalars before invoking NyxStrike.\nPrefer explicit catalogue fields (`target`, `url`, `wordlist`, etc.) before `additional_args`; extra tokens must still match the packaged wrapper behaviour and your engagement rules.\nPer-field catalogue hints (first sentence of modal help):\n- `hash_file` (required): Line-delimited hashes for John/hashcat/ophcrack.\n- `wordlist` (optional): Agent-resident newline wordlist for content/DNS/password tooling.\n- `format_type` (optional): John the Ripper format string (`raw-md5`, `wpapsk`, \u2026).\n- `additional_args` (optional): Shell tokens appended after mapped CLI arguments."
  },
  "joomscan": {
    "long_description": "Joomla-specific vulnerability scanner inspecting core/components for known exploits and configuration weaknesses.\n\nOutput usually enumerates reachable paths, crawler graphs, fingerprints, robots hints, deduplicated URLs, or header snapshots.\n\nCombine directory bruteforce, spidering, and header probes sequentially: surface mapping before attempting injection payloads.\n\nMisconfigured crawlers crawl out of scope rapidly\u2014align seed URLs, blacklist regexes, and depth caps with authorised programs.\n\nCipherStrike transmits the JSON envelope to `/api/tools/web_recon/joomscan`; NyxStrike performs final validation, then shells out or calls the bundled integration advertised for this catalogue id.",
    "safety": "Operate only inside authorized penetration tests or sanctioned lab networks. Respect stop conditions, lawful intercept rules, and internal change windows. Sensitive parameters are captured in CipherStrike tenant execution logs alongside NyxStrike response snippets \u2014 avoid pasting production secrets when alternatives exist.",
    "usage": "CipherStrike `joomscan` executes against `/api/tools/web_recon/joomscan` via POST JSON envelopes assembled in the Execute modal.\n\n1. Include `url`, optional API tokens, and enumeration toggles for users or plugins.\n2. Expect aggressive plugins list\u2014validate versions manually before claiming exploitability.\n3. Throttle brute-force modules; many CMS tools include login tests.\n4. Archive JSON output for regression testing after patching.\n\nCipherStrike mechanics: send a JSON POST from the Execute modal\u2014the agent drops empty optional scalars before invoking NyxStrike.\nPrefer explicit catalogue fields (`target`, `url`, `wordlist`, etc.) before `additional_args`; extra tokens must still match the packaged wrapper behaviour and your engagement rules.\nPer-field catalogue hints (first sentence of modal help):\n- `url` (required): Fully qualified HTTP/S URL for crawlers, fuzzers, or vuln scanners (scheme required).\n- `additional_args` (optional): Shell tokens appended after mapped CLI arguments."
  },
  "jwt-analyzer": {
    "long_description": "JWT tooling that decodes JOSE blobs, hunts `alg=none`, tests RS\u2192HS swaps, brute-forces weak secrets and correlates forged tokens via optional target URLs.\n\nJWT tooling emits decoded segments confusion attack verdicts brute force hints optional replay artefacts.\n\nDifferentiates from graphql scanners by specialising JOSE cryptographic misuse.\n\nWeak secret dictionaries belong to chartered pentests.\n\nCipherStrike transmits the JSON envelope to `/api/tools/jwt_analyzer`; NyxStrike performs final validation, then shells out or calls the bundled integration advertised for this catalogue id.",
    "safety": "Operate only inside authorized penetration tests or sanctioned lab networks. Respect stop conditions, lawful intercept rules, and internal change windows. Sensitive parameters are captured in CipherStrike tenant execution logs alongside NyxStrike response snippets \u2014 avoid pasting production secrets when alternatives exist. Active payloads may corrupt data stores\u2014replay only seeded test accounts created for the drill.",
    "usage": "CipherStrike `jwt-analyzer` executes against `/api/tools/jwt_analyzer` via POST JSON envelopes assembled in the Execute modal.\n\n1. Paste `jwt_token`, optional `secrets` dictionaries, attacker URLs referencing replay sinks.\n2. Validate algorithm downgrade attempts only on disposable sessions.\n3. Document each tampered token hashed for evidentiary continuity.\n4. Stop brute forcing secrets when charters cap attempts.\n\nCipherStrike mechanics: send a JSON POST from the Execute modal\u2014the agent drops empty optional scalars before invoking NyxStrike.\nPrefer explicit catalogue fields (`target`, `url`, `wordlist`, etc.) before `additional_args`; extra tokens must still match the packaged wrapper behaviour and your engagement rules.\nPer-field catalogue hints (first sentence of modal help):\n- `jwt_token` (required): Raw JWT/JWS blob for decode, key confusion, or RS256\u2192HS256 testing\u2014handle as live secret.\n- `target_url` (optional): Optional HTTP sink where JWT tamper harnesses replay altered tokens."
  },
  "katana": {
    "long_description": "ProjectDiscovery\u2019s katana crawler streams JS-aware navigation graphs for attack-surface inventories and pipeline-friendly URL emission.\n\nOutput usually enumerates reachable paths, crawler graphs, fingerprints, robots hints, deduplicated URLs, or header snapshots.\n\nCombine directory bruteforce, spidering, and header probes sequentially: surface mapping before attempting injection payloads.\n\nMisconfigured crawlers crawl out of scope rapidly\u2014align seed URLs, blacklist regexes, and depth caps with authorised programs.\n\nCipherStrike transmits the JSON envelope to `/api/tools/katana`; NyxStrike performs final validation, then shells out or calls the bundled integration advertised for this catalogue id.",
    "safety": "Operate only inside authorized penetration tests or sanctioned lab networks. Respect stop conditions, lawful intercept rules, and internal change windows. Sensitive parameters are captured in CipherStrike tenant execution logs alongside NyxStrike response snippets \u2014 avoid pasting production secrets when alternatives exist.",
    "usage": "CipherStrike `katana` executes against `/api/tools/katana` via POST JSON envelopes assembled in the Execute modal.\n\n1. Provide one or more seed URLs and optional proxy, cookie, or depth controls.\n2. Enable JavaScript parsing only when headless components are permitted\u2014more CPU, more scope risk.\n3. Capture stdout URL streams into your evidence store; they anchor later parameter mining.\n4. Use `additional_args` for crawler-specific flags absent from first-class fields.\n\nCipherStrike mechanics: send a JSON POST from the Execute modal\u2014the agent drops empty optional scalars before invoking NyxStrike.\nPrefer explicit catalogue fields (`target`, `url`, `wordlist`, etc.) before `additional_args`; extra tokens must still match the packaged wrapper behaviour and your engagement rules.\nPer-field catalogue hints (first sentence of modal help):\n- `url` (required): Fully qualified HTTP/S URL for crawlers, fuzzers, or vuln scanners (scheme required).\n- `additional_args` (optional): Shell tokens appended after mapped CLI arguments."
  },
  "kismet": {
    "long_description": "kismet passively observes multi-protocol wireless frames with logging/IDS components on supported NICs;coordinate spectrum licensing.\n\n802.11 harnesses emit captures, WPA hash material, rogue AP scaffolding, injector acknowledgements, and audit logs keyed to channels/BSSIDs.\n\nLaboratory rigs should dedicate monitor-mode interfaces plus isolated spectrum; tethered enterprise APs rarely tolerate injection homework.\n\nJamming neighbours or intercepting unauthorised traffic violates law\u2014operate strictly inside sanctioned RF enclosures and licensed bands.\n\nCipherStrike transmits the JSON envelope to `/api/tools/kismet`; NyxStrike performs final validation, then shells out or calls the bundled integration advertised for this catalogue id.",
    "safety": "Operate only inside authorized penetration tests or sanctioned lab networks. Respect stop conditions, lawful intercept rules, and internal change windows. Sensitive parameters are captured in CipherStrike tenant execution logs alongside NyxStrike response snippets \u2014 avoid pasting production secrets when alternatives exist. Wireless probing and injection are lawful only on spectrum and networks you control by license or contract.",
    "usage": "CipherStrike `kismet` executes against `/api/tools/kismet` via POST JSON envelopes assembled in the Execute modal.\n\n1. Provide interface definitions logging directories and drone alert preferences per kismet packaging.\n2. Primarily passive\u2014respect privacy expectations on shared spectrum.\n3. Expect huge PCAP volumes; prune routinely.\n4. Correlate sightings with authorised asset inventories only.\n\nCipherStrike mechanics: send a JSON POST from the Execute modal\u2014the agent drops empty optional scalars before invoking NyxStrike.\nPrefer explicit catalogue fields (`target`, `url`, `wordlist`, etc.) before `additional_args`; extra tokens must still match the packaged wrapper behaviour and your engagement rules.\nPer-field catalogue hints (first sentence of modal help):\n- `interface` (required): Network interface (`wlan0mon`, `eth0`) for Wi\u2011Fi or Responder jobs.\n- `log_prefix` (optional): Prefix applied to chunked log artefacts on long engagements.\n- `duration` (optional): Runtime cap for Responder poison loops or Falco capture windows.\n- `additional_args` (optional): Shell tokens appended after mapped CLI arguments."
  },
  "kube-bench": {
    "long_description": "kube-bench executes CIS Kubernetes benchmark checks constrained by kubeconfig targets/version hints/config directories.\n\nCloud posture connectors emit JSON/HTML/SARIF findings enumerating IAM sprawl, open buckets, CIS deviations, stale keys, and attack-path narratives.\n\nCloud scans require least-privilege API roles scoped to enumerated accounts\u2014not root keys embedded in plaintext JSON long term.\n\nProvider APIs log every call; minimise repeated broad scans and purge credentials promptly after tabletop windows.\n\nCipherStrike transmits the JSON envelope to `/api/tools/kube-bench`; NyxStrike performs final validation, then shells out or calls the bundled integration advertised for this catalogue id.",
    "safety": "Operate only inside authorized penetration tests or sanctioned lab networks. Respect stop conditions, lawful intercept rules, and internal change windows. Sensitive parameters are captured in CipherStrike tenant execution logs alongside NyxStrike response snippets \u2014 avoid pasting production secrets when alternatives exist. Cloud API tokens authenticate broad actions\u2014purge keys after tabletop exercises.",
    "usage": "CipherStrike `kube-bench` executes against `/api/tools/kube-bench` via POST JSON envelopes assembled in the Execute modal.\n\n1. Provide kubeconfig paths image refs benchmark ids directories matching Trivy/Checkov/kube-bench/Clair/Falco wrappers.\n2. Align severities/report formats with SOC ingestion pipelines.\n3. Remediation tips require owner teams\u2014don't auto-patch.\n4. Some scans mutate nothing; others exec inside clusters\u2014confirm blast radius.\n\nCipherStrike mechanics: send a JSON POST from the Execute modal\u2014the agent drops empty optional scalars before invoking NyxStrike.\nPrefer explicit catalogue fields (`target`, `url`, `wordlist`, etc.) before `additional_args`; extra tokens must still match the packaged wrapper behaviour and your engagement rules.\nPer-field catalogue hints (first sentence of modal help):\n- `targets` (optional): Host groups for kube-bench-style scopes or multitarget wrappers.\n- `version` (optional): Surface `--version`-style banners when connectors expose them.\n- `config_dir` (optional): kube-bench JSON directory or auxiliary DNS config roots.\n- `output_format` (optional): Structured report format (`json`, `xml`, `sarif`, `html`).\n- `additional_args` (optional): Shell tokens appended after mapped CLI arguments."
  },
  "kube-hunter": {
    "long_description": "kube-hunter actively/passively probes Kubernetes clusters for attack paths (exposed dashboards, kubelet APIs, mis-scoped service accounts) per upstream hunter modules.\n\nCloud posture connectors emit JSON/HTML/SARIF findings enumerating IAM sprawl, open buckets, CIS deviations, stale keys, and attack-path narratives.\n\nCloud scans require least-privilege API roles scoped to enumerated accounts\u2014not root keys embedded in plaintext JSON long term.\n\nProvider APIs log every call; minimise repeated broad scans and purge credentials promptly after tabletop windows.\n\nCipherStrike transmits the JSON envelope to `/api/tools/kube-hunter`; NyxStrike performs final validation, then shells out or calls the bundled integration advertised for this catalogue id.",
    "safety": "Operate only inside authorized penetration tests or sanctioned lab networks. Respect stop conditions, lawful intercept rules, and internal change windows. Sensitive parameters are captured in CipherStrike tenant execution logs alongside NyxStrike response snippets \u2014 avoid pasting production secrets when alternatives exist. Cloud API tokens authenticate broad actions\u2014purge keys after tabletop exercises.",
    "usage": "CipherStrike `kube-hunter` executes against `/api/tools/kube-hunter` via POST JSON envelopes assembled in the Execute modal.\n\n1. Deliver cluster entrypoints hunters modules per kube-hunter route.\n2. Active hunters may disrupt workloads\u2014coordinate with platform SRE.\n3. Store JSON outputs for ticketing.\n4. Never aim at unmanaged multi-tenant clusters.\n\nCipherStrike mechanics: send a JSON POST from the Execute modal\u2014the agent drops empty optional scalars before invoking NyxStrike.\nPrefer explicit catalogue fields (`target`, `url`, `wordlist`, etc.) before `additional_args`; extra tokens must still match the packaged wrapper behaviour and your engagement rules.\nPer-field catalogue hints (first sentence of modal help):\n- `additional_args` (optional): Shell tokens appended after mapped CLI arguments."
  },
  "ldapdomaindump": {
    "long_description": "ldapdomaindump pulls LDAP-visible AD objects\u2014users, groups, computers, policies\u2014ideal for foothold inventories when LDAP binds succeed.\n\nAD helpers export user lists, trusts, constrained delegations, policy summaries, responder captures, SMB maps, LDAP dumps.\n\nUse dedicated assessment domain accounts wherever possible\u2014these emits land in Defender and SIEM timelines.\n\nCredential material in transit may enable domain-wide compromise\u2014treat transcripts as collateral damage.\n\nCipherStrike transmits the JSON envelope to `/api/tools/active_directory/ldapdomaindump`; NyxStrike performs final validation, then shells out or calls the bundled integration advertised for this catalogue id.",
    "safety": "Operate only inside authorized penetration tests or sanctioned lab networks. Respect stop conditions, lawful intercept rules, and internal change windows. Sensitive parameters are captured in CipherStrike tenant execution logs alongside NyxStrike response snippets \u2014 avoid pasting production secrets when alternatives exist. Domain queries leave authentication telemetry\u2014prefer dedicated assessment accounts.",
    "usage": "CipherStrike `ldapdomaindump` executes against `/api/tools/active_directory/ldapdomaindump` via POST JSON envelopes assembled in the Execute modal.\n\n1. Supply `domain`, LDAP `username`/`password` or hashes, `dc_ip`, and TLS toggles matching ldapdomaindump needs.\n2. Review HTML/JSON dumps for unintended exposure before sharing broadly.\n3. Pair with Kerberos ticketing tests only when dual-authorised.\n4. Limit scope to organisational units named in contracts.\n\nCipherStrike mechanics: send a JSON POST from the Execute modal\u2014the agent drops empty optional scalars before invoking NyxStrike.\nPrefer explicit catalogue fields (`target`, `url`, `wordlist`, etc.) before `additional_args`; extra tokens must still match the packaged wrapper behaviour and your engagement rules.\nPer-field catalogue hints (first sentence of modal help):\n- `hostname` (required): The workspace forwards `hostname` in the CipherStrike POST body unchanged after stripping empty optional fields.\n- `username` (optional): Username for SMB/LDAP/WinRM clients and login brute modules.\n- `password` (optional): Cleartext password for authenticated scans\u2014rotate if leaked into logs.\n- `authtype` (optional): LDAP bind mode (`NTLM`, `SIMPLE`, \u2026) for ldapdomaindump."
  },
  "libc-database": {
    "long_description": "libc-database matches leaked symbol offsets to packaged libc builds for exploit reliability on remote targets.\n\nDebuggers/disassemblers/gadget hunters produce listings, scripted traces, annotated graphs, emulation logs, or CTF scaffolding.\n\nStatic analysis excels on offline artefacts; symbolic tooling demands compute and may path-explode on large binaries.\n\nMalware-grade samples may trigger host controls\u2014sandbox accordingly and sanitise artefacts before sharing externally.\n\nCipherStrike transmits the JSON envelope to `/api/tools/libc-database`; NyxStrike performs final validation, then shells out or calls the bundled integration advertised for this catalogue id.",
    "safety": "Operate only inside authorized penetration tests or sanctioned lab networks. Respect stop conditions, lawful intercept rules, and internal change windows. Sensitive parameters are captured in CipherStrike tenant execution logs alongside NyxStrike response snippets \u2014 avoid pasting production secrets when alternatives exist. Binary tooling may invoke malware safeguards\u2014sandbox untrusted specimens.",
    "usage": "CipherStrike `libc-database` executes against `/api/tools/libc-database` via POST JSON envelopes assembled in the Execute modal.\n\n1. Provide `binary`, `libc_path`, architectures, libc IDs, gadgets filters per tool.\n2. Validate gadget addresses on target libc builds\u2014offsets shift with patches.\n3. Document gadget chains reviewed by peers.\n4. Keep exploit artifacts confidential.\n\nCipherStrike mechanics: send a JSON POST from the Execute modal\u2014the agent drops empty optional scalars before invoking NyxStrike.\nPrefer explicit catalogue fields (`target`, `url`, `wordlist`, etc.) before `additional_args`; extra tokens must still match the packaged wrapper behaviour and your engagement rules.\nPer-field catalogue hints (first sentence of modal help):\n- `symbols` (required): Symbol offset blob identifying libc for libc-database lookups.\n- `action` (optional): `airmon-ng` verb (`start`/`stop`) or CloudMapper subcommand name.\n- `libc_id` (optional): libc-database download id after a match is found.\n- `additional_args` (optional): Shell tokens appended after mapped CLI arguments."
  },
  "maltego": {
    "long_description": "Maltego remote transforms pivot OSINT graphs\u2014supply target seeds plus transform descriptors compatible with investigator editions.\n\nResults are textual lists, JSON artefacts, archive URLs, or passive reconnaissance breadcrumbs suitable for ingestion into trackers.\n\nOSINT tooling should precede intrusive scanning wherever policy demands minimal touch; escalate to DNS brute or crawler stages only after validating scope wording.\n\nPassive APIs throttle aggressively and may log lookups under shared keys\u2014credential handling matters.\n\nCipherStrike transmits the JSON envelope to `/api/tools/maltego`; NyxStrike performs final validation, then shells out or calls the bundled integration advertised for this catalogue id.",
    "safety": "Operate only inside authorized penetration tests or sanctioned lab networks. Respect stop conditions, lawful intercept rules, and internal change windows. Sensitive parameters are captured in CipherStrike tenant execution logs alongside NyxStrike response snippets \u2014 avoid pasting production secrets when alternatives exist.",
    "usage": "CipherStrike `maltego` executes against `/api/tools/maltego` via POST JSON envelopes assembled in the Execute modal.\n\n1. Load workspace modules transforms API keys complying with Spiderfoot/Recon-ng/Maltego licensing.\n2. Longitudinal jobs (`poll_interval`) may take hours\u2014checkpoint state.\n3. Graph exports can leak third-party identifiers handle per GDPR charters.\n4. Disable modules that scrape disallowed jurisdictions.\n\nCipherStrike mechanics: send a JSON POST from the Execute modal\u2014the agent drops empty optional scalars before invoking NyxStrike.\nPrefer explicit catalogue fields (`target`, `url`, `wordlist`, etc.) before `additional_args`; extra tokens must still match the packaged wrapper behaviour and your engagement rules.\nPer-field catalogue hints (first sentence of modal help):\n- `target` (required): Primary IPv4/IPv6/hostname/CIDR or bulk newline list routed into network scanners and RPC clients.\n- `transforms` (optional): Maltego transform identifiers or descriptors.\n- `additional_args` (optional): Shell tokens appended after mapped CLI arguments."
  },
  "masscan": {
    "long_description": "Masscan is Robert Graham\u2019s asynchronous SYN scanner designed for sweeping very large nets quickly by issuing raw packets with an isolated userspace TCP stack\u2014rate-limit explicitly because it stresses upstream transit.\n\nMasscan prints asynchronous handshake outcomes at extreme speeds using its isolated TCP stack summaries.\n\nUse Masscan before Nmap when you must enumerate broad CIDRs quickly\u2014but always constrain rates and reconcile with ICMP discovery gaps.\n\nAsynchronous pacing can saturate uplinks causing collateral packet loss alarms.\n\nCipherStrike transmits the JSON envelope to `/api/tools/masscan`; NyxStrike performs final validation, then shells out or calls the bundled integration advertised for this catalogue id.",
    "safety": "Operate only inside authorized penetration tests or sanctioned lab networks. Respect stop conditions, lawful intercept rules, and internal change windows. Sensitive parameters are captured in CipherStrike tenant execution logs alongside NyxStrike response snippets \u2014 avoid pasting production secrets when alternatives exist. Port scanners and handshake grabbers alert defenders\u2014mirror scope IPs exactly as authorised. Packet floods can trigger upstream DDoS mitigations\u2014start with small rates and expand only on owned ranges.",
    "usage": "CipherStrike `masscan` executes against `/api/tools/masscan` via POST JSON envelopes assembled in the Execute modal.\n\n1. Limit `target` to cleared ranges; Masscan\u2019s speed makes accidental neighbour scanning common.\n2. Set `rate`, `ports`, and interface selections conservatively; document the chosen packets-per-second in run notes.\n3. Expect binary-friendly output\u2014pipe results into Rustscan/Nmap for deeper fingerprints.\n4. Never raise rate to defeat upstream QoS; coordinate with network owners if drops appear.\n\nCipherStrike mechanics: send a JSON POST from the Execute modal\u2014the agent drops empty optional scalars before invoking NyxStrike.\nPrefer explicit catalogue fields (`target`, `url`, `wordlist`, etc.) before `additional_args`; extra tokens must still match the packaged wrapper behaviour and your engagement rules.\nPer-field catalogue hints (first sentence of modal help):\n- `target` (required): Primary IPv4/IPv6/hostname/CIDR or bulk newline list routed into network scanners and RPC clients.\n- `ports` (optional): Port list (`22,443`, `1-65535`) for Masscan/Nmap templates.\n- `rate` (optional): Masscan `--rate` packets-per-second ceiling\u2014keep low outside owned networks.\n- `additional_args` (optional): Shell tokens appended after mapped CLI arguments."
  },
  "massdns": {
    "long_description": "MassDNS is a staggeringly fast resolver stub issuing millions of iterative queries with socket fan-out knobs, hashing heuristics, and optional drop-privilege controls\u2014keep resolvers sane.\n\nMassDNS streams enormous resolver datasets with hashing-friendly outputs plus optional flushing controls.\n\nPair MassDNS upstream of shuffleDNS or internal analytics when brute lists exceed millions of queries.\n\nResolver abuse triggers ISP abuse desks\u2014whitelist resolvers throttle concurrency and purge sensitive labels.\n\nCipherStrike transmits the JSON envelope to `/api/tools/massdns`; NyxStrike performs final validation, then shells out or calls the bundled integration advertised for this catalogue id.",
    "safety": "Operate only inside authorized penetration tests or sanctioned lab networks. Respect stop conditions, lawful intercept rules, and internal change windows. Sensitive parameters are captured in CipherStrike tenant execution logs alongside NyxStrike response snippets \u2014 avoid pasting production secrets when alternatives exist. Resolver storms look like botnet traffic; private resolvers and hard caps are mandatory outside isolated labs.",
    "usage": "CipherStrike `massdns` executes against `/api/tools/massdns` via POST JSON envelopes assembled in the Execute modal.\n\n1. Populate `domains` corpus and resolver tuning (`resolve_count`, `threads`, hashes, flush flags).\n2. Always bound jobs\u2014MassDNS excels at unintended self-DDoS via resolvers.\n3. Inspect logs for truncation; large wordlists spill to disk unexpectedly.\n4. Pair with PCAP only when lawful taps exist.\n\nCipherStrike mechanics: send a JSON POST from the Execute modal\u2014the agent drops empty optional scalars before invoking NyxStrike.\nPrefer explicit catalogue fields (`target`, `url`, `wordlist`, etc.) before `additional_args`; extra tokens must still match the packaged wrapper behaviour and your engagement rules.\nPer-field catalogue hints (first sentence of modal help):\n- `domainlist` (required): Path to newline-separated domains for MassDNS bulk resolution.\n- `bindto` (optional): Local IPv4 MassDNS binds to for egress control.\n- `busy_poll` (optional): MassDNS trades CPU for lower latency via tight socket polling loops.\n- `resolve_count` (optional): MassDNS simultaneous resolution concurrency (`-r`).\n- `drop_group` (optional): Supplemental group drop for hardened MassDNS runs.\n- `drop_user` (optional): Unprivileged POSIX user MassDNS drops to after binding.\n- `extended_input` (optional): Allow MassDNS extended input encodings beyond bare labels.\n- `filter` (optional): MassDNS output-filter DSL restricting printed answers.\n- `flush` (optional): Force MassDNS to flush partial results mid-run.\n- `ignore` (optional): Substring denylist skipping brittle resolver targets.\n- `interval` (optional): Millis between MassDNS resolver batches.\n- `error_log` (optional): MassDNS stderr/log redirection target.\n- `norecurse` (optional): MassDNS stub mode without recursive iteration.\n- `output` (optional): Primary output sink some CLIs require (`-o`, `--output`).\n- `predictable` (optional): Deterministic MassDNS behaviour for repeatable tests.\n- `processes` (optional): The workspace forwards `processes` in the CipherStrike POST body unchanged after stripping empty optional fields.\n- `quiet` (optional): Suppress non-error MassDNS chatter.\n- `rand_src_ipv6` (optional): Textual IPv6 source selection hint for MassDNS.\n- `rcvbuf` (optional): Kernel receive-buffer hint for MassDNS sockets.\n- _(and 12 more optional keys \u2014 see catalogue defaults in the Execute modal)._\n- MassDNS emits huge DNS traffic \u2014 limit `resolve_count`, `threads`, or input size on shared links."
  },
  "mdk4": {
    "long_description": "MDK4 is a frame injection destructor testing AP/client resilience (`deauth`, `beacon flood`, ESSIDs) strictly for cages you control.\n\n802.11 harnesses emit captures, WPA hash material, rogue AP scaffolding, injector acknowledgements, and audit logs keyed to channels/BSSIDs.\n\nLaboratory rigs should dedicate monitor-mode interfaces plus isolated spectrum; tethered enterprise APs rarely tolerate injection homework.\n\nJamming neighbours or intercepting unauthorised traffic violates law\u2014operate strictly inside sanctioned RF enclosures and licensed bands.\n\nCipherStrike transmits the JSON envelope to `/api/tools/wifi_pentest/mdk4`; NyxStrike performs final validation, then shells out or calls the bundled integration advertised for this catalogue id.",
    "safety": "Operate only inside authorized penetration tests or sanctioned lab networks. Respect stop conditions, lawful intercept rules, and internal change windows. Sensitive parameters are captured in CipherStrike tenant execution logs alongside NyxStrike response snippets \u2014 avoid pasting production secrets when alternatives exist. Wireless probing and injection are lawful only on spectrum and networks you control by license or contract. Destruction testing can brick cheap APs\u2014expect hardware failures.",
    "usage": "CipherStrike `mdk4` executes against `/api/tools/wifi_pentest/mdk4` via POST JSON envelopes assembled in the Execute modal.\n\n1. Place adapters in monitor mode upstream when required (`airmon-ng` helper routes).\n2. Provide capture paths, ESSIDs/EAP configs, handshake targets, attack modes, regulatory paperwork references.\n3. Never point attack modes at civilian infrastructure\u2014hardware isolation only.\n4. Convert captures only within labs; distributing PMKIDs may violate telecom rules.\n\nCipherStrike mechanics: send a JSON POST from the Execute modal\u2014the agent drops empty optional scalars before invoking NyxStrike.\nPrefer explicit catalogue fields (`target`, `url`, `wordlist`, etc.) before `additional_args`; extra tokens must still match the packaged wrapper behaviour and your engagement rules.\nPer-field catalogue hints (first sentence of modal help):\n- `interface` (required): Network interface (`wlan0mon`, `eth0`) for Wi\u2011Fi or Responder jobs.\n- `attack_mode` (required): `aireplay-ng` attack name **or** hashcat `-a` attack id\u2014meaning depends on catalogue entry context.\n- `target_bssid` (required): 802.11 BSSID narrowed for Bettercap Wi\u2011Fi tooling.\n- `ssid_wordlist` (optional): Newline wordlist path on the agent for brute/DNS/content tooling.\n- `burst_rate` (optional): Wi\u2011Fi injection throttle\u2014reduce outside shielded enclosures."
  },
  "medusa": {
    "long_description": "Modular login brute-force frameworks\u2014Medusa stresses parallel transports while Patator unifies dictionaries across SMB, SSH, HTTP, etc., with Pythonic verbosity controls.\n\nMedusa emphasises stable parallel transport modules with predictable output tables.\n\nChoose Medusa for simpler parallel modules hydra lacks Patator retains scriptability.\n\nTune `threads` to respect lockout policies.\n\nCipherStrike transmits the JSON envelope to `/api/tools/medusa`; NyxStrike performs final validation, then shells out or calls the bundled integration advertised for this catalogue id.",
    "safety": "Operate only inside authorized penetration tests or sanctioned lab networks. Respect stop conditions, lawful intercept rules, and internal change windows. Sensitive parameters are captured in CipherStrike tenant execution logs alongside NyxStrike response snippets \u2014 avoid pasting production secrets when alternatives exist. Credential guesses can trigger lockouts; coordinate with SOC and honour throttling mandates. Same lockout cautions as Hydra; prefer service-specific throttles.",
    "usage": "CipherStrike `medusa` executes against `/api/tools/medusa` via POST JSON envelopes assembled in the Execute modal.\n\n1. Provide `target`, `username`/`username_file`, `password_file`, and protocol/service selectors.\n2. Apply `threads`, `delay`, and attempt caps mandated by SOC.\n3. Stop on first success unless horizontal movement is chartered\u2014credential stuffing may violate policy.\n4. Archive cracked creds securely; rotate them after testing.\n\nCipherStrike mechanics: send a JSON POST from the Execute modal\u2014the agent drops empty optional scalars before invoking NyxStrike.\nPrefer explicit catalogue fields (`target`, `url`, `wordlist`, etc.) before `additional_args`; extra tokens must still match the packaged wrapper behaviour and your engagement rules.\nPer-field catalogue hints (first sentence of modal help):\n- `target` (required): Primary IPv4/IPv6/hostname/CIDR or bulk newline list routed into network scanners and RPC clients.\n- `module` (required): Metasploit module path, Patator dialect, or NetExec plugin name.\n- `username` (optional): Username for SMB/LDAP/WinRM clients and login brute modules.\n- `username_file` (optional): Username list for credential spraying modules.\n- `password` (optional): Cleartext password for authenticated scans\u2014rotate if leaked into logs.\n- `password_file` (optional): Dictionary file for Hydra/Medusa/Patator.\n- `additional_args` (optional): Shell tokens appended after mapped CLI arguments."
  },
  "msfconsole": {
    "long_description": "Metasploit console automation accepts module paths plus option dictionaries aligning with canonical `use`, `set`, `run` flows.\n\nmsfconsole transcripts include module consoles jobs sessions resource script errors.\n\nMetasploit remains reference for staged exploits coordinating listeners though noisier than custom tooling.\n\nPayload handlers open listener ports tighten firewall narratives.\n\nCipherStrike transmits the JSON envelope to `/api/tools/metasploit`; NyxStrike performs final validation, then shells out or calls the bundled integration advertised for this catalogue id.",
    "safety": "Operate only inside authorized penetration tests or sanctioned lab networks. Respect stop conditions, lawful intercept rules, and internal change windows. Sensitive parameters are captured in CipherStrike tenant execution logs alongside NyxStrike response snippets \u2014 avoid pasting production secrets when alternatives exist. Weaponised modules may crash services\u2014snapshot lab VMs and segregate egress before launching. Exploit modules may destroy services; maintain VM snapshots and segregate listener hosts.",
    "usage": "CipherStrike `msfconsole` executes against `/api/tools/metasploit` via POST JSON envelopes assembled in the Execute modal.\n\n1. Identify `module` plus `options` dict mirroring msfconsole `set` verbs.\n2. Dry-run payloads on disposable VMs first.\n3. Watch job output for unstaged timeouts\u2014handlers must be reachable legally.\n4. Never cross legal boundaries via auto-run scripts.\n\nCipherStrike mechanics: send a JSON POST from the Execute modal\u2014the agent drops empty optional scalars before invoking NyxStrike.\nPrefer explicit catalogue fields (`target`, `url`, `wordlist`, etc.) before `additional_args`; extra tokens must still match the packaged wrapper behaviour and your engagement rules.\nPer-field catalogue hints (first sentence of modal help):\n- `module` (required): Metasploit module path, Patator dialect, or NetExec plugin name.\n- `options` (optional): JSON map of Metasploit `set` assignments for scripted console use.\n- `options` should mirror Metasploit `set` pairs; payloads execute only on sanctioned lab hosts."
  },
  "msfvenom": {
    "long_description": "Metasploit\u2019s msfvenom stages encoded payloads targeting multiple architectures and transports (reverse/bind meterpreter, staged/unstaged) with selectable output formats (`elf`, `exe`, `msi`, scripting languages).\n\nmsfvenom prints encoder summaries payload bytes optional templates.\n\nmsfvenom centralises staged payload recipes versus compiling shellcode manually.\n\nArtifacts must stay inside malware analysis pipelines.\n\nCipherStrike transmits the JSON envelope to `/api/tools/msfvenom`; NyxStrike performs final validation, then shells out or calls the bundled integration advertised for this catalogue id.",
    "safety": "Operate only inside authorized penetration tests or sanctioned lab networks. Respect stop conditions, lawful intercept rules, and internal change windows. Sensitive parameters are captured in CipherStrike tenant execution logs alongside NyxStrike response snippets \u2014 avoid pasting production secrets when alternatives exist. Weaponised modules may crash services\u2014snapshot lab VMs and segregate egress before launching. Generated binaries are indistinguishable from real malware\u2014label, hash, and store securely.",
    "usage": "CipherStrike `msfvenom` executes against `/api/tools/msfvenom` via POST JSON envelopes assembled in the Execute modal.\n\n1. Select `payload`, `format`, `lhost`, `lport`, and encoder options documented for msfvenom.\n2. Hash every artifact, document listener expectations, and store binaries encrypted.\n3. Test stage-specific networking (NAT, egress allowlists) before engagement.\n4. Never deliver msfvenom output to unauthorised recipients.\n\nCipherStrike mechanics: send a JSON POST from the Execute modal\u2014the agent drops empty optional scalars before invoking NyxStrike.\nPrefer explicit catalogue fields (`target`, `url`, `wordlist`, etc.) before `additional_args`; extra tokens must still match the packaged wrapper behaviour and your engagement rules.\nPer-field catalogue hints (first sentence of modal help):\n- `payload` (required): msfvenom payload specifier (`windows/x64/meterpreter/reverse_tcp`).\n- `format` (optional): msfvenom output container (`elf`, `exe`, `raw`, `python`).\n- `lhost` (optional): Callback host embedded in generated payloads.\n- `lport` (optional): Callback port embedded in generated payloads.\n- `additional_args` (optional): Shell tokens appended after mapped CLI arguments."
  },
  "mysql": {
    "long_description": "mysql/mariadb CLI connector runs scoped SQL probes\u2014prefer least-privilege DB users and terminate TLS tunnels per vendor guidance.\n\nDatabase probes show schema excerpts, benign SQL probe results, latency statistics, credential failures.\n\nUse least-privilege database accounts with read-mostly grants unless destructive testing is chartered.\n\nSQL strings echo to DB audit logs alongside NyxStrike\u2014avoid production accounts without rollover plans.\n\nCipherStrike transmits the JSON envelope to `/api/tools/mysql`; NyxStrike performs final validation, then shells out or calls the bundled integration advertised for this catalogue id.",
    "safety": "Operate only inside authorized penetration tests or sanctioned lab networks. Respect stop conditions, lawful intercept rules, and internal change windows. Sensitive parameters are captured in CipherStrike tenant execution logs alongside NyxStrike response snippets \u2014 avoid pasting production secrets when alternatives exist. Database credentials in JSON propagate to bastion logs\u2014rotate after tabletop exercises. SQL statements may modify data even when read-only intent\u2014use replica sandboxes when possible.",
    "usage": "CipherStrike `mysql` executes against `/api/tools/mysql` via POST JSON envelopes assembled in the Execute modal.\n\n1. Supply connection strings databases SQL fragments credentials per mysql/sqlite wrappers.\n2. Dry-run selects before mutating\u2014even read queries may lock rows.\n3. Log every statement for auditors.\n4. Use disposable databases whenever feasible.\n\nCipherStrike mechanics: send a JSON POST from the Execute modal\u2014the agent drops empty optional scalars before invoking NyxStrike.\nPrefer explicit catalogue fields (`target`, `url`, `wordlist`, etc.) before `additional_args`; extra tokens must still match the packaged wrapper behaviour and your engagement rules.\nPer-field catalogue hints (first sentence of modal help):\n- `query` (required): The workspace forwards `query` in the CipherStrike POST body unchanged after stripping empty optional fields.\n- `host` (required): The workspace forwards `host` in the CipherStrike POST body unchanged after stripping empty optional fields.\n- `user` (required): The workspace forwards `user` in the CipherStrike POST body unchanged after stripping empty optional fields.\n- `password` (required): Cleartext password for authenticated scans\u2014rotate if leaked into logs.\n- `database` (required): The workspace forwards `database` in the CipherStrike POST body unchanged after stripping empty optional fields."
  },
  "nbtscan": {
    "long_description": "nbtscan issues NetBIOS status probes across LAN segments spotting Windows hosts revealing names/workgroups swiftly.\n\nExpect port listings, traceroute-like hints, service banners, NIC inventories, handshake captures, or ARP inventories depending on tooling.\n\nFast SYN scanners suit huge ranges; balanced scanners add scripts and versioning; SMB-oriented tools deepen Windows visibility\u2014choose based on scope scale and covertness mandates.\n\nHigh packet rates overwhelm small networks\u2014rate-limit, subdivide subnets, and obtain written approval before touching neighbouring tenants.\n\nCipherStrike transmits the JSON envelope to `/api/tools/nbtscan`; NyxStrike performs final validation, then shells out or calls the bundled integration advertised for this catalogue id.",
    "safety": "Operate only inside authorized penetration tests or sanctioned lab networks. Respect stop conditions, lawful intercept rules, and internal change windows. Sensitive parameters are captured in CipherStrike tenant execution logs alongside NyxStrike response snippets \u2014 avoid pasting production secrets when alternatives exist. Port scanners and handshake grabbers alert defenders\u2014mirror scope IPs exactly as authorised.",
    "usage": "CipherStrike `nbtscan` executes against `/api/tools/nbtscan` via POST JSON envelopes assembled in the Execute modal.\n\n1. Provide subnet/CIDR/IP lists plus timing fields accepted by `nbtscan`-style wrappers on the bastion.\n2. Expect coarse NetBIOS names/workgroups\u2014not a substitute for SMB authentication testing.\n3. Pair positive hits with `enum4linux(-ng)`, `rpcclient`, or `smbmap` once credentialed access is chartered.\n4. Broadcast scopes can overwhelm small offices\u2014obtain SOC approval before broad sweeps.\n\nCipherStrike mechanics: send a JSON POST from the Execute modal\u2014the agent drops empty optional scalars before invoking NyxStrike.\nPrefer explicit catalogue fields (`target`, `url`, `wordlist`, etc.) before `additional_args`; extra tokens must still match the packaged wrapper behaviour and your engagement rules.\nPer-field catalogue hints (first sentence of modal help):\n- `target` (required): Primary IPv4/IPv6/hostname/CIDR or bulk newline list routed into network scanners and RPC clients.\n- `verbose` (optional): Extra SMB/RPC/DNS chatter for troubleshooting.\n- `timeout` (optional): Seconds for socket/DNS/CLI overall timeouts.\n- `additional_args` (optional): Shell tokens appended after mapped CLI arguments."
  },
  "nikto": {
    "long_description": "Nikto is a long-standing CGI/web server auditor that hunts dangerous files, insecure headers, risky methods, outdated software fingerprints, and canned misconfiguration patterns.\n\nNikto logs verbose CGI misconfig risky headers obsolete modules Tomcat fingerprints.\n\nNikto excels at shotgun webserver regression sweeps complementary to nuclei's template specificity.\n\nHuge output buries actionable items triage diligently.\n\nCipherStrike transmits the JSON envelope to `/api/tools/nikto`; NyxStrike performs final validation, then shells out or calls the bundled integration advertised for this catalogue id.",
    "safety": "Operate only inside authorized penetration tests or sanctioned lab networks. Respect stop conditions, lawful intercept rules, and internal change windows. Sensitive parameters are captured in CipherStrike tenant execution logs alongside NyxStrike response snippets \u2014 avoid pasting production secrets when alternatives exist.",
    "usage": "CipherStrike `nikto` executes against `/api/tools/nikto` via POST JSON envelopes assembled in the Execute modal.\n\n1. Supply `target` root URLs; Nikto will crawl auxiliary paths aggressively.\n2. Expect verbose positives\u2014correlate with contemporary threat models because many checks are historical.\n3. Pair with `whatweb` or `nuclei` for modern coverage.\n4. Rate-limit when reverse proxies are sensitive to duplicate requests.\n\nCipherStrike mechanics: send a JSON POST from the Execute modal\u2014the agent drops empty optional scalars before invoking NyxStrike.\nPrefer explicit catalogue fields (`target`, `url`, `wordlist`, etc.) before `additional_args`; extra tokens must still match the packaged wrapper behaviour and your engagement rules.\nPer-field catalogue hints (first sentence of modal help):\n- `target` (required): Primary IPv4/IPv6/hostname/CIDR or bulk newline list routed into network scanners and RPC clients.\n- `additional_args` (optional): Shell tokens appended after mapped CLI arguments."
  },
  "nmap": {
    "long_description": "Nmap (Network Mapper) is the open-source scanner for host discovery, TCP/UDP probing, service fingerprinting, and Nmap Scripting Engine checks; results label ports as open, closed, or filtered per Nmap\u2019s state machine.\n\nNmap returns host discovery summaries, TCP/UDP state tables (`open|filtered|closed`), scripted findings, traceroute breadcrumbs, optional OS guesses, and NSE artefacts depending on selections.\n\nChoose Nmap when you need balanced coverage with scripting fingerprints; defer to Masscan or Rustscan for Internet-scale sweeps, then deepen hot hosts with targeted Nmap passes.\n\nPrivileged SYN scans (`-sS`) require raw sockets; UDP passes are noisy and slow.\n\nCipherStrike transmits the JSON envelope to `/api/tools/nmap`; NyxStrike performs final validation, then shells out or calls the bundled integration advertised for this catalogue id.",
    "safety": "Operate only inside authorized penetration tests or sanctioned lab networks. Respect stop conditions, lawful intercept rules, and internal change windows. Sensitive parameters are captured in CipherStrike tenant execution logs alongside NyxStrike response snippets \u2014 avoid pasting production secrets when alternatives exist.",
    "usage": "CipherStrike `nmap` executes against `/api/tools/nmap` via POST JSON envelopes assembled in the Execute modal.\n\n1. Enumerate `target` exactly as approved (single host, CIDR, or file reference per connector contract).\n2. Map scan intent to `ports`, `scan_type`, `timing`, and boolean service/OS detection flags before touching `additional_args`.\n3. Advanced entries should align `nse_scripts` and stealth toggles with SOC notification requirements.\n4. Read output chronologically: host discovery, port states, then scripts; treat `filtered` as inconclusive without follow-up.\n5. Reserve `additional_args` for flags not exposed as first-class JSON fields after verifying the NyxStrike template supports them.\n\nCipherStrike mechanics: send a JSON POST from the Execute modal\u2014the agent drops empty optional scalars before invoking NyxStrike.\nPrefer explicit catalogue fields (`target`, `url`, `wordlist`, etc.) before `additional_args`; extra tokens must still match the packaged wrapper behaviour and your engagement rules.\nPer-field catalogue hints (first sentence of modal help):\n- `target` (required): Primary IPv4/IPv6/hostname/CIDR or bulk newline list routed into network scanners and RPC clients.\n- `scan_type` (optional): Connector-specific profile: Nmap flags, Trivy scan class, Burp macro, or ZAP policy name.\n- `ports` (optional): Port list (`22,443`, `1-65535`) for Masscan/Nmap templates.\n- `additional_args` (optional): Shell tokens appended after mapped CLI arguments."
  },
  "nmap_advanced": {
    "long_description": "Advanced Nmap profile exposes scripted scan types (`-sS`, UDP blends), granular timing templates, selectable NSE script lists, optional OS/version detection bundles, stealth toggles, and passthrough CLI fragments.\n\nAdvanced routes expose scripted timing templates granular NSE lists OS detection toggles stealth switches and sanctioned passthrough segments while remaining JSON-first.\n\nIdentical differentiation as base Nmap but with fuller control surfaces for mature operators automating regressions.\n\nMalformed `additional_args` easily break quoting\u2014trial on lab hosts.\n\nCipherStrike transmits the JSON envelope to `/api/tools/nmap-advanced`; NyxStrike performs final validation, then shells out or calls the bundled integration advertised for this catalogue id.",
    "safety": "Operate only inside authorized penetration tests or sanctioned lab networks. Respect stop conditions, lawful intercept rules, and internal change windows. Sensitive parameters are captured in CipherStrike tenant execution logs alongside NyxStrike response snippets \u2014 avoid pasting production secrets when alternatives exist. Port scanners and handshake grabbers alert defenders\u2014mirror scope IPs exactly as authorised.",
    "usage": "CipherStrike `nmap_advanced` executes against `/api/tools/nmap-advanced` via POST JSON envelopes assembled in the Execute modal.\n\n1. Enumerate `target` exactly as approved (single host, CIDR, or file reference per connector contract).\n2. Map scan intent to `ports`, `scan_type`, `timing`, and boolean service/OS detection flags before touching `additional_args`.\n3. Advanced entries should align `nse_scripts` and stealth toggles with SOC notification requirements.\n4. Read output chronologically: host discovery, port states, then scripts; treat `filtered` as inconclusive without follow-up.\n5. Reserve `additional_args` for flags not exposed as first-class JSON fields after verifying the NyxStrike template supports them.\n\nCipherStrike mechanics: send a JSON POST from the Execute modal\u2014the agent drops empty optional scalars before invoking NyxStrike.\nPrefer explicit catalogue fields (`target`, `url`, `wordlist`, etc.) before `additional_args`; extra tokens must still match the packaged wrapper behaviour and your engagement rules.\nPer-field catalogue hints (first sentence of modal help):\n- `target` (required): Primary IPv4/IPv6/hostname/CIDR or bulk newline list routed into network scanners and RPC clients.\n- `scan_type` (optional): Connector-specific profile: Nmap flags, Trivy scan class, Burp macro, or ZAP policy name.\n- `ports` (optional): Port list (`22,443`, `1-65535`) for Masscan/Nmap templates.\n- `timing` (optional): Nmap `-T0`\u2026`-T5` template for advanced wrappers.\n- `nse_scripts` (optional): Comma-separated NSE script list (`default,safe,vuln`).\n- `os_detection` (optional): Enable OS fingerprinting (`nmap -O`) where templates map it.\n- `version_detection` (optional): Enable service/version probing (`nmap -sV`) mappings.\n- `aggressive` (optional): When true, wrappers usually opt into intrusive bundles such as `nmap -A`; requires privileged raw sockets.\n- `stealth` (optional): Prefer quieter scan templates when mappers honour the flag.\n- `additional_args` (optional): Shell tokens appended after mapped CLI arguments."
  },
  "nuclei": {
    "long_description": "Nuclei is ProjectDiscovery\u2019s template DSL runner that executes YAML-defined checks spanning HTTP/TCP/cloud/file protocols with clustered execution and granular severity/tag filters.\n\nNuclei aggregates YAML severity tagged findings clustered by host template metadata optional collaborator hooks.\n\nNuclei templates replace bespoke curl scripts iterate faster than Nikto CGI sweeps albeit with template maintenance overhead.\n\nAggressive templates may contain destructive payloads read template metadata before unattended runs.\n\nCipherStrike transmits the JSON envelope to `/api/tools/nuclei`; NyxStrike performs final validation, then shells out or calls the bundled integration advertised for this catalogue id.",
    "safety": "Operate only inside authorized penetration tests or sanctioned lab networks. Respect stop conditions, lawful intercept rules, and internal change windows. Sensitive parameters are captured in CipherStrike tenant execution logs alongside NyxStrike response snippets \u2014 avoid pasting production secrets when alternatives exist. Active payloads may corrupt data stores\u2014replay only seeded test accounts created for the drill.",
    "usage": "CipherStrike `nuclei` executes against `/api/tools/nuclei` via POST JSON envelopes assembled in the Execute modal.\n\n1. Point `target` inputs (hosts, URLs) at approved assets and choose template directories or tags via structured fields.\n2. Start with low-severity templates, then escalate only after customer sign-off.\n3. Parse JSONL output for `critical`/`high` hits and manually confirm exploitability.\n4. Keep custom templates under version control; accidental destructive checks happen via typos.\n\nCipherStrike mechanics: send a JSON POST from the Execute modal\u2014the agent drops empty optional scalars before invoking NyxStrike.\nPrefer explicit catalogue fields (`target`, `url`, `wordlist`, etc.) before `additional_args`; extra tokens must still match the packaged wrapper behaviour and your engagement rules.\nPer-field catalogue hints (first sentence of modal help):\n- `target` (required): Primary IPv4/IPv6/hostname/CIDR or bulk newline list routed into network scanners and RPC clients.\n- `severity` (optional): Minimum issue class for terrascan/trivy style reporting.\n- `tags` (optional): exiftool tag projection or scanner tag filters.\n- `template` (optional): Nuclei template id glob/path when exposed by the route.\n- `additional_args` (optional): Shell tokens appended after mapped CLI arguments."
  },
  "nxc": {
    "long_description": "NetExec (classic CrackMapExec descendant) abstracts SMB/WinRM/LDAP/MSSQL post-explo modules with cohesive cred handling.\n\nExpect port listings, traceroute-like hints, service banners, NIC inventories, handshake captures, or ARP inventories depending on tooling.\n\nFast SYN scanners suit huge ranges; balanced scanners add scripts and versioning; SMB-oriented tools deepen Windows visibility\u2014choose based on scope scale and covertness mandates.\n\nHigh packet rates overwhelm small networks\u2014rate-limit, subdivide subnets, and obtain written approval before touching neighbouring tenants.\n\nCipherStrike transmits the JSON envelope to `/api/tools/netexec`; NyxStrike performs final validation, then shells out or calls the bundled integration advertised for this catalogue id.",
    "safety": "Operate only inside authorized penetration tests or sanctioned lab networks. Respect stop conditions, lawful intercept rules, and internal change windows. Sensitive parameters are captured in CipherStrike tenant execution logs alongside NyxStrike response snippets \u2014 avoid pasting production secrets when alternatives exist. Port scanners and handshake grabbers alert defenders\u2014mirror scope IPs exactly as authorised. CrackMapExec descendants are loud on SMB\u2014expect EDR correlation.",
    "usage": "CipherStrike `nxc` executes against `/api/tools/netexec` via POST JSON envelopes assembled in the Execute modal.\n\n1. Specify `protocol`, `target`, creds hashes modules command macros.\n2. Many modules escalate quickly\u2014tie each action to written objectives.\n3. Expect Defender correlation IDs referencing your source IP/time.\n4. Use jump boxes dedicated to engagements.\n\nCipherStrike mechanics: send a JSON POST from the Execute modal\u2014the agent drops empty optional scalars before invoking NyxStrike.\nPrefer explicit catalogue fields (`target`, `url`, `wordlist`, etc.) before `additional_args`; extra tokens must still match the packaged wrapper behaviour and your engagement rules.\nPer-field catalogue hints (first sentence of modal help):\n- `target` (required): Primary IPv4/IPv6/hostname/CIDR or bulk newline list routed into network scanners and RPC clients.\n- `protocol` (optional): NetExec transport (`smb`, `winrm`, `ldap`, `mssql`, \u2026).\n- `username` (optional): Username for SMB/LDAP/WinRM clients and login brute modules.\n- `password` (optional): Cleartext password for authenticated scans\u2014rotate if leaked into logs.\n- `module` (optional): Metasploit module path, Patator dialect, or NetExec plugin name.\n- `additional_args` (optional): Shell tokens appended after mapped CLI arguments."
  },
  "objdump": {
    "long_description": "GNU objdump disassembles object files/ELFs with optional intel syntax toggles and architecture-specific views.\n\nDebuggers/disassemblers/gadget hunters produce listings, scripted traces, annotated graphs, emulation logs, or CTF scaffolding.\n\nStatic analysis excels on offline artefacts; symbolic tooling demands compute and may path-explode on large binaries.\n\nMalware-grade samples may trigger host controls\u2014sandbox accordingly and sanitise artefacts before sharing externally.\n\nCipherStrike transmits the JSON envelope to `/api/tools/objdump`; NyxStrike performs final validation, then shells out or calls the bundled integration advertised for this catalogue id.",
    "safety": "Operate only inside authorized penetration tests or sanctioned lab networks. Respect stop conditions, lawful intercept rules, and internal change windows. Sensitive parameters are captured in CipherStrike tenant execution logs alongside NyxStrike response snippets \u2014 avoid pasting production secrets when alternatives exist. Binary tooling may invoke malware safeguards\u2014sandbox untrusted specimens.",
    "usage": "CipherStrike `objdump` executes against `/api/tools/objdump` via POST JSON envelopes assembled in the Execute modal.\n\n1. Pass file paths macros scripts analysis toggles aligning with gdb/radare2/ghidra headless wrappers.\n2. Long analyses need `timeout` fields\u2014coordinate with infra.\n3. Store project directories on encrypted volumes.\n4. Symbolic backends may spin CPU\u2014quota jobs fairly.\n\nCipherStrike mechanics: send a JSON POST from the Execute modal\u2014the agent drops empty optional scalars before invoking NyxStrike.\nPrefer explicit catalogue fields (`target`, `url`, `wordlist`, etc.) before `additional_args`; extra tokens must still match the packaged wrapper behaviour and your engagement rules.\nPer-field catalogue hints (first sentence of modal help):\n- `binary` (required): Executable for radare2/ghidra/objdump/ropper workflows.\n- `disassemble` (optional): Force disassembly-oriented output in objdump-style connectors.\n- `additional_args` (optional): Shell tokens appended after mapped CLI arguments."
  },
  "one-gadget": {
    "long_description": "one_gadget scans libc builds for single-instruction `execve(/bin/sh)` pivots using leakable constraints.\n\nDebuggers/disassemblers/gadget hunters produce listings, scripted traces, annotated graphs, emulation logs, or CTF scaffolding.\n\nStatic analysis excels on offline artefacts; symbolic tooling demands compute and may path-explode on large binaries.\n\nMalware-grade samples may trigger host controls\u2014sandbox accordingly and sanitise artefacts before sharing externally.\n\nCipherStrike transmits the JSON envelope to `/api/tools/one-gadget`; NyxStrike performs final validation, then shells out or calls the bundled integration advertised for this catalogue id.",
    "safety": "Operate only inside authorized penetration tests or sanctioned lab networks. Respect stop conditions, lawful intercept rules, and internal change windows. Sensitive parameters are captured in CipherStrike tenant execution logs alongside NyxStrike response snippets \u2014 avoid pasting production secrets when alternatives exist. Binary tooling may invoke malware safeguards\u2014sandbox untrusted specimens.",
    "usage": "CipherStrike `one-gadget` executes against `/api/tools/one-gadget` via POST JSON envelopes assembled in the Execute modal.\n\n1. Provide `binary`, `libc_path`, architectures, libc IDs, gadgets filters per tool.\n2. Validate gadget addresses on target libc builds\u2014offsets shift with patches.\n3. Document gadget chains reviewed by peers.\n4. Keep exploit artifacts confidential.\n\nCipherStrike mechanics: send a JSON POST from the Execute modal\u2014the agent drops empty optional scalars before invoking NyxStrike.\nPrefer explicit catalogue fields (`target`, `url`, `wordlist`, etc.) before `additional_args`; extra tokens must still match the packaged wrapper behaviour and your engagement rules.\nPer-field catalogue hints (first sentence of modal help):\n- `libc_path` (required): `libc.so.6` path examined by one_gadget.\n- `level` (optional): Commix injection depth, ophcrack gadget level, or similar integer tier.\n- `additional_args` (optional): Shell tokens appended after mapped CLI arguments."
  },
  "ophcrack": {
    "long_description": "Ophcrack loads rainbow tables optimised for speedy LM/NTLM hash lookups when suitable table packs exist on-disk.\n\nOutput enumerates guessed credentials, cracked hashes, cracked handshakes, or rule-engine statistics keyed to service modules.\n\nCredential attacks belong after softer enumeration proves plausible usernames/services; brute force last to avoid cascading lockouts.\n\nAlways enforce attempt ceilings, alerting hooks, and service-owner approval\u2014plaintext secrets land in bastion transcripts.\n\nCipherStrike transmits the JSON envelope to `/api/tools/password-cracking/ophcrack`; NyxStrike performs final validation, then shells out or calls the bundled integration advertised for this catalogue id.",
    "safety": "Operate only inside authorized penetration tests or sanctioned lab networks. Respect stop conditions, lawful intercept rules, and internal change windows. Sensitive parameters are captured in CipherStrike tenant execution logs alongside NyxStrike response snippets \u2014 avoid pasting production secrets when alternatives exist. Credential guesses can trigger lockouts; coordinate with SOC and honour throttling mandates.",
    "usage": "CipherStrike `ophcrack` executes against `/api/tools/password-cracking/ophcrack` via POST JSON envelopes assembled in the Execute modal.\n\n1. Identify formats with hashid-style helpers before importing into John-compatible modes.\n2. Provide hash files or SAM extracts as required; rainbow tables demand local `tables_dir` mounts.\n3. Expect long runtimes\u2014snapshot progress files.\n4. When cracking succeeds, escalate to credential rotation\u2014not operational reuse.\n\nCipherStrike mechanics: send a JSON POST from the Execute modal\u2014the agent drops empty optional scalars before invoking NyxStrike.\nPrefer explicit catalogue fields (`target`, `url`, `wordlist`, etc.) before `additional_args`; extra tokens must still match the packaged wrapper behaviour and your engagement rules.\nPer-field catalogue hints (first sentence of modal help):\n- `hash_file` (required): Line-delimited hashes for John/hashcat/ophcrack.\n- `tables_dir` (optional): Ophcrack rainbow-table base directory.\n- `tables` (optional): Named rainbow tables passed to ophcrack.\n- `additional_args` (optional): Shell tokens appended after mapped CLI arguments."
  },
  "outguess": {
    "long_description": "Outguess classic JPEG redundancy-channel steganography embed/extractor with iterative statistical refinements.\n\nForensic tooling streams carved files, timelines, entropy maps, PCAP decodes, volatility tables, SQLite extractions.\n\nForensic artefacts may contain HIPAA/PII regulated content\u2014encrypt exports immediately.\n\nResource-heavy jobs need disk-bound IO quotas; carve only targeted ranges when multi-terabyte images are mounted.\n\nCipherStrike transmits the JSON envelope to `/api/tools/outguess`; NyxStrike performs final validation, then shells out or calls the bundled integration advertised for this catalogue id.",
    "safety": "Operate only inside authorized penetration tests or sanctioned lab networks. Respect stop conditions, lawful intercept rules, and internal change windows. Sensitive parameters are captured in CipherStrike tenant execution logs alongside NyxStrike response snippets \u2014 avoid pasting production secrets when alternatives exist. Evidence images may contain regulated data\u2014encrypt at rest immediately after extraction.",
    "usage": "CipherStrike `outguess` executes against `/api/tools/outguess` via POST JSON envelopes assembled in the Execute modal.\n\n1. Provide cover/embed files passphrase actions keyed to steghide/stegsolve/zsteg/outguess flows.\n2. Outputs may be ambiguous\u2014combine with entropy analysis elsewhere.\n3. Carriers might be copyrighted\u2014handle distribution carefully.\n4. Document attempted transforms for regulators.\n\nCipherStrike mechanics: send a JSON POST from the Execute modal\u2014the agent drops empty optional scalars before invoking NyxStrike.\nPrefer explicit catalogue fields (`target`, `url`, `wordlist`, etc.) before `additional_args`; extra tokens must still match the packaged wrapper behaviour and your engagement rules.\nPer-field catalogue hints (first sentence of modal help):\n- `file_path` (required): Readable path for xxd/exiftool style utilities.\n- `action` (optional): `airmon-ng` verb (`start`/`stop`) or CloudMapper subcommand name.\n- `passphrase` (optional): WPA PSK or Steghide passphrase for crypto operations.\n- `output_file` (optional): Explicit output path for logs/PCAPNG/JSON when CLIs support `-o`.\n- `additional_args` (optional): Shell tokens appended after mapped CLI arguments."
  },
  "pacu": {
    "long_description": "Pacu is RhinoSecurity\u2019s modular AWS exploitation post-exploitation framework tracking keys, IAM abuse, snapshots, buckets, persistence, and enumerated service metadata under named sessions.\n\nCloud posture connectors emit JSON/HTML/SARIF findings enumerating IAM sprawl, open buckets, CIS deviations, stale keys, and attack-path narratives.\n\nCloud scans require least-privilege API roles scoped to enumerated accounts\u2014not root keys embedded in plaintext JSON long term.\n\nProvider APIs log every call; minimise repeated broad scans and purge credentials promptly after tabletop windows.\n\nCipherStrike transmits the JSON envelope to `/api/tools/pacu`; NyxStrike performs final validation, then shells out or calls the bundled integration advertised for this catalogue id.",
    "safety": "Operate only inside authorized penetration tests or sanctioned lab networks. Respect stop conditions, lawful intercept rules, and internal change windows. Sensitive parameters are captured in CipherStrike tenant execution logs alongside NyxStrike response snippets \u2014 avoid pasting production secrets when alternatives exist. Cloud API tokens authenticate broad actions\u2014purge keys after tabletop exercises.",
    "usage": "CipherStrike `pacu` executes against `/api/tools/pacu` via POST JSON envelopes assembled in the Execute modal.\n\n1. Populate cloud profile/session fields provider flags services directories per scout/pacu/cloudmapper wrappers.\n2. Expect large IAM graphs\u2014narrow services first.\n3. Export HTML/JSON responsibly; URLs may contain account identifiers.\n4. Rotate cloud keys after engagements.\n\nCipherStrike mechanics: send a JSON POST from the Execute modal\u2014the agent drops empty optional scalars before invoking NyxStrike.\nPrefer explicit catalogue fields (`target`, `url`, `wordlist`, etc.) before `additional_args`; extra tokens must still match the packaged wrapper behaviour and your engagement rules.\nPer-field catalogue hints (first sentence of modal help):\n- `session_name` (required): Pacu workspace label isolating Dynamo-style AWS attack state on the agent.\n- `modules` (optional): Recon-ng module chain, Pacu module list, or related automation selector.\n- `data_services` (optional): The workspace forwards `data_services` in the CipherStrike POST body unchanged after stripping empty optional fields.\n- `regions` (optional): The workspace forwards `regions` in the CipherStrike POST body unchanged after stripping empty optional fields.\n- `additional_args` (optional): Shell tokens appended after mapped CLI arguments."
  },
  "paramspider": {
    "long_description": "ParamSpider correlates archival sources to enumerate parameters per domain respecting depth/recursion/exclusion lists.\n\nOutput usually enumerates reachable paths, crawler graphs, fingerprints, robots hints, deduplicated URLs, or header snapshots.\n\nCombine directory bruteforce, spidering, and header probes sequentially: surface mapping before attempting injection payloads.\n\nMisconfigured crawlers crawl out of scope rapidly\u2014align seed URLs, blacklist regexes, and depth caps with authorised programs.\n\nCipherStrike transmits the JSON envelope to `/api/tools/paramspider`; NyxStrike performs final validation, then shells out or calls the bundled integration advertised for this catalogue id.",
    "safety": "Operate only inside authorized penetration tests or sanctioned lab networks. Respect stop conditions, lawful intercept rules, and internal change windows. Sensitive parameters are captured in CipherStrike tenant execution logs alongside NyxStrike response snippets \u2014 avoid pasting production secrets when alternatives exist.",
    "usage": "CipherStrike `paramspider` executes against `/api/tools/paramspider` via POST JSON envelopes assembled in the Execute modal.\n\n1. Tie `domains` exclusions depth flags to archival mining expectations.\n2. Validate parameters still exist live before exploitation.\n3. Respect archive licensing.\n4. Deduplicate with `uro` pipelines.\n\nCipherStrike mechanics: send a JSON POST from the Execute modal\u2014the agent drops empty optional scalars before invoking NyxStrike.\nPrefer explicit catalogue fields (`target`, `url`, `wordlist`, etc.) before `additional_args`; extra tokens must still match the packaged wrapper behaviour and your engagement rules.\nPer-field catalogue hints (first sentence of modal help):\n- `domain` (required): Authorised DNS apex fed to Amass/subfinder/GAU style OSINT connectors.\n- `level` (optional): Commix injection depth, ophcrack gadget level, or similar integer tier.\n- `exclude` (optional): The workspace forwards `exclude` in the CipherStrike POST body unchanged after stripping empty optional fields.\n- `output` (optional): Primary output sink some CLIs require (`-o`, `--output`).\n- `additional_args` (optional): Shell tokens appended after mapped CLI arguments."
  },
  "parsero": {
    "long_description": "Parsero parses `robots.txt` directives to resurrect hidden paths that operators attempted to withhold from benign crawlers.\n\nResults are textual lists, JSON artefacts, archive URLs, or passive reconnaissance breadcrumbs suitable for ingestion into trackers.\n\nOSINT tooling should precede intrusive scanning wherever policy demands minimal touch; escalate to DNS brute or crawler stages only after validating scope wording.\n\nPassive APIs throttle aggressively and may log lookups under shared keys\u2014credential handling matters.\n\nCipherStrike transmits the JSON envelope to `/api/tools/osint/parsero`; NyxStrike performs final validation, then shells out or calls the bundled integration advertised for this catalogue id.",
    "safety": "Operate only inside authorized penetration tests or sanctioned lab networks. Respect stop conditions, lawful intercept rules, and internal change windows. Sensitive parameters are captured in CipherStrike tenant execution logs alongside NyxStrike response snippets \u2014 avoid pasting production secrets when alternatives exist.",
    "usage": "CipherStrike `parsero` executes against `/api/tools/osint/parsero` via POST JSON envelopes assembled in the Execute modal.\n\n1. Provide the base `url`; Parsero retrieves robots entries only.\n2. Feed discovered paths into authorised directory fuzzers\u2014not into destructive testers without review.\n3. Some sites purposely list noisy paths\u2014validate manually.\n4. Combine outputs with archival sources (`gau`).\n\nCipherStrike mechanics: send a JSON POST from the Execute modal\u2014the agent drops empty optional scalars before invoking NyxStrike.\nPrefer explicit catalogue fields (`target`, `url`, `wordlist`, etc.) before `additional_args`; extra tokens must still match the packaged wrapper behaviour and your engagement rules.\nPer-field catalogue hints (first sentence of modal help):\n- `target` (required): Primary IPv4/IPv6/hostname/CIDR or bulk newline list routed into network scanners and RPC clients.\n- `additional_args` (optional): Shell tokens appended after mapped CLI arguments."
  },
  "patator": {
    "long_description": "Modular login brute-force frameworks\u2014Medusa stresses parallel transports while Patator unifies dictionaries across SMB, SSH, HTTP, etc., with Pythonic verbosity controls.\n\nPatator emits Pythonic verbosity with modular transports covering SMB HTTP databases.\n\nReach for Patator when hydra-medusa lacks required modules\u2014not for stealth.\n\nVerbose modules leak secrets into logs.\n\nCipherStrike transmits the JSON envelope to `/api/tools/patator`; NyxStrike performs final validation, then shells out or calls the bundled integration advertised for this catalogue id.",
    "safety": "Operate only inside authorized penetration tests or sanctioned lab networks. Respect stop conditions, lawful intercept rules, and internal change windows. Sensitive parameters are captured in CipherStrike tenant execution logs alongside NyxStrike response snippets \u2014 avoid pasting production secrets when alternatives exist. Credential guesses can trigger lockouts; coordinate with SOC and honour throttling mandates. Verbose logging may record cracked secrets\u2014restrict log destinations.",
    "usage": "CipherStrike `patator` executes against `/api/tools/patator` via POST JSON envelopes assembled in the Execute modal.\n\n1. Provide `target`, `username`/`username_file`, `password_file`, and protocol/service selectors.\n2. Apply `threads`, `delay`, and attempt caps mandated by SOC.\n3. Stop on first success unless horizontal movement is chartered\u2014credential stuffing may violate policy.\n4. Archive cracked creds securely; rotate them after testing.\n\nCipherStrike mechanics: send a JSON POST from the Execute modal\u2014the agent drops empty optional scalars before invoking NyxStrike.\nPrefer explicit catalogue fields (`target`, `url`, `wordlist`, etc.) before `additional_args`; extra tokens must still match the packaged wrapper behaviour and your engagement rules.\nPer-field catalogue hints (first sentence of modal help):\n- `target` (required): Primary IPv4/IPv6/hostname/CIDR or bulk newline list routed into network scanners and RPC clients.\n- `module` (required): Metasploit module path, Patator dialect, or NetExec plugin name.\n- `username` (optional): Username for SMB/LDAP/WinRM clients and login brute modules.\n- `username_file` (optional): Username list for credential spraying modules.\n- `password` (optional): Cleartext password for authenticated scans\u2014rotate if leaked into logs.\n- `password_file` (optional): Dictionary file for Hydra/Medusa/Patator.\n- `additional_args` (optional): Shell tokens appended after mapped CLI arguments."
  },
  "photorec": {
    "long_description": "PhotoRec file carver rebuilds artefacts ignoring filesystem metadata\u2014paired with selectable file families.\n\nForensic tooling streams carved files, timelines, entropy maps, PCAP decodes, volatility tables, SQLite extractions.\n\nForensic artefacts may contain HIPAA/PII regulated content\u2014encrypt exports immediately.\n\nResource-heavy jobs need disk-bound IO quotas; carve only targeted ranges when multi-terabyte images are mounted.\n\nCipherStrike transmits the JSON envelope to `/api/tools/photorec`; NyxStrike performs final validation, then shells out or calls the bundled integration advertised for this catalogue id.",
    "safety": "Operate only inside authorized penetration tests or sanctioned lab networks. Respect stop conditions, lawful intercept rules, and internal change windows. Sensitive parameters are captured in CipherStrike tenant execution logs alongside NyxStrike response snippets \u2014 avoid pasting production secrets when alternatives exist. Evidence images may contain regulated data\u2014encrypt at rest immediately after extraction.",
    "usage": "CipherStrike `photorec` executes against `/api/tools/photorec` via POST JSON envelopes assembled in the Execute modal.\n\n1. Point `input_file`/`image_path`/`output_dir` fields at sanctioned evidence stores.\n2. Choose carving signatures conservatively\u2014broad signatures balloon storage.\n3. Keep chain-of-custody notes for every exported file.\n4. Some carvers overwrite slack space\u2014snapshot images first.\n\nCipherStrike mechanics: send a JSON POST from the Execute modal\u2014the agent drops empty optional scalars before invoking NyxStrike.\nPrefer explicit catalogue fields (`target`, `url`, `wordlist`, etc.) before `additional_args`; extra tokens must still match the packaged wrapper behaviour and your engagement rules.\nPer-field catalogue hints (first sentence of modal help):\n- `input_file` (required): Evidence blob for foremost/scalpel/testdisk carvers.\n- `output_dir` (optional): Directory root for AutoRecon, foremost, Scout exports.\n- `additional_args` (optional): Shell tokens appended after mapped CLI arguments."
  },
  "preview-attack-chain": {
    "long_description": "Planner service that derives ordered attack hypotheses from analysed signals; preview mode avoids persistence while creation mode persists structured chains for assisted operations.\n\nIntelligence routers combine analysis, chaining, profiling, orchestration manifests, AI-selected toolplans, and explanatory narratives about why certain jobs fire.\n\nSmart planners trade determinism for speed\u2014validate proposed fan-outs before unattended execution on sensitive ranges.\n\nAutomated chains still require human scope checks before touching regulated data classifications.\n\nCipherStrike transmits the JSON envelope to `/api/intelligence/preview-attack-chain`; NyxStrike performs final validation, then shells out or calls the bundled integration advertised for this catalogue id.",
    "safety": "Operate only inside authorized penetration tests or sanctioned lab networks. Respect stop conditions, lawful intercept rules, and internal change windows. Sensitive parameters are captured in CipherStrike tenant execution logs alongside NyxStrike response snippets \u2014 avoid pasting production secrets when alternatives exist. Automated chaining still needs data-handling approvals for anything beyond public banners.",
    "usage": "CipherStrike `preview-attack-chain` executes against `/api/intelligence/preview-attack-chain` via POST JSON envelopes assembled in the Execute modal.\n\n1. Confirm the written scope authorises automated intelligence or orchestration against the supplied target.\n2. Populate `target` with the exact hostname, URL, or asset identifier your engagement letter lists\u2014no implicit wildcards.\n3. For `smart-scan`, tune `objective` and `max_tools` so the planner cannot fan out beyond your risk appetite; review the proposed fan-out in session logs before accepting unattended runs.\n4. `preview-attack-chain` avoids persistence\u2014use it first, then run `create-attack-chain` only when stored hypotheses are required.\n5. Interpret AI output as advisory: validate every recommended action with deterministic tooling and customer policies.\n`additional_args` is rarely used on these routes; prefer structured fields.\n\nCipherStrike mechanics: send a JSON POST from the Execute modal\u2014the agent drops empty optional scalars before invoking NyxStrike.\nPrefer explicit catalogue fields (`target`, `url`, `wordlist`, etc.) before `additional_args`; extra tokens must still match the packaged wrapper behaviour and your engagement rules.\nPer-field catalogue hints (first sentence of modal help):\n- `target` (required): Primary IPv4/IPv6/hostname/CIDR or bulk newline list routed into network scanners and RPC clients.\n- `objective` (optional): Planner hint for smart-scan/attack-chain heuristics (`comprehensive`, `stealth`)."
  },
  "prowler": {
    "long_description": "Prowler automates hundreds of AWS/Azure/GCP checks across CIS benchmarks, incident response playbooks, custom profiles, and compliance reporting exports.\n\nCloud posture connectors emit JSON/HTML/SARIF findings enumerating IAM sprawl, open buckets, CIS deviations, stale keys, and attack-path narratives.\n\nCloud scans require least-privilege API roles scoped to enumerated accounts\u2014not root keys embedded in plaintext JSON long term.\n\nProvider APIs log every call; minimise repeated broad scans and purge credentials promptly after tabletop windows.\n\nCipherStrike transmits the JSON envelope to `/api/tools/prowler`; NyxStrike performs final validation, then shells out or calls the bundled integration advertised for this catalogue id.",
    "safety": "Operate only inside authorized penetration tests or sanctioned lab networks. Respect stop conditions, lawful intercept rules, and internal change windows. Sensitive parameters are captured in CipherStrike tenant execution logs alongside NyxStrike response snippets \u2014 avoid pasting production secrets when alternatives exist. Cloud API tokens authenticate broad actions\u2014purge keys after tabletop exercises.",
    "usage": "CipherStrike `prowler` executes against `/api/tools/prowler` via POST JSON envelopes assembled in the Execute modal.\n\n1. Populate cloud profile/session fields provider flags services directories per scout/pacu/cloudmapper wrappers.\n2. Expect large IAM graphs\u2014narrow services first.\n3. Export HTML/JSON responsibly; URLs may contain account identifiers.\n4. Rotate cloud keys after engagements.\n\nCipherStrike mechanics: send a JSON POST from the Execute modal\u2014the agent drops empty optional scalars before invoking NyxStrike.\nPrefer explicit catalogue fields (`target`, `url`, `wordlist`, etc.) before `additional_args`; extra tokens must still match the packaged wrapper behaviour and your engagement rules.\nPer-field catalogue hints (first sentence of modal help):\n- `provider` (optional): Cloud vendor (`aws`, `azure`, `gcp`, `oci`).\n- `profile` (optional): Volatility KDBG profile **or** named cloud CLI profile for Prowler\u2014check tool context.\n- `region` (optional): Cloud region focus for audit connectors.\n- `checks` (optional): CSV benchmark control ids (docker-bench), Prowler check ids, **or** Schemathesis check names\u2014depends on route.\n- `additional_args` (optional): Shell tokens appended after mapped CLI arguments."
  },
  "pwninit": {
    "long_description": "pwninit patches CTF binaries with supplied libc/ld pairs and emits starter exploit templates reducing manual linking drudgery.\n\nDebuggers/disassemblers/gadget hunters produce listings, scripted traces, annotated graphs, emulation logs, or CTF scaffolding.\n\nStatic analysis excels on offline artefacts; symbolic tooling demands compute and may path-explode on large binaries.\n\nMalware-grade samples may trigger host controls\u2014sandbox accordingly and sanitise artefacts before sharing externally.\n\nCipherStrike transmits the JSON envelope to `/api/tools/pwninit`; NyxStrike performs final validation, then shells out or calls the bundled integration advertised for this catalogue id.",
    "safety": "Operate only inside authorized penetration tests or sanctioned lab networks. Respect stop conditions, lawful intercept rules, and internal change windows. Sensitive parameters are captured in CipherStrike tenant execution logs alongside NyxStrike response snippets \u2014 avoid pasting production secrets when alternatives exist. Binary tooling may invoke malware safeguards\u2014sandbox untrusted specimens.",
    "usage": "CipherStrike `pwninit` executes against `/api/tools/pwninit` via POST JSON envelopes assembled in the Execute modal.\n\n1. Embed pwntools/angr scripting via `script_content`; pair with `target_host`/`target_port` or local `target_binary`.\n2. `pwninit` requires libc/ld pairings\u2014supply accurate versions.\n3. Never point scaffolding at production\u2014use CTF nets or clones.\n4. Record exploit randomness seeds for deterministic replay.\n\nCipherStrike mechanics: send a JSON POST from the Execute modal\u2014the agent drops empty optional scalars before invoking NyxStrike.\nPrefer explicit catalogue fields (`target`, `url`, `wordlist`, etc.) before `additional_args`; extra tokens must still match the packaged wrapper behaviour and your engagement rules.\nPer-field catalogue hints (first sentence of modal help):\n- `binary` (required): Executable for radare2/ghidra/objdump/ropper workflows.\n- `libc` (optional): Optional libc.so path for pwninit bundling.\n- `ld` (optional): Dynamic linker path accompanying pwninit bundles.\n- `template_type` (optional): Exploit scaffold format (`python`, `c`).\n- `additional_args` (optional): Shell tokens appended after mapped CLI arguments."
  },
  "pwntools": {
    "long_description": "pwntools supplies tube abstractions, ROP builders, ELF parsing, and exploit scaffolding executed from `script_content` against local/remote targets.\n\nDebuggers/disassemblers/gadget hunters produce listings, scripted traces, annotated graphs, emulation logs, or CTF scaffolding.\n\nStatic analysis excels on offline artefacts; symbolic tooling demands compute and may path-explode on large binaries.\n\nMalware-grade samples may trigger host controls\u2014sandbox accordingly and sanitise artefacts before sharing externally.\n\nCipherStrike transmits the JSON envelope to `/api/tools/pwntools`; NyxStrike performs final validation, then shells out or calls the bundled integration advertised for this catalogue id.",
    "safety": "Operate only inside authorized penetration tests or sanctioned lab networks. Respect stop conditions, lawful intercept rules, and internal change windows. Sensitive parameters are captured in CipherStrike tenant execution logs alongside NyxStrike response snippets \u2014 avoid pasting production secrets when alternatives exist. Binary tooling may invoke malware safeguards\u2014sandbox untrusted specimens.",
    "usage": "CipherStrike `pwntools` executes against `/api/tools/pwntools` via POST JSON envelopes assembled in the Execute modal.\n\n1. Embed pwntools/angr scripting via `script_content`; pair with `target_host`/`target_port` or local `target_binary`.\n2. `pwninit` requires libc/ld pairings\u2014supply accurate versions.\n3. Never point scaffolding at production\u2014use CTF nets or clones.\n4. Record exploit randomness seeds for deterministic replay.\n\nCipherStrike mechanics: send a JSON POST from the Execute modal\u2014the agent drops empty optional scalars before invoking NyxStrike.\nPrefer explicit catalogue fields (`target`, `url`, `wordlist`, etc.) before `additional_args`; extra tokens must still match the packaged wrapper behaviour and your engagement rules.\nPer-field catalogue hints (first sentence of modal help):\n- `script_content` (required): Inline pwntools/angr code executed before harness launch.\n- `target_binary` (optional): Local binary executed by pwntools when `exploit_type` is local.\n- `target_host` (optional): Remote host for pwntools or callback harnesses.\n- `target_port` (optional): Remote TCP port paired with `target_host` for exploit callbacks.\n- `exploit_type` (optional): pwntools harness mode (`local` vs `remote`).\n- `additional_args` (optional): Shell tokens appended after mapped CLI arguments."
  },
  "qsreplace": {
    "long_description": "qsreplace rewrites URL query fragments with placeholder tokens prepping ffuf/ffuf-compatible pipelines (often `replacement=FUZZ`).\n\nAPI tooling highlights schema quirks, undocumented parameters, malformed JWT behaviours, fuzz deltas, and property-test regressions referencing OpenAPI/GraphQL artefacts.\n\nSchema-first testers complement browser-only assessments by exercising machine contracts that never render in GUIs.\n\nGenerated traffic can cascade into billing or email workflows\u2014narrow operationIds and verbs before unattended fuzzing.\n\nCipherStrike transmits the JSON envelope to `/api/tools/qsreplace`; NyxStrike performs final validation, then shells out or calls the bundled integration advertised for this catalogue id.",
    "safety": "Operate only inside authorized penetration tests or sanctioned lab networks. Respect stop conditions, lawful intercept rules, and internal change windows. Sensitive parameters are captured in CipherStrike tenant execution logs alongside NyxStrike response snippets \u2014 avoid pasting production secrets when alternatives exist. API fuzzing can trigger costly side-effects (email, billing, writes)\u2014narrow scope before unattended runs.",
    "usage": "CipherStrike `qsreplace` executes against `/api/tools/qsreplace` via POST JSON envelopes assembled in the Execute modal.\n\n1. Pipe newline URLs via `stdin` equivalent fields documented for qsreplace/anew/uro.\n2. Mark replacement tokens distinctly when prepping ffuf placeholders.\n3. Use `uro` to dedupe monstrous crawler outputs feeding nuclei templates.\n4. Document pipeline ordering for reproducibility.\n\nCipherStrike mechanics: send a JSON POST from the Execute modal\u2014the agent drops empty optional scalars before invoking NyxStrike.\nPrefer explicit catalogue fields (`target`, `url`, `wordlist`, etc.) before `additional_args`; extra tokens must still match the packaged wrapper behaviour and your engagement rules.\nPer-field catalogue hints (first sentence of modal help):\n- `urls` (required): URL batch for qsreplace, uro, or anew dedup pipelines.\n- `replacement` (optional): qsreplace token replacing query values (often `FUZZ`).\n- `additional_args` (optional): Shell tokens appended after mapped CLI arguments."
  },
  "radare2": {
    "long_description": "radare2 is the scriptable reversing framework supporting multi-arch disassembly/visual modes with batch `-c` command strings.\n\nDebuggers/disassemblers/gadget hunters produce listings, scripted traces, annotated graphs, emulation logs, or CTF scaffolding.\n\nStatic analysis excels on offline artefacts; symbolic tooling demands compute and may path-explode on large binaries.\n\nMalware-grade samples may trigger host controls\u2014sandbox accordingly and sanitise artefacts before sharing externally.\n\nCipherStrike transmits the JSON envelope to `/api/tools/radare2`; NyxStrike performs final validation, then shells out or calls the bundled integration advertised for this catalogue id.",
    "safety": "Operate only inside authorized penetration tests or sanctioned lab networks. Respect stop conditions, lawful intercept rules, and internal change windows. Sensitive parameters are captured in CipherStrike tenant execution logs alongside NyxStrike response snippets \u2014 avoid pasting production secrets when alternatives exist. Binary tooling may invoke malware safeguards\u2014sandbox untrusted specimens.",
    "usage": "CipherStrike `radare2` executes against `/api/tools/radare2` via POST JSON envelopes assembled in the Execute modal.\n\n1. Pass file paths macros scripts analysis toggles aligning with gdb/radare2/ghidra headless wrappers.\n2. Long analyses need `timeout` fields\u2014coordinate with infra.\n3. Store project directories on encrypted volumes.\n4. Symbolic backends may spin CPU\u2014quota jobs fairly.\n\nCipherStrike mechanics: send a JSON POST from the Execute modal\u2014the agent drops empty optional scalars before invoking NyxStrike.\nPrefer explicit catalogue fields (`target`, `url`, `wordlist`, etc.) before `additional_args`; extra tokens must still match the packaged wrapper behaviour and your engagement rules.\nPer-field catalogue hints (first sentence of modal help):\n- `file` (required): Binary/firmware specimen for binwalk/strings/checksec.\n- `commands` (optional): Macro batch (`rpcclient` or `r2 -c`) replayed verbatim.\n- `additional_args` (optional): Shell tokens appended after mapped CLI arguments."
  },
  "recon-ng": {
    "long_description": "Recon-ng is a modular CLI recon framework injecting marketplace modules seeded by authoritative domains (`modules`).\n\nResults are textual lists, JSON artefacts, archive URLs, or passive reconnaissance breadcrumbs suitable for ingestion into trackers.\n\nOSINT tooling should precede intrusive scanning wherever policy demands minimal touch; escalate to DNS brute or crawler stages only after validating scope wording.\n\nPassive APIs throttle aggressively and may log lookups under shared keys\u2014credential handling matters.\n\nCipherStrike transmits the JSON envelope to `/api/tools/recon_ng`; NyxStrike performs final validation, then shells out or calls the bundled integration advertised for this catalogue id.",
    "safety": "Operate only inside authorized penetration tests or sanctioned lab networks. Respect stop conditions, lawful intercept rules, and internal change windows. Sensitive parameters are captured in CipherStrike tenant execution logs alongside NyxStrike response snippets \u2014 avoid pasting production secrets when alternatives exist.",
    "usage": "CipherStrike `recon-ng` executes against `/api/tools/recon_ng` via POST JSON envelopes assembled in the Execute modal.\n\n1. Load workspace modules transforms API keys complying with Spiderfoot/Recon-ng/Maltego licensing.\n2. Longitudinal jobs (`poll_interval`) may take hours\u2014checkpoint state.\n3. Graph exports can leak third-party identifiers handle per GDPR charters.\n4. Disable modules that scrape disallowed jurisdictions.\n\nCipherStrike mechanics: send a JSON POST from the Execute modal\u2014the agent drops empty optional scalars before invoking NyxStrike.\nPrefer explicit catalogue fields (`target`, `url`, `wordlist`, etc.) before `additional_args`; extra tokens must still match the packaged wrapper behaviour and your engagement rules.\nPer-field catalogue hints (first sentence of modal help):\n- `domain` (required): Authorised DNS apex fed to Amass/subfinder/GAU style OSINT connectors.\n- `modules` (optional): Recon-ng module chain, Pacu module list, or related automation selector.\n- `additional_args` (optional): Shell tokens appended after mapped CLI arguments."
  },
  "responder": {
    "long_description": "Responder poisons broadcast name-resolution protocols harvesting challenge responses,optionally spoofing WPAD, performing traffic analysis loops, configurable durations\u2014keep off production VLANs.\n\nResponder emits captured hashes poisoning logs responder modules duration bounded captures.\n\nResponder complements Impacket dumping after LLMNR/NBT-NS poisoning succeeds.\n\nBroadcast poisoning harms VOIP fleets isolate VLANs.\n\nCipherStrike transmits the JSON envelope to `/api/tools/responder`; NyxStrike performs final validation, then shells out or calls the bundled integration advertised for this catalogue id.",
    "safety": "Operate only inside authorized penetration tests or sanctioned lab networks. Respect stop conditions, lawful intercept rules, and internal change windows. Sensitive parameters are captured in CipherStrike tenant execution logs alongside NyxStrike response snippets \u2014 avoid pasting production secrets when alternatives exist. Port scanners and handshake grabbers alert defenders\u2014mirror scope IPs exactly as authorised. Broadcast poisoning affects every host on the segment\u2014never cross into production VLANs without isolation.",
    "usage": "CipherStrike `responder` executes against `/api/tools/responder` via POST JSON envelopes assembled in the Execute modal.\n\n1. Specify `interface` poison duration analyse toggles impersonation safeguards.\n2. Keep responder on segmented VLAN taps\u2014broadcast domains amplify damage.\n3. Capture hashes securely; cracking belongs to chartered phases.\n4. Stop runs immediately if unintended clients attach.\n\nCipherStrike mechanics: send a JSON POST from the Execute modal\u2014the agent drops empty optional scalars before invoking NyxStrike.\nPrefer explicit catalogue fields (`target`, `url`, `wordlist`, etc.) before `additional_args`; extra tokens must still match the packaged wrapper behaviour and your engagement rules.\nPer-field catalogue hints (first sentence of modal help):\n- `interface` (required): Network interface (`wlan0mon`, `eth0`) for Wi\u2011Fi or Responder jobs.\n- `analyze` (optional): Responder analyse-only posture versus active spoofing loops.\n- `wpad` (optional): Responder answers WPAD\u2014illegal outside sanctioned RF enclosures.\n- `force_wpad_auth` (optional): Force captive WPAD auth flows\u2014only in isolated RF labs.\n- `fingerprint` (optional): Responder OS/service fingerprinting during poisoning jobs.\n- `duration` (optional): Runtime cap for Responder poison loops or Falco capture windows.\n- `additional_args` (optional): Shell tokens appended after mapped CLI arguments."
  },
  "ropgadget": {
    "long_description": "ROPgadget enumerates syscall/gadget inventories with filters for arches and instruction constraints.\n\nDebuggers/disassemblers/gadget hunters produce listings, scripted traces, annotated graphs, emulation logs, or CTF scaffolding.\n\nStatic analysis excels on offline artefacts; symbolic tooling demands compute and may path-explode on large binaries.\n\nMalware-grade samples may trigger host controls\u2014sandbox accordingly and sanitise artefacts before sharing externally.\n\nCipherStrike transmits the JSON envelope to `/api/tools/ropgadget`; NyxStrike performs final validation, then shells out or calls the bundled integration advertised for this catalogue id.",
    "safety": "Operate only inside authorized penetration tests or sanctioned lab networks. Respect stop conditions, lawful intercept rules, and internal change windows. Sensitive parameters are captured in CipherStrike tenant execution logs alongside NyxStrike response snippets \u2014 avoid pasting production secrets when alternatives exist. Binary tooling may invoke malware safeguards\u2014sandbox untrusted specimens.",
    "usage": "CipherStrike `ropgadget` executes against `/api/tools/ropgadget` via POST JSON envelopes assembled in the Execute modal.\n\n1. Provide `binary`, `libc_path`, architectures, libc IDs, gadgets filters per tool.\n2. Validate gadget addresses on target libc builds\u2014offsets shift with patches.\n3. Document gadget chains reviewed by peers.\n4. Keep exploit artifacts confidential.\n\nCipherStrike mechanics: send a JSON POST from the Execute modal\u2014the agent drops empty optional scalars before invoking NyxStrike.\nPrefer explicit catalogue fields (`target`, `url`, `wordlist`, etc.) before `additional_args`; extra tokens must still match the packaged wrapper behaviour and your engagement rules.\nPer-field catalogue hints (first sentence of modal help):\n- `file` (required): Binary/firmware specimen for binwalk/strings/checksec.\n- `additional_args` (optional): Shell tokens appended after mapped CLI arguments."
  },
  "ropper": {
    "long_description": "Ropper enumerates ROP/JOP gadgets with quality scoring, arch awareness, and search filters.\n\nDebuggers/disassemblers/gadget hunters produce listings, scripted traces, annotated graphs, emulation logs, or CTF scaffolding.\n\nStatic analysis excels on offline artefacts; symbolic tooling demands compute and may path-explode on large binaries.\n\nMalware-grade samples may trigger host controls\u2014sandbox accordingly and sanitise artefacts before sharing externally.\n\nCipherStrike transmits the JSON envelope to `/api/tools/ropper`; NyxStrike performs final validation, then shells out or calls the bundled integration advertised for this catalogue id.",
    "safety": "Operate only inside authorized penetration tests or sanctioned lab networks. Respect stop conditions, lawful intercept rules, and internal change windows. Sensitive parameters are captured in CipherStrike tenant execution logs alongside NyxStrike response snippets \u2014 avoid pasting production secrets when alternatives exist. Binary tooling may invoke malware safeguards\u2014sandbox untrusted specimens.",
    "usage": "CipherStrike `ropper` executes against `/api/tools/ropper` via POST JSON envelopes assembled in the Execute modal.\n\n1. Provide `binary`, `libc_path`, architectures, libc IDs, gadgets filters per tool.\n2. Validate gadget addresses on target libc builds\u2014offsets shift with patches.\n3. Document gadget chains reviewed by peers.\n4. Keep exploit artifacts confidential.\n\nCipherStrike mechanics: send a JSON POST from the Execute modal\u2014the agent drops empty optional scalars before invoking NyxStrike.\nPrefer explicit catalogue fields (`target`, `url`, `wordlist`, etc.) before `additional_args`; extra tokens must still match the packaged wrapper behaviour and your engagement rules.\nPer-field catalogue hints (first sentence of modal help):\n- `binary` (required): Executable for radare2/ghidra/objdump/ropper workflows.\n- `gadget_type` (optional): Ropper category (`rop`, `jop`, `sys`).\n- `quality` (optional): Ropper minimum gadget quality rank.\n- `arch` (optional): ISA hint (`x86`, `amd64`, `aarch64`) for gadget tools.\n- `search_string` (optional): Mnemonic substring filter for gadget dumps.\n- `additional_args` (optional): Shell tokens appended after mapped CLI arguments."
  },
  "rpcclient": {
    "long_description": "rpcclient (Samba) issues MS-RPC calls (`enumdomusers`, `querydominfo`) using inline command macros.\n\nExpect port listings, traceroute-like hints, service banners, NIC inventories, handshake captures, or ARP inventories depending on tooling.\n\nFast SYN scanners suit huge ranges; balanced scanners add scripts and versioning; SMB-oriented tools deepen Windows visibility\u2014choose based on scope scale and covertness mandates.\n\nHigh packet rates overwhelm small networks\u2014rate-limit, subdivide subnets, and obtain written approval before touching neighbouring tenants.\n\nCipherStrike transmits the JSON envelope to `/api/tools/rpcclient`; NyxStrike performs final validation, then shells out or calls the bundled integration advertised for this catalogue id.",
    "safety": "Operate only inside authorized penetration tests or sanctioned lab networks. Respect stop conditions, lawful intercept rules, and internal change windows. Sensitive parameters are captured in CipherStrike tenant execution logs alongside NyxStrike response snippets \u2014 avoid pasting production secrets when alternatives exist. Port scanners and handshake grabbers alert defenders\u2014mirror scope IPs exactly as authorised.",
    "usage": "CipherStrike `rpcclient` executes against `/api/tools/rpcclient` via POST JSON envelopes assembled in the Execute modal.\n\n1. Provide `target`, authentication material (password, `hashes`, AES keys, `dc_ip`, Kerberos booleans) before pasting `commands` macros recognised by `rpcclient`.\n2. Each macro (`enumdomusers`, `querydominfo`, etc.) should correspond to an operator-approved action; avoid destructive RPC unless explicitly chartered.\n3. Expect Samba client stderr alongside results\u2014Kerberos clock skew and SPN typos are the usual failure modes.\n4. Treat output as sensitive AD intelligence; redact before ticketing and pair with `ldapdomaindump` for coverage.\n\nCipherStrike mechanics: send a JSON POST from the Execute modal\u2014the agent drops empty optional scalars before invoking NyxStrike.\nPrefer explicit catalogue fields (`target`, `url`, `wordlist`, etc.) before `additional_args`; extra tokens must still match the packaged wrapper behaviour and your engagement rules.\nPer-field catalogue hints (first sentence of modal help):\n- `target` (required): Primary IPv4/IPv6/hostname/CIDR or bulk newline list routed into network scanners and RPC clients.\n- `username` (optional): Username for SMB/LDAP/WinRM clients and login brute modules.\n- `password` (optional): Cleartext password for authenticated scans\u2014rotate if leaked into logs.\n- `domain` (optional): Authorised DNS apex fed to Amass/subfinder/GAU style OSINT connectors.\n- `commands` (optional): Macro batch (`rpcclient` or `r2 -c`) replayed verbatim.\n- `additional_args` (optional): Shell tokens appended after mapped CLI arguments."
  },
  "rustscan": {
    "long_description": "Rustscan is an extremely fast preliminary port enumerator that optionally hands results to downstream Nmap for deeper fingerprinting (batch size/timeouts materially affect responsiveness on busy segments).\n\nRustscan bursts port lists then optionally forwards live hosts toward Nmap fingerprints.\n\nSelect Rustscan for developer-speed triage loops; pivot to scripted Nmap batches for nuanced service telemetry.\n\nBatch sizing and timeouts interact with kernel buffers\u2014mis-tuning loses responses on busy segments.\n\nCipherStrike transmits the JSON envelope to `/api/tools/rustscan`; NyxStrike performs final validation, then shells out or calls the bundled integration advertised for this catalogue id.",
    "safety": "Operate only inside authorized penetration tests or sanctioned lab networks. Respect stop conditions, lawful intercept rules, and internal change windows. Sensitive parameters are captured in CipherStrike tenant execution logs alongside NyxStrike response snippets \u2014 avoid pasting production secrets when alternatives exist. Port scanners and handshake grabbers alert defenders\u2014mirror scope IPs exactly as authorised. Rustscan\u2019s default aggression can overwhelm firewalls\u2014align batch sizes with network owners.",
    "usage": "CipherStrike `rustscan` executes against `/api/tools/rustscan` via POST JSON envelopes assembled in the Execute modal.\n\n1. Provide host lists or CIDRs via `target` and align `ports` with the coverage window you need.\n2. Tune batch size and timeout fields (per registry optional map) to match NIC/driver capabilities.\n3. When Rustscan hands off to Nmap, capture both outputs to preserve evidence of open services.\n4. Watch for false negatives on lossy Wi-Fi or VPN paths\u2014retest critical hosts calmly.\n\nCipherStrike mechanics: send a JSON POST from the Execute modal\u2014the agent drops empty optional scalars before invoking NyxStrike.\nPrefer explicit catalogue fields (`target`, `url`, `wordlist`, etc.) before `additional_args`; extra tokens must still match the packaged wrapper behaviour and your engagement rules.\nPer-field catalogue hints (first sentence of modal help):\n- `target` (required): Primary IPv4/IPv6/hostname/CIDR or bulk newline list routed into network scanners and RPC clients.\n- `additional_args` (optional): Shell tokens appended after mapped CLI arguments."
  },
  "scalpel": {
    "long_description": "Scalpel carves configurable file types guided by forensic header/footer definitions akin to foremost.\n\nForensic tooling streams carved files, timelines, entropy maps, PCAP decodes, volatility tables, SQLite extractions.\n\nForensic artefacts may contain HIPAA/PII regulated content\u2014encrypt exports immediately.\n\nResource-heavy jobs need disk-bound IO quotas; carve only targeted ranges when multi-terabyte images are mounted.\n\nCipherStrike transmits the JSON envelope to `/api/tools/scalpel`; NyxStrike performs final validation, then shells out or calls the bundled integration advertised for this catalogue id.",
    "safety": "Operate only inside authorized penetration tests or sanctioned lab networks. Respect stop conditions, lawful intercept rules, and internal change windows. Sensitive parameters are captured in CipherStrike tenant execution logs alongside NyxStrike response snippets \u2014 avoid pasting production secrets when alternatives exist. Evidence images may contain regulated data\u2014encrypt at rest immediately after extraction.",
    "usage": "CipherStrike `scalpel` executes against `/api/tools/scalpel` via POST JSON envelopes assembled in the Execute modal.\n\n1. Point `input_file`/`image_path`/`output_dir` fields at sanctioned evidence stores.\n2. Choose carving signatures conservatively\u2014broad signatures balloon storage.\n3. Keep chain-of-custody notes for every exported file.\n4. Some carvers overwrite slack space\u2014snapshot images first.\n\nCipherStrike mechanics: send a JSON POST from the Execute modal\u2014the agent drops empty optional scalars before invoking NyxStrike.\nPrefer explicit catalogue fields (`target`, `url`, `wordlist`, etc.) before `additional_args`; extra tokens must still match the packaged wrapper behaviour and your engagement rules.\nPer-field catalogue hints (first sentence of modal help):\n- `input_file` (required): Evidence blob for foremost/scalpel/testdisk carvers.\n- `output_dir` (optional): Directory root for AutoRecon, foremost, Scout exports.\n- `config` (optional): cloudmapper account metadata or secondary service configuration path.\n- `additional_args` (optional): Shell tokens appended after mapped CLI arguments."
  },
  "schemathesis": {
    "long_description": "Schemathesis derives property tests from OpenAPI/GraphQL definitions with phased execution, concurrency, rate limiting, timeouts, reporting `checks` aligning with Hypothesis integrations.\n\nSchemathesis merges Hypothesis-backed fuzzing with phased OpenAPI/GraphQL coverage and exportable artefacts (HTML/JUnit/SARIF).\n\nProperty-based runs outperform ad hoc curl suites for contract regressions though they consume more runtime and flaky services need tuning.\n\nWatch check toggles (`checks`) carefully\u2014automated validations can amplify noise on brittle endpoints.\n\nCipherStrike transmits the JSON envelope to `/api/tools/api_fuzz/schemathesis`; NyxStrike performs final validation, then shells out or calls the bundled integration advertised for this catalogue id.",
    "safety": "Operate only inside authorized penetration tests or sanctioned lab networks. Respect stop conditions, lawful intercept rules, and internal change windows. Sensitive parameters are captured in CipherStrike tenant execution logs alongside NyxStrike response snippets \u2014 avoid pasting production secrets when alternatives exist. API fuzzing can trigger costly side-effects (email, billing, writes)\u2014narrow scope before unattended runs. Property-based fuzzing can create resources or send mail\u2014use synthetic environments.",
    "usage": "CipherStrike `schemathesis` executes against `/api/tools/api_fuzz/schemathesis` via POST JSON envelopes assembled in the Execute modal.\n\n1. Configure schema URLs or uploaded OpenAPI/GraphQL descriptors plus phased execution (`phases`), workers, rate limiting, timeouts, `max_examples`, include/exclude `operation_id` guards, check lists, and report formats demanded by SOC pipelines.\n2. Start with narrower phases (`examples` only) against staging tenants before unleashing combined coverage + fuzz states.\n3. Inspect Hypothesis shrinking logs\u2014they pinpoint minimal failing sequences worthy of CVE-style write-ups.\n4. Export SARIF/HTML/JUnit artefacts into ticketing with severity annotations summarising reproducible payloads.\n\nCipherStrike mechanics: send a JSON POST from the Execute modal\u2014the agent drops empty optional scalars before invoking NyxStrike.\nPrefer explicit catalogue fields (`target`, `url`, `wordlist`, etc.) before `additional_args`; extra tokens must still match the packaged wrapper behaviour and your engagement rules.\nPer-field catalogue hints (first sentence of modal help):\n- `schema` (required): The workspace forwards `schema` in the CipherStrike POST body unchanged after stripping empty optional fields.\n- `base_url` (optional): API root such as `https://api.target` that relative fuzz paths expand against.\n- `checks` (optional): CSV benchmark control ids (docker-bench), Prowler check ids, **or** Schemathesis check names\u2014depends on route.\n- `workers` (optional): Schemathesis Hypothesis worker count.\n- `max_examples` (optional): Schemathesis generated-example cap per operation.\n- `headers` (optional): HTTP headers as JSON map or newline text for replay utilities.\n- `auth` (optional): Compact credential or Authorization header string for Schemathesis/HTTP harnesses.\n- `request_timeout` (optional): Per-request HTTP timeout for Schemathesis.\n- `timeout` (optional): Seconds for socket/DNS/CLI overall timeouts.\n- `phases` (optional): Schemathesis comma-separated phases (`examples`, `coverage`, \u2026).\n- `mode` (optional): High-level tool mode (Amass `enum`, gobuster `dir`, shuffleDNS profile, etc.).\n- `rate_limit` (optional): Global HTTP RPS throttle for schema fuzzing.\n- `report_formats` (optional): Schemathesis export format list (`html,json,junit`).\n- `report_dir` (optional): Schemathesis/Scout HTML/SARIF/JSON bundle root.\n- `include_operation_id` (optional): Restrict fuzzing to these OpenAPI operationIds.\n- `exclude_operation_id` (optional): Skip risky OpenAPI operationIds.\n- `max_failures` (optional): Fail-fast threshold for flaky API fuzz campaigns.\n- `additional_args` (optional): Shell tokens appended after mapped CLI arguments."
  },
  "scout-suite": {
    "long_description": "Scout Suite audits AWS/Azure/GCP/OCI footprints with hierarchical HTML reports, selectable services, curated exceptions and profile-aware authentication.\n\nCloud posture connectors emit JSON/HTML/SARIF findings enumerating IAM sprawl, open buckets, CIS deviations, stale keys, and attack-path narratives.\n\nCloud scans require least-privilege API roles scoped to enumerated accounts\u2014not root keys embedded in plaintext JSON long term.\n\nProvider APIs log every call; minimise repeated broad scans and purge credentials promptly after tabletop windows.\n\nCipherStrike transmits the JSON envelope to `/api/tools/scout-suite`; NyxStrike performs final validation, then shells out or calls the bundled integration advertised for this catalogue id.",
    "safety": "Operate only inside authorized penetration tests or sanctioned lab networks. Respect stop conditions, lawful intercept rules, and internal change windows. Sensitive parameters are captured in CipherStrike tenant execution logs alongside NyxStrike response snippets \u2014 avoid pasting production secrets when alternatives exist. Cloud API tokens authenticate broad actions\u2014purge keys after tabletop exercises.",
    "usage": "CipherStrike `scout-suite` executes against `/api/tools/scout-suite` via POST JSON envelopes assembled in the Execute modal.\n\n1. Populate cloud profile/session fields provider flags services directories per scout/pacu/cloudmapper wrappers.\n2. Expect large IAM graphs\u2014narrow services first.\n3. Export HTML/JSON responsibly; URLs may contain account identifiers.\n4. Rotate cloud keys after engagements.\n\nCipherStrike mechanics: send a JSON POST from the Execute modal\u2014the agent drops empty optional scalars before invoking NyxStrike.\nPrefer explicit catalogue fields (`target`, `url`, `wordlist`, etc.) before `additional_args`; extra tokens must still match the packaged wrapper behaviour and your engagement rules.\nPer-field catalogue hints (first sentence of modal help):\n- `provider` (optional): Cloud vendor (`aws`, `azure`, `gcp`, `oci`).\n- `profile` (optional): Volatility KDBG profile **or** named cloud CLI profile for Prowler\u2014check tool context.\n- `report_dir` (optional): Schemathesis/Scout HTML/SARIF/JSON bundle root.\n- `services` (optional): Service allowlist for Scout Suite / cloud modules.\n- `exceptions` (optional): Scout JSON describing suppressed false positives.\n- `additional_args` (optional): Shell tokens appended after mapped CLI arguments."
  },
  "searchsploit": {
    "long_description": "searchsploit queries local Exploit-DB mirrors for textual matches aligning with responsibly disclosed disclosures.\n\nMetasploit/payload harnesses expose sessions, staged shellcode artefacts, staged listeners, meterpreter transcripts, and failure diagnostics.\n\nExploitation tooling is for labs with containment; maintain VM snapshots because modules may patch or crash unstable services.\n\nPayload generation can trip AV egress controls\u2014isolate build hosts and watermark binaries for traceability.\n\nCipherStrike transmits the JSON envelope to `/api/tools/exploit_framework/exploit_db`; NyxStrike performs final validation, then shells out or calls the bundled integration advertised for this catalogue id.",
    "safety": "Operate only inside authorized penetration tests or sanctioned lab networks. Respect stop conditions, lawful intercept rules, and internal change windows. Sensitive parameters are captured in CipherStrike tenant execution logs alongside NyxStrike response snippets \u2014 avoid pasting production secrets when alternatives exist. Weaponised modules may crash services\u2014snapshot lab VMs and segregate egress before launching.",
    "usage": "CipherStrike `searchsploit` executes against `/api/tools/exploit_framework/exploit_db` via POST JSON envelopes assembled in the Execute modal.\n\n1. Provide search strings aligning with exploit-db offline mirrors bundled on NyxStrike.\n2. Exploit PoCs may be unreliable\u2014audit code before executing.\n3. Record CVE references for Responsible Disclosure trackers.\n4. Keep mirrors updated per compliance policy.\n\nCipherStrike mechanics: send a JSON POST from the Execute modal\u2014the agent drops empty optional scalars before invoking NyxStrike.\nPrefer explicit catalogue fields (`target`, `url`, `wordlist`, etc.) before `additional_args`; extra tokens must still match the packaged wrapper behaviour and your engagement rules.\nPer-field catalogue hints (first sentence of modal help):\n- `query` (required): The workspace forwards `query` in the CipherStrike POST body unchanged after stripping empty optional fields.\n- `additional_args` (optional): Shell tokens appended after mapped CLI arguments."
  },
  "sherlock": {
    "long_description": "Sherlock enumerates pseudonyms across hundreds of exposed social sites flagging dormant or hijackable personas.\n\nResults are textual lists, JSON artefacts, archive URLs, or passive reconnaissance breadcrumbs suitable for ingestion into trackers.\n\nOSINT tooling should precede intrusive scanning wherever policy demands minimal touch; escalate to DNS brute or crawler stages only after validating scope wording.\n\nPassive APIs throttle aggressively and may log lookups under shared keys\u2014credential handling matters.\n\nCipherStrike transmits the JSON envelope to `/api/tools/osint/sherlock`; NyxStrike performs final validation, then shells out or calls the bundled integration advertised for this catalogue id.",
    "safety": "Operate only inside authorized penetration tests or sanctioned lab networks. Respect stop conditions, lawful intercept rules, and internal change windows. Sensitive parameters are captured in CipherStrike tenant execution logs alongside NyxStrike response snippets \u2014 avoid pasting production secrets when alternatives exist.",
    "usage": "CipherStrike `sherlock` executes against `/api/tools/osint/sherlock` via POST JSON envelopes assembled in the Execute modal.\n\n1. Provide handles timeout concurrency flags Sherlock expects.\n2. Social hits may belong to unrelated users verify manually.\n3. Respect site ToS\u2014even OSINT engagements need legal review sometimes.\n4. Cache JSON for privacy reviews before wider sharing.\n\nCipherStrike mechanics: send a JSON POST from the Execute modal\u2014the agent drops empty optional scalars before invoking NyxStrike.\nPrefer explicit catalogue fields (`target`, `url`, `wordlist`, etc.) before `additional_args`; extra tokens must still match the packaged wrapper behaviour and your engagement rules.\nPer-field catalogue hints (first sentence of modal help):\n- `username` (required): Username for SMB/LDAP/WinRM clients and login brute modules."
  },
  "shuffledns": {
    "long_description": "ProjectDiscovery shuffleDNS wraps MassDNS-compatible resolution with wildcard detection, brute wordlists, trusted resolvers, and JSON-friendly reporting.\n\nshuffleDNS normalises brute inputs runs wildcard detection leverages trusted resolver baselines emits JSON summaries.\n\nUse shuffleDNS orchestration whenever ProjectDiscovery-compatible DNS workflows demand MassDNS backends with PD ergonomics.\n\nMisconfigured wildcard logic yields false negatives\u2014supply trusted resolver pairs from scope owners.\n\nCipherStrike transmits the JSON envelope to `/api/tools/shuffledns`; NyxStrike performs final validation, then shells out or calls the bundled integration advertised for this catalogue id.",
    "safety": "Operate only inside authorized penetration tests or sanctioned lab networks. Respect stop conditions, lawful intercept rules, and internal change windows. Sensitive parameters are captured in CipherStrike tenant execution logs alongside NyxStrike response snippets \u2014 avoid pasting production secrets when alternatives exist.",
    "usage": "CipherStrike `shuffledns` executes against `/api/tools/shuffledns` via POST JSON envelopes assembled in the Execute modal.\n\n1. Feed `domains`/`domain`, `list`, resolver pools, wildcard controls, optional MassDNS bridging fields.\n2. Validate `trusted_resolver` to reduce poisoned brute-force positives.\n3. Monitor query rates for provider abuse alerts.\n4. Export JSON-compatible outputs before downstream chaining.\n\nCipherStrike mechanics: send a JSON POST from the Execute modal\u2014the agent drops empty optional scalars before invoking NyxStrike.\nPrefer explicit catalogue fields (`target`, `url`, `wordlist`, etc.) before `additional_args`; extra tokens must still match the packaged wrapper behaviour and your engagement rules.\nPer-field catalogue hints (first sentence of modal help):\n- `domain` (optional): Authorised DNS apex fed to Amass/subfinder/GAU style OSINT connectors.\n- `domains` (optional): Additional apex names shuffledns can merge with `domain` for brute jobs.\n- `auto_domain` (optional): shuffleDNS may infer apex domains from mixed inputs\u2014disable for strict scope.\n- `list` (optional): ShuffleDNS bruteforce wordlist path.\n- `wordlist` (optional): Agent-resident newline wordlist for content/DNS/password tooling.\n- `resolver` (optional): Resolver `ip:port` for shuffleDNS active jobs.\n- `trusted_resolver` (optional): Ground-truth resolver for shuffleDNS wildcard logic.\n- `raw_input` (optional): Domain corpus without normalisation for advanced DNS utilities.\n- `mode` (optional): High-level tool mode (Amass `enum`, gobuster `dir`, shuffleDNS profile, etc.).\n- `threads` (optional): Concurrent workers for ffuf/arjun/sublist3r/feroxbuster HTTP jobs.\n- `output` (optional): Primary output sink some CLIs require (`-o`, `--output`).\n- `json` (optional): Emit JSON-line output for ShuffleDNS-compatible tooling.\n- `wildcard_output` (optional): The workspace forwards `wildcard_output` in the CipherStrike POST body unchanged after stripping empty optional fields.\n- `massdns` (optional): MassDNS binary path or helper directory for shuffleDNS glue.\n- `massdns_cmd` (optional): Custom MassDNS launcher prefix when images differ.\n- `directory` (optional): Repository root for Checkov or broad filesystem Trivy scans.\n- `retries` (optional): Resolver retries for ShuffleDNS brute mode.\n- `strict_wildcard` (optional): shuffleDNS strict wildcard handling for poisoned zones.\n- _(and 8 more optional keys \u2014 see catalogue defaults in the Execute modal)._\n- shuffleDNS fronts MassDNS-compatible jobs; tighten `resolver`/`trusted_resolver` when testing split-horizon DNS."
  },
  "sleuthkit": {
    "long_description": "SleuthKit CLI primitives (`blkls`, `icat`, `fls`) ingest forensic images for scripted evidence plumbing.\n\nForensic tooling streams carved files, timelines, entropy maps, PCAP decodes, volatility tables, SQLite extractions.\n\nForensic artefacts may contain HIPAA/PII regulated content\u2014encrypt exports immediately.\n\nResource-heavy jobs need disk-bound IO quotas; carve only targeted ranges when multi-terabyte images are mounted.\n\nCipherStrike transmits the JSON envelope to `/api/tools/sleuthkit`; NyxStrike performs final validation, then shells out or calls the bundled integration advertised for this catalogue id.",
    "safety": "Operate only inside authorized penetration tests or sanctioned lab networks. Respect stop conditions, lawful intercept rules, and internal change windows. Sensitive parameters are captured in CipherStrike tenant execution logs alongside NyxStrike response snippets \u2014 avoid pasting production secrets when alternatives exist. Evidence images may contain regulated data\u2014encrypt at rest immediately after extraction.",
    "usage": "CipherStrike `sleuthkit` executes against `/api/tools/sleuthkit` via POST JSON envelopes assembled in the Execute modal.\n\n1. Point `image_path`/`directory`/`case_name`/`project_name` fields at sanctioned evidence lockers.\n2. Long-running ingestion jobs need disk quotas\u2014coordinate with infra.\n3. Exported timelines may contain privileged communications restrict viewers.\n4. Chain SleuthKit outputs into Autopsy dashboards when routes split duties.\n\nCipherStrike mechanics: send a JSON POST from the Execute modal\u2014the agent drops empty optional scalars before invoking NyxStrike.\nPrefer explicit catalogue fields (`target`, `url`, `wordlist`, etc.) before `additional_args`; extra tokens must still match the packaged wrapper behaviour and your engagement rules.\nPer-field catalogue hints (first sentence of modal help):\n- `image_path` (required): Forensic disk image for Autopsy/sleuthkit ingestion.\n- `command` (optional): Escaped command string for psexec/wmiexec/smbexec-style runners.\n- `additional_args` (optional): Shell tokens appended after mapped CLI arguments."
  },
  "smart-scan": {
    "long_description": "Smart-scan orchestrates multiple catalogue tools with AI-assisted parameter selection, parallel fan-out, and guardrails limited by `max_tools` and enterprise policy.\n\nIntelligence routers combine analysis, chaining, profiling, orchestration manifests, AI-selected toolplans, and explanatory narratives about why certain jobs fire.\n\nSmart planners trade determinism for speed\u2014validate proposed fan-outs before unattended execution on sensitive ranges.\n\nAutomated chains still require human scope checks before touching regulated data classifications.\n\nCipherStrike transmits the JSON envelope to `/api/intelligence/smart-scan`; NyxStrike performs final validation, then shells out or calls the bundled integration advertised for this catalogue id.",
    "safety": "Operate only inside authorized penetration tests or sanctioned lab networks. Respect stop conditions, lawful intercept rules, and internal change windows. Sensitive parameters are captured in CipherStrike tenant execution logs alongside NyxStrike response snippets \u2014 avoid pasting production secrets when alternatives exist. Automated chaining still needs data-handling approvals for anything beyond public banners.",
    "usage": "CipherStrike `smart-scan` executes against `/api/intelligence/smart-scan` via POST JSON envelopes assembled in the Execute modal.\n\n1. Confirm the written scope authorises automated intelligence or orchestration against the supplied target.\n2. Populate `target` with the exact hostname, URL, or asset identifier your engagement letter lists\u2014no implicit wildcards.\n3. For `smart-scan`, tune `objective` and `max_tools` so the planner cannot fan out beyond your risk appetite; review the proposed fan-out in session logs before accepting unattended runs.\n4. `preview-attack-chain` avoids persistence\u2014use it first, then run `create-attack-chain` only when stored hypotheses are required.\n5. Interpret AI output as advisory: validate every recommended action with deterministic tooling and customer policies.\n`additional_args` is rarely used on these routes; prefer structured fields.\n\nCipherStrike mechanics: send a JSON POST from the Execute modal\u2014the agent drops empty optional scalars before invoking NyxStrike.\nPrefer explicit catalogue fields (`target`, `url`, `wordlist`, etc.) before `additional_args`; extra tokens must still match the packaged wrapper behaviour and your engagement rules.\nPer-field catalogue hints (first sentence of modal help):\n- `target` (required): Primary IPv4/IPv6/hostname/CIDR or bulk newline list routed into network scanners and RPC clients.\n- `objective` (optional): Planner hint for smart-scan/attack-chain heuristics (`comprehensive`, `stealth`).\n- `max_tools` (optional): Smart-scan parallel tool budget."
  },
  "smbmap": {
    "long_description": "smbmap lists SMB shares, permissions, and common misconfigurations with optional command execution primitives where creds permit.\n\nExpect port listings, traceroute-like hints, service banners, NIC inventories, handshake captures, or ARP inventories depending on tooling.\n\nFast SYN scanners suit huge ranges; balanced scanners add scripts and versioning; SMB-oriented tools deepen Windows visibility\u2014choose based on scope scale and covertness mandates.\n\nHigh packet rates overwhelm small networks\u2014rate-limit, subdivide subnets, and obtain written approval before touching neighbouring tenants.\n\nCipherStrike transmits the JSON envelope to `/api/tools/smbmap`; NyxStrike performs final validation, then shells out or calls the bundled integration advertised for this catalogue id.",
    "safety": "Operate only inside authorized penetration tests or sanctioned lab networks. Respect stop conditions, lawful intercept rules, and internal change windows. Sensitive parameters are captured in CipherStrike tenant execution logs alongside NyxStrike response snippets \u2014 avoid pasting production secrets when alternatives exist. Port scanners and handshake grabbers alert defenders\u2014mirror scope IPs exactly as authorised.",
    "usage": "CipherStrike `smbmap` executes against `/api/tools/smbmap` via POST JSON envelopes assembled in the Execute modal.\n\n1. Enter `target`, share, and credential material per engagement policy\u2014rotate secrets afterwards.\n2. Decide whether command execution helpers are in scope; disable them when read-only mapping suffices.\n3. Capture share permission tables from stdout; they feed privilege escalation narratives.\n4. Large share trees take time\u2014scope recursion options if the connector exposes them.\n\nCipherStrike mechanics: send a JSON POST from the Execute modal\u2014the agent drops empty optional scalars before invoking NyxStrike.\nPrefer explicit catalogue fields (`target`, `url`, `wordlist`, etc.) before `additional_args`; extra tokens must still match the packaged wrapper behaviour and your engagement rules.\nPer-field catalogue hints (first sentence of modal help):\n- `target` (required): Primary IPv4/IPv6/hostname/CIDR or bulk newline list routed into network scanners and RPC clients.\n- `additional_args` (optional): Shell tokens appended after mapped CLI arguments."
  },
  "spiderfoot": {
    "long_description": "Spiderfoot orchestrates modular OSINT sweeps (>200 connectors) emitting correlation graphs correlating artefacts for a seeded `target`.\n\nResults are textual lists, JSON artefacts, archive URLs, or passive reconnaissance breadcrumbs suitable for ingestion into trackers.\n\nOSINT tooling should precede intrusive scanning wherever policy demands minimal touch; escalate to DNS brute or crawler stages only after validating scope wording.\n\nPassive APIs throttle aggressively and may log lookups under shared keys\u2014credential handling matters.\n\nCipherStrike transmits the JSON envelope to `/api/tools/osint/spiderfoot`; NyxStrike performs final validation, then shells out or calls the bundled integration advertised for this catalogue id.",
    "safety": "Operate only inside authorized penetration tests or sanctioned lab networks. Respect stop conditions, lawful intercept rules, and internal change windows. Sensitive parameters are captured in CipherStrike tenant execution logs alongside NyxStrike response snippets \u2014 avoid pasting production secrets when alternatives exist.",
    "usage": "CipherStrike `spiderfoot` executes against `/api/tools/osint/spiderfoot` via POST JSON envelopes assembled in the Execute modal.\n\n1. Load workspace modules transforms API keys complying with Spiderfoot/Recon-ng/Maltego licensing.\n2. Longitudinal jobs (`poll_interval`) may take hours\u2014checkpoint state.\n3. Graph exports can leak third-party identifiers handle per GDPR charters.\n4. Disable modules that scrape disallowed jurisdictions.\n\nCipherStrike mechanics: send a JSON POST from the Execute modal\u2014the agent drops empty optional scalars before invoking NyxStrike.\nPrefer explicit catalogue fields (`target`, `url`, `wordlist`, etc.) before `additional_args`; extra tokens must still match the packaged wrapper behaviour and your engagement rules.\nPer-field catalogue hints (first sentence of modal help):\n- `target` (required): Primary IPv4/IPv6/hostname/CIDR or bulk newline list routed into network scanners and RPC clients."
  },
  "sqlite3": {
    "long_description": "SQLite evaluator executes examiner `.schema`/`.read` flows against forensic databases without networked listeners.\n\nDatabase probes show schema excerpts, benign SQL probe results, latency statistics, credential failures.\n\nUse least-privilege database accounts with read-mostly grants unless destructive testing is chartered.\n\nSQL strings echo to DB audit logs alongside NyxStrike\u2014avoid production accounts without rollover plans.\n\nCipherStrike transmits the JSON envelope to `/api/tools/sqlite3`; NyxStrike performs final validation, then shells out or calls the bundled integration advertised for this catalogue id.",
    "safety": "Operate only inside authorized penetration tests or sanctioned lab networks. Respect stop conditions, lawful intercept rules, and internal change windows. Sensitive parameters are captured in CipherStrike tenant execution logs alongside NyxStrike response snippets \u2014 avoid pasting production secrets when alternatives exist. Database credentials in JSON propagate to bastion logs\u2014rotate after tabletop exercises.",
    "usage": "CipherStrike `sqlite3` executes against `/api/tools/sqlite3` via POST JSON envelopes assembled in the Execute modal.\n\n1. Supply connection strings databases SQL fragments credentials per mysql/sqlite wrappers.\n2. Dry-run selects before mutating\u2014even read queries may lock rows.\n3. Log every statement for auditors.\n4. Use disposable databases whenever feasible.\n\nCipherStrike mechanics: send a JSON POST from the Execute modal\u2014the agent drops empty optional scalars before invoking NyxStrike.\nPrefer explicit catalogue fields (`target`, `url`, `wordlist`, etc.) before `additional_args`; extra tokens must still match the packaged wrapper behaviour and your engagement rules.\nPer-field catalogue hints (first sentence of modal help):\n- `db_path` (required): Agent filesystem path for `db_path` consumed by the connector.\n- `query` (required): The workspace forwards `query` in the CipherStrike POST body unchanged after stripping empty optional fields."
  },
  "sqlmap": {
    "long_description": "sqlmap automates detecting and exploiting classic SQL injection classes (union, blind, stacked, time-based) across dozens of DBMS engines with granular tamper and data extraction features.\n\nsqlmap reports injection classes payloads tamper combos extracted table fragments plus timing histograms.\n\nReserve sqlmap once manual triage validates candidate parameters tamper combos belong to chartered DBMS families.\n\nData exfil payloads may violate privacy charters\u2014clamp `--dump` equivalents via policy.\n\nCipherStrike transmits the JSON envelope to `/api/tools/sqlmap`; NyxStrike performs final validation, then shells out or calls the bundled integration advertised for this catalogue id.",
    "safety": "Operate only inside authorized penetration tests or sanctioned lab networks. Respect stop conditions, lawful intercept rules, and internal change windows. Sensitive parameters are captured in CipherStrike tenant execution logs alongside NyxStrike response snippets \u2014 avoid pasting production secrets when alternatives exist. Automated extraction can export regulated columns\u2014use allowlists and isolated databases.",
    "usage": "CipherStrike `sqlmap` executes against `/api/tools/sqlmap` via POST JSON envelopes assembled in the Execute modal.\n\n1. Provide `url`, parameter hints, data blobs, and tamper options exactly as the wrapper maps from sqlmap.\n2. Run only against consenting databases; start with `--technique` equivalents exposed as JSON fields.\n3. Review data exfiltration settings\u2014disable dumping unless charters allow it.\n4. Log tamper selections to reproduce findings deterministically.\n\nCipherStrike mechanics: send a JSON POST from the Execute modal\u2014the agent drops empty optional scalars before invoking NyxStrike.\nPrefer explicit catalogue fields (`target`, `url`, `wordlist`, etc.) before `additional_args`; extra tokens must still match the packaged wrapper behaviour and your engagement rules.\nPer-field catalogue hints (first sentence of modal help):\n- `url` (required): Fully qualified HTTP/S URL for crawlers, fuzzers, or vuln scanners (scheme required).\n- `data` (optional): sqlmap POST body, Commix injection context, or hashpump cleartext segment.\n- `additional_args` (optional): Shell tokens appended after mapped CLI arguments."
  },
  "steghide": {
    "long_description": "steghide embeds AES/zip-compressed payloads in lossless carriers with passphrase-controlled integrity checks `action` selects embed/extract/info flows.\n\nForensic tooling streams carved files, timelines, entropy maps, PCAP decodes, volatility tables, SQLite extractions.\n\nForensic artefacts may contain HIPAA/PII regulated content\u2014encrypt exports immediately.\n\nResource-heavy jobs need disk-bound IO quotas; carve only targeted ranges when multi-terabyte images are mounted.\n\nCipherStrike transmits the JSON envelope to `/api/tools/steghide`; NyxStrike performs final validation, then shells out or calls the bundled integration advertised for this catalogue id.",
    "safety": "Operate only inside authorized penetration tests or sanctioned lab networks. Respect stop conditions, lawful intercept rules, and internal change windows. Sensitive parameters are captured in CipherStrike tenant execution logs alongside NyxStrike response snippets \u2014 avoid pasting production secrets when alternatives exist. Evidence images may contain regulated data\u2014encrypt at rest immediately after extraction.",
    "usage": "CipherStrike `steghide` executes against `/api/tools/steghide` via POST JSON envelopes assembled in the Execute modal.\n\n1. Provide cover/embed files passphrase actions keyed to steghide/stegsolve/zsteg/outguess flows.\n2. Outputs may be ambiguous\u2014combine with entropy analysis elsewhere.\n3. Carriers might be copyrighted\u2014handle distribution carefully.\n4. Document attempted transforms for regulators.\n\nCipherStrike mechanics: send a JSON POST from the Execute modal\u2014the agent drops empty optional scalars before invoking NyxStrike.\nPrefer explicit catalogue fields (`target`, `url`, `wordlist`, etc.) before `additional_args`; extra tokens must still match the packaged wrapper behaviour and your engagement rules.\nPer-field catalogue hints (first sentence of modal help):\n- `cover_file` (required): Carrier file for Steghide embed/extract.\n- `action` (optional): `airmon-ng` verb (`start`/`stop`) or CloudMapper subcommand name.\n- `embed_file` (optional): Secret payload Steghide hides inside `cover_file`.\n- `passphrase` (optional): WPA PSK or Steghide passphrase for crypto operations.\n- `output_file` (optional): Explicit output path for logs/PCAPNG/JSON when CLIs support `-o`.\n- `additional_args` (optional): Shell tokens appended after mapped CLI arguments."
  },
  "stegsolve": {
    "long_description": "Stegsolve visual-analyses bitmap planes aiding manual LSB/permutation stego detection for CTF/lab artefacts.\n\nForensic tooling streams carved files, timelines, entropy maps, PCAP decodes, volatility tables, SQLite extractions.\n\nForensic artefacts may contain HIPAA/PII regulated content\u2014encrypt exports immediately.\n\nResource-heavy jobs need disk-bound IO quotas; carve only targeted ranges when multi-terabyte images are mounted.\n\nCipherStrike transmits the JSON envelope to `/api/tools/stegsolve`; NyxStrike performs final validation, then shells out or calls the bundled integration advertised for this catalogue id.",
    "safety": "Operate only inside authorized penetration tests or sanctioned lab networks. Respect stop conditions, lawful intercept rules, and internal change windows. Sensitive parameters are captured in CipherStrike tenant execution logs alongside NyxStrike response snippets \u2014 avoid pasting production secrets when alternatives exist. Evidence images may contain regulated data\u2014encrypt at rest immediately after extraction.",
    "usage": "CipherStrike `stegsolve` executes against `/api/tools/stegsolve` via POST JSON envelopes assembled in the Execute modal.\n\n1. Provide cover/embed files passphrase actions keyed to steghide/stegsolve/zsteg/outguess flows.\n2. Outputs may be ambiguous\u2014combine with entropy analysis elsewhere.\n3. Carriers might be copyrighted\u2014handle distribution carefully.\n4. Document attempted transforms for regulators.\n\nCipherStrike mechanics: send a JSON POST from the Execute modal\u2014the agent drops empty optional scalars before invoking NyxStrike.\nPrefer explicit catalogue fields (`target`, `url`, `wordlist`, etc.) before `additional_args`; extra tokens must still match the packaged wrapper behaviour and your engagement rules.\nPer-field catalogue hints (first sentence of modal help):\n- `file_path` (required): Readable path for xxd/exiftool style utilities.\n- `additional_args` (optional): Shell tokens appended after mapped CLI arguments."
  },
  "strings": {
    "long_description": "GNU/strings surfaces printable sequences aiding triage inside firmware dumps or unstructured binaries.\n\nForensic tooling streams carved files, timelines, entropy maps, PCAP decodes, volatility tables, SQLite extractions.\n\nForensic artefacts may contain HIPAA/PII regulated content\u2014encrypt exports immediately.\n\nResource-heavy jobs need disk-bound IO quotas; carve only targeted ranges when multi-terabyte images are mounted.\n\nCipherStrike transmits the JSON envelope to `/api/tools/strings`; NyxStrike performs final validation, then shells out or calls the bundled integration advertised for this catalogue id.",
    "safety": "Operate only inside authorized penetration tests or sanctioned lab networks. Respect stop conditions, lawful intercept rules, and internal change windows. Sensitive parameters are captured in CipherStrike tenant execution logs alongside NyxStrike response snippets \u2014 avoid pasting production secrets when alternatives exist. Evidence images may contain regulated data\u2014encrypt at rest immediately after extraction.",
    "usage": "CipherStrike `strings` executes against `/api/tools/strings` via POST JSON envelopes assembled in the Execute modal.\n\n1. Provide `binary`, `file_path`, or CLI-equivalent selectors for quick static insight.\n2. Use triage utilities before heavyweight Ghidra/autopsy timelines.\n3. Outputs are succinct\u2014combine with checklist reporting.\n4. Malware artefacts remain hazardous\u2014sandbox hosts accordingly.\n\nCipherStrike mechanics: send a JSON POST from the Execute modal\u2014the agent drops empty optional scalars before invoking NyxStrike.\nPrefer explicit catalogue fields (`target`, `url`, `wordlist`, etc.) before `additional_args`; extra tokens must still match the packaged wrapper behaviour and your engagement rules.\nPer-field catalogue hints (first sentence of modal help):\n- `file` (required): Binary/firmware specimen for binwalk/strings/checksec.\n- `additional_args` (optional): Shell tokens appended after mapped CLI arguments."
  },
  "subfinder": {
    "long_description": "Subfinder merges dozens of passive API/certificate/OSINT connectors for subdomain discovery with sane rate limits controlled via feature flags (e.g., `silent`, `all_sources`).\n\nResults are textual lists, JSON artefacts, archive URLs, or passive reconnaissance breadcrumbs suitable for ingestion into trackers.\n\nOSINT tooling should precede intrusive scanning wherever policy demands minimal touch; escalate to DNS brute or crawler stages only after validating scope wording.\n\nPassive APIs throttle aggressively and may log lookups under shared keys\u2014credential handling matters.\n\nCipherStrike transmits the JSON envelope to `/api/tools/subfinder`; NyxStrike performs final validation, then shells out or calls the bundled integration advertised for this catalogue id.",
    "safety": "Operate only inside authorized penetration tests or sanctioned lab networks. Respect stop conditions, lawful intercept rules, and internal change windows. Sensitive parameters are captured in CipherStrike tenant execution logs alongside NyxStrike response snippets \u2014 avoid pasting production secrets when alternatives exist.",
    "usage": "CipherStrike `subfinder` executes against `/api/tools/subfinder` via POST JSON envelopes assembled in the Execute modal.\n\n1. Populate `domain` and API-dependent toggles (`all_sources`).\n2. Run during OSINT phases; results seed active scanning once approved.\n3. Deduplicate output with shuffleDNS/wordlists cautiously.\n4. Store JSON for traceability across assessments.\n\nCipherStrike mechanics: send a JSON POST from the Execute modal\u2014the agent drops empty optional scalars before invoking NyxStrike.\nPrefer explicit catalogue fields (`target`, `url`, `wordlist`, etc.) before `additional_args`; extra tokens must still match the packaged wrapper behaviour and your engagement rules.\nPer-field catalogue hints (first sentence of modal help):\n- `domain` (required): Authorised DNS apex fed to Amass/subfinder/GAU style OSINT connectors.\n- `silent` (optional): The workspace forwards `silent` in the CipherStrike POST body unchanged after stripping empty optional fields.\n- `all_sources` (optional): Expand subfinder to every passive source\u2014more external queries and latency.\n- `additional_args` (optional): Shell tokens appended after mapped CLI arguments."
  },
  "sublist3r": {
    "long_description": "Sublist3r aggregates search-engine and OSINT subdomain hints with tuneable parallelism per engine selections.\n\nResults are textual lists, JSON artefacts, archive URLs, or passive reconnaissance breadcrumbs suitable for ingestion into trackers.\n\nOSINT tooling should precede intrusive scanning wherever policy demands minimal touch; escalate to DNS brute or crawler stages only after validating scope wording.\n\nPassive APIs throttle aggressively and may log lookups under shared keys\u2014credential handling matters.\n\nCipherStrike transmits the JSON envelope to `/api/osint/tools/sublist3r`; NyxStrike performs final validation, then shells out or calls the bundled integration advertised for this catalogue id.",
    "safety": "Operate only inside authorized penetration tests or sanctioned lab networks. Respect stop conditions, lawful intercept rules, and internal change windows. Sensitive parameters are captured in CipherStrike tenant execution logs alongside NyxStrike response snippets \u2014 avoid pasting production secrets when alternatives exist.",
    "usage": "CipherStrike `sublist3r` executes against `/api/osint/tools/sublist3r` via POST JSON envelopes assembled in the Execute modal.\n\n1. Provide engines and parallelism fields per Sublist3r JSON schema.\n2. Expect heuristic noise\u2014engines change frequently.\n3. Merge with authoritative DNS before claiming ownership.\n4. API failures should be retried politely.\n\nCipherStrike mechanics: send a JSON POST from the Execute modal\u2014the agent drops empty optional scalars before invoking NyxStrike.\nPrefer explicit catalogue fields (`target`, `url`, `wordlist`, etc.) before `additional_args`; extra tokens must still match the packaged wrapper behaviour and your engagement rules.\nPer-field catalogue hints (first sentence of modal help):\n- `domain` (required): Authorised DNS apex fed to Amass/subfinder/GAU style OSINT connectors.\n- `threads` (optional): Concurrent workers for ffuf/arjun/sublist3r/feroxbuster HTTP jobs.\n- `engine` (optional): Sublist3r search-engine string."
  },
  "tcpdump": {
    "long_description": "tcpdump records libpcap expressions with BER-length filters\u2014coordinate before tapping sensitive VLANs.\n\n802.11 harnesses emit captures, WPA hash material, rogue AP scaffolding, injector acknowledgements, and audit logs keyed to channels/BSSIDs.\n\nLaboratory rigs should dedicate monitor-mode interfaces plus isolated spectrum; tethered enterprise APs rarely tolerate injection homework.\n\nJamming neighbours or intercepting unauthorised traffic violates law\u2014operate strictly inside sanctioned RF enclosures and licensed bands.\n\nCipherStrike transmits the JSON envelope to `/api/tools/tcpdump`; NyxStrike performs final validation, then shells out or calls the bundled integration advertised for this catalogue id.",
    "safety": "Operate only inside authorized penetration tests or sanctioned lab networks. Respect stop conditions, lawful intercept rules, and internal change windows. Sensitive parameters are captured in CipherStrike tenant execution logs alongside NyxStrike response snippets \u2014 avoid pasting production secrets when alternatives exist. Wireless probing and injection are lawful only on spectrum and networks you control by license or contract. Promiscuous taps may capture sensitive payloads\u2014follow lawful intercept policies.",
    "usage": "CipherStrike `tcpdump` executes against `/api/tools/tcpdump` via POST JSON envelopes assembled in the Execute modal.\n\n1. Provide PCAP paths capture/display filters exporters stats toggles aligning with tcpdump/tshark/wireshark packaging.\n2. Expect sensitive payloads minimise distribution encrypt everything.\n3. Follow lawful intercept mandates when analysing customer traffic mirrors.\n4. GUI Wireshark stays human-driven headless watchers feed SIEM deltas.\n\nCipherStrike mechanics: send a JSON POST from the Execute modal\u2014the agent drops empty optional scalars before invoking NyxStrike.\nPrefer explicit catalogue fields (`target`, `url`, `wordlist`, etc.) before `additional_args`; extra tokens must still match the packaged wrapper behaviour and your engagement rules.\nPer-field catalogue hints (first sentence of modal help):\n- `interface` (required): Network interface (`wlan0mon`, `eth0`) for Wi\u2011Fi or Responder jobs.\n- `filter` (optional): MassDNS output-filter DSL restricting printed answers.\n- `count` (optional): `aireplay-ng` packet burst counter.\n- `output_file` (optional): Explicit output path for logs/PCAPNG/JSON when CLIs support `-o`.\n- `additional_args` (optional): Shell tokens appended after mapped CLI arguments."
  },
  "technology-detection": {
    "long_description": "Technology stack inference pass that maps observed banners, HTML, and TLS clues to testing guidance for later manual steps.\n\nIntelligence routers combine analysis, chaining, profiling, orchestration manifests, AI-selected toolplans, and explanatory narratives about why certain jobs fire.\n\nSmart planners trade determinism for speed\u2014validate proposed fan-outs before unattended execution on sensitive ranges.\n\nAutomated chains still require human scope checks before touching regulated data classifications.\n\nCipherStrike transmits the JSON envelope to `/api/intelligence/technology-detection`; NyxStrike performs final validation, then shells out or calls the bundled integration advertised for this catalogue id.",
    "safety": "Operate only inside authorized penetration tests or sanctioned lab networks. Respect stop conditions, lawful intercept rules, and internal change windows. Sensitive parameters are captured in CipherStrike tenant execution logs alongside NyxStrike response snippets \u2014 avoid pasting production secrets when alternatives exist. Automated chaining still needs data-handling approvals for anything beyond public banners.",
    "usage": "CipherStrike `technology-detection` executes against `/api/intelligence/technology-detection` via POST JSON envelopes assembled in the Execute modal.\n\n1. Confirm the written scope authorises automated intelligence or orchestration against the supplied target.\n2. Populate `target` with the exact hostname, URL, or asset identifier your engagement letter lists\u2014no implicit wildcards.\n3. For `smart-scan`, tune `objective` and `max_tools` so the planner cannot fan out beyond your risk appetite; review the proposed fan-out in session logs before accepting unattended runs.\n4. `preview-attack-chain` avoids persistence\u2014use it first, then run `create-attack-chain` only when stored hypotheses are required.\n5. Interpret AI output as advisory: validate every recommended action with deterministic tooling and customer policies.\n`additional_args` is rarely used on these routes; prefer structured fields.\n\nCipherStrike mechanics: send a JSON POST from the Execute modal\u2014the agent drops empty optional scalars before invoking NyxStrike.\nPrefer explicit catalogue fields (`target`, `url`, `wordlist`, etc.) before `additional_args`; extra tokens must still match the packaged wrapper behaviour and your engagement rules.\nPer-field catalogue hints (first sentence of modal help):\n- `target` (required): Primary IPv4/IPv6/hostname/CIDR or bulk newline list routed into network scanners and RPC clients."
  },
  "terrascan": {
    "long_description": "Terrascan applies vendor policy packs (`policy_type`), severity thresholds, IaC scanning mode toggles spanning Terraform/Kubernetes registries etc.\n\nCloud posture connectors emit JSON/HTML/SARIF findings enumerating IAM sprawl, open buckets, CIS deviations, stale keys, and attack-path narratives.\n\nCloud scans require least-privilege API roles scoped to enumerated accounts\u2014not root keys embedded in plaintext JSON long term.\n\nProvider APIs log every call; minimise repeated broad scans and purge credentials promptly after tabletop windows.\n\nCipherStrike transmits the JSON envelope to `/api/tools/terrascan`; NyxStrike performs final validation, then shells out or calls the bundled integration advertised for this catalogue id.",
    "safety": "Operate only inside authorized penetration tests or sanctioned lab networks. Respect stop conditions, lawful intercept rules, and internal change windows. Sensitive parameters are captured in CipherStrike tenant execution logs alongside NyxStrike response snippets \u2014 avoid pasting production secrets when alternatives exist. Cloud API tokens authenticate broad actions\u2014purge keys after tabletop exercises.",
    "usage": "CipherStrike `terrascan` executes against `/api/tools/terrascan` via POST JSON envelopes assembled in the Execute modal.\n\n1. Provide kubeconfig paths image refs benchmark ids directories matching Trivy/Checkov/kube-bench/Clair/Falco wrappers.\n2. Align severities/report formats with SOC ingestion pipelines.\n3. Remediation tips require owner teams\u2014don't auto-patch.\n4. Some scans mutate nothing; others exec inside clusters\u2014confirm blast radius.\n\nCipherStrike mechanics: send a JSON POST from the Execute modal\u2014the agent drops empty optional scalars before invoking NyxStrike.\nPrefer explicit catalogue fields (`target`, `url`, `wordlist`, etc.) before `additional_args`; extra tokens must still match the packaged wrapper behaviour and your engagement rules.\nPer-field catalogue hints (first sentence of modal help):\n- `scan_type` (optional): Connector-specific profile: Nmap flags, Trivy scan class, Burp macro, or ZAP policy name.\n- `iac_dir` (optional): Terraform/Kubernetes manifest tree for Terrascan.\n- `policy_type` (optional): The workspace forwards `policy_type` in the CipherStrike POST body unchanged after stripping empty optional fields.\n- `output_format` (optional): Structured report format (`json`, `xml`, `sarif`, `html`).\n- `severity` (optional): Minimum issue class for terrascan/trivy style reporting.\n- `additional_args` (optional): Shell tokens appended after mapped CLI arguments."
  },
  "testdisk": {
    "long_description": "TestDisk recovers partitions/boot sectors and undeletes files referencing interactive CLI flows automated here.\n\nForensic tooling streams carved files, timelines, entropy maps, PCAP decodes, volatility tables, SQLite extractions.\n\nForensic artefacts may contain HIPAA/PII regulated content\u2014encrypt exports immediately.\n\nResource-heavy jobs need disk-bound IO quotas; carve only targeted ranges when multi-terabyte images are mounted.\n\nCipherStrike transmits the JSON envelope to `/api/tools/testdisk`; NyxStrike performs final validation, then shells out or calls the bundled integration advertised for this catalogue id.",
    "safety": "Operate only inside authorized penetration tests or sanctioned lab networks. Respect stop conditions, lawful intercept rules, and internal change windows. Sensitive parameters are captured in CipherStrike tenant execution logs alongside NyxStrike response snippets \u2014 avoid pasting production secrets when alternatives exist. Evidence images may contain regulated data\u2014encrypt at rest immediately after extraction.",
    "usage": "CipherStrike `testdisk` executes against `/api/tools/testdisk` via POST JSON envelopes assembled in the Execute modal.\n\n1. Point `input_file`/`image_path`/`output_dir` fields at sanctioned evidence stores.\n2. Choose carving signatures conservatively\u2014broad signatures balloon storage.\n3. Keep chain-of-custody notes for every exported file.\n4. Some carvers overwrite slack space\u2014snapshot images first.\n\nCipherStrike mechanics: send a JSON POST from the Execute modal\u2014the agent drops empty optional scalars before invoking NyxStrike.\nPrefer explicit catalogue fields (`target`, `url`, `wordlist`, etc.) before `additional_args`; extra tokens must still match the packaged wrapper behaviour and your engagement rules.\nPer-field catalogue hints (first sentence of modal help):\n- `disk` (required): The workspace forwards `disk` in the CipherStrike POST body unchanged after stripping empty optional fields.\n- `additional_args` (optional): Shell tokens appended after mapped CLI arguments."
  },
  "testssl": {
    "long_description": "testssl.sh is a LibreSSL/OpenSSL-heavy TLS auditor enumerating cipher suites, certificate issues, known weaknesses, and downgrade vectors without needing a heavyweight GUI harness.\n\nOutput usually enumerates reachable paths, crawler graphs, fingerprints, robots hints, deduplicated URLs, or header snapshots.\n\nCombine directory bruteforce, spidering, and header probes sequentially: surface mapping before attempting injection payloads.\n\nMisconfigured crawlers crawl out of scope rapidly\u2014align seed URLs, blacklist regexes, and depth caps with authorised programs.\n\nCipherStrike transmits the JSON envelope to `/api/tools/testssl`; NyxStrike performs final validation, then shells out or calls the bundled integration advertised for this catalogue id.",
    "safety": "Operate only inside authorized penetration tests or sanctioned lab networks. Respect stop conditions, lawful intercept rules, and internal change windows. Sensitive parameters are captured in CipherStrike tenant execution logs alongside NyxStrike response snippets \u2014 avoid pasting production secrets when alternatives exist.",
    "usage": "CipherStrike `testssl` executes against `/api/tools/testssl` via POST JSON envelopes assembled in the Execute modal.\n\n1. Point `target` at host:port or URL forms accepted by testssl.sh packaging.\n2. Choose probe depth flags (ciphers, headers, vulnerabilities) via structured fields before `additional_args`.\n3. Expect lengthy output\u2014grep for `NOT ok`/`LOW` severity markers when triaging.\n4. Document mitigations per finding; some warnings are informational on legacy appliances.\n\nCipherStrike mechanics: send a JSON POST from the Execute modal\u2014the agent drops empty optional scalars before invoking NyxStrike.\nPrefer explicit catalogue fields (`target`, `url`, `wordlist`, etc.) before `additional_args`; extra tokens must still match the packaged wrapper behaviour and your engagement rules.\nPer-field catalogue hints (first sentence of modal help):\n- `target` (required): Primary IPv4/IPv6/hostname/CIDR or bulk newline list routed into network scanners and RPC clients.\n- `protocols` (optional): The workspace forwards `protocols` in the CipherStrike POST body unchanged after stripping empty optional fields.\n- `server_defaults` (optional): The workspace forwards `server_defaults` in the CipherStrike POST body unchanged after stripping empty optional fields.\n- `quiet` (optional): Suppress non-error MassDNS chatter.\n- `additional_args` (optional): Shell tokens appended after mapped CLI arguments."
  },
  "theHarvester": {
    "long_description": "theHarvester harvests emails, hosts, IPs, ASN metadata from search engines/APIs aligning with passive intel tradecraft.\n\nResults are textual lists, JSON artefacts, archive URLs, or passive reconnaissance breadcrumbs suitable for ingestion into trackers.\n\nOSINT tooling should precede intrusive scanning wherever policy demands minimal touch; escalate to DNS brute or crawler stages only after validating scope wording.\n\nPassive APIs throttle aggressively and may log lookups under shared keys\u2014credential handling matters.\n\nCipherStrike transmits the JSON envelope to `/api/tools/recon/theharvester`; NyxStrike performs final validation, then shells out or calls the bundled integration advertised for this catalogue id.",
    "safety": "Operate only inside authorized penetration tests or sanctioned lab networks. Respect stop conditions, lawful intercept rules, and internal change windows. Sensitive parameters are captured in CipherStrike tenant execution logs alongside NyxStrike response snippets \u2014 avoid pasting production secrets when alternatives exist.",
    "usage": "CipherStrike `theHarvester` executes against `/api/tools/recon/theharvester` via POST JSON envelopes assembled in the Execute modal.\n\n1. Configure `domains`/`sources`/API keys respecting theHarvester module availability.\n2. Expect mixed-quality emails\u2014verify via MX checks before phishing simulations.\n3. Output seeds further OSINT dashboards.\n4. Log source quotas to avoid bans.\n\nCipherStrike mechanics: send a JSON POST from the Execute modal\u2014the agent drops empty optional scalars before invoking NyxStrike.\nPrefer explicit catalogue fields (`target`, `url`, `wordlist`, etc.) before `additional_args`; extra tokens must still match the packaged wrapper behaviour and your engagement rules.\nPer-field catalogue hints (first sentence of modal help):\n- `domain` (required): Authorised DNS apex fed to Amass/subfinder/GAU style OSINT connectors.\n- `additional_args` (optional): Shell tokens appended after mapped CLI arguments."
  },
  "trivy": {
    "long_description": "Aqua Trivy scans container images, IaC, Kubernetes manifests, or filesystems for CVEs and misconfigurations with severity filters.\n\nCloud posture connectors emit JSON/HTML/SARIF findings enumerating IAM sprawl, open buckets, CIS deviations, stale keys, and attack-path narratives.\n\nCloud scans require least-privilege API roles scoped to enumerated accounts\u2014not root keys embedded in plaintext JSON long term.\n\nProvider APIs log every call; minimise repeated broad scans and purge credentials promptly after tabletop windows.\n\nCipherStrike transmits the JSON envelope to `/api/tools/trivy`; NyxStrike performs final validation, then shells out or calls the bundled integration advertised for this catalogue id.",
    "safety": "Operate only inside authorized penetration tests or sanctioned lab networks. Respect stop conditions, lawful intercept rules, and internal change windows. Sensitive parameters are captured in CipherStrike tenant execution logs alongside NyxStrike response snippets \u2014 avoid pasting production secrets when alternatives exist. Cloud API tokens authenticate broad actions\u2014purge keys after tabletop exercises.",
    "usage": "CipherStrike `trivy` executes against `/api/tools/trivy` via POST JSON envelopes assembled in the Execute modal.\n\n1. Provide kubeconfig paths image refs benchmark ids directories matching Trivy/Checkov/kube-bench/Clair/Falco wrappers.\n2. Align severities/report formats with SOC ingestion pipelines.\n3. Remediation tips require owner teams\u2014don't auto-patch.\n4. Some scans mutate nothing; others exec inside clusters\u2014confirm blast radius.\n\nCipherStrike mechanics: send a JSON POST from the Execute modal\u2014the agent drops empty optional scalars before invoking NyxStrike.\nPrefer explicit catalogue fields (`target`, `url`, `wordlist`, etc.) before `additional_args`; extra tokens must still match the packaged wrapper behaviour and your engagement rules.\nPer-field catalogue hints (first sentence of modal help):\n- `target` (required): Primary IPv4/IPv6/hostname/CIDR or bulk newline list routed into network scanners and RPC clients.\n- `scan_type` (optional): Connector-specific profile: Nmap flags, Trivy scan class, Burp macro, or ZAP policy name.\n- `severity` (optional): Minimum issue class for terrascan/trivy style reporting.\n- `additional_args` (optional): Shell tokens appended after mapped CLI arguments."
  },
  "tshark": {
    "long_description": "tshark is Wireshark\u2019s text mode exposing decoding pipelines, `-T` exporters, scripting (`-z`) stats for automation.\n\ntshark emits text decodes exporters stats suitable for scripted analysis.\n\nUse tshark in CI reproducible hunts reserve Wireshark GUIs for deep dives.\n\nDisplay filters differ from capture filters misunderstandings omit evidence.\n\nCipherStrike transmits the JSON envelope to `/api/tools/tshark`; NyxStrike performs final validation, then shells out or calls the bundled integration advertised for this catalogue id.",
    "safety": "Operate only inside authorized penetration tests or sanctioned lab networks. Respect stop conditions, lawful intercept rules, and internal change windows. Sensitive parameters are captured in CipherStrike tenant execution logs alongside NyxStrike response snippets \u2014 avoid pasting production secrets when alternatives exist. Wireless probing and injection are lawful only on spectrum and networks you control by license or contract.",
    "usage": "CipherStrike `tshark` executes against `/api/tools/tshark` via POST JSON envelopes assembled in the Execute modal.\n\n1. Provide PCAP paths capture/display filters exporters stats toggles aligning with tcpdump/tshark/wireshark packaging.\n2. Expect sensitive payloads minimise distribution encrypt everything.\n3. Follow lawful intercept mandates when analysing customer traffic mirrors.\n4. GUI Wireshark stays human-driven headless watchers feed SIEM deltas.\n\nCipherStrike mechanics: send a JSON POST from the Execute modal\u2014the agent drops empty optional scalars before invoking NyxStrike.\nPrefer explicit catalogue fields (`target`, `url`, `wordlist`, etc.) before `additional_args`; extra tokens must still match the packaged wrapper behaviour and your engagement rules.\nPer-field catalogue hints (first sentence of modal help):\n- `interface` (required): Network interface (`wlan0mon`, `eth0`) for Wi\u2011Fi or Responder jobs.\n- `capture_filter` (optional): The workspace forwards `capture_filter` in the CipherStrike POST body unchanged after stripping empty optional fields.\n- `display_filter` (optional): The workspace forwards `display_filter` in the CipherStrike POST body unchanged after stripping empty optional fields.\n- `duration` (optional): Runtime cap for Responder poison loops or Falco capture windows.\n- `output_file` (optional): Explicit output path for logs/PCAPNG/JSON when CLIs support `-o`.\n- `additional_args` (optional): Shell tokens appended after mapped CLI arguments."
  },
  "uro": {
    "long_description": "uro trims URL lists removing noisy duplicates with optional blacklist/whitelist filters.\n\nAPI tooling highlights schema quirks, undocumented parameters, malformed JWT behaviours, fuzz deltas, and property-test regressions referencing OpenAPI/GraphQL artefacts.\n\nSchema-first testers complement browser-only assessments by exercising machine contracts that never render in GUIs.\n\nGenerated traffic can cascade into billing or email workflows\u2014narrow operationIds and verbs before unattended fuzzing.\n\nCipherStrike transmits the JSON envelope to `/api/tools/uro`; NyxStrike performs final validation, then shells out or calls the bundled integration advertised for this catalogue id.",
    "safety": "Operate only inside authorized penetration tests or sanctioned lab networks. Respect stop conditions, lawful intercept rules, and internal change windows. Sensitive parameters are captured in CipherStrike tenant execution logs alongside NyxStrike response snippets \u2014 avoid pasting production secrets when alternatives exist. API fuzzing can trigger costly side-effects (email, billing, writes)\u2014narrow scope before unattended runs.",
    "usage": "CipherStrike `uro` executes against `/api/tools/uro` via POST JSON envelopes assembled in the Execute modal.\n\n1. Pipe newline URLs via `stdin` equivalent fields documented for qsreplace/anew/uro.\n2. Mark replacement tokens distinctly when prepping ffuf placeholders.\n3. Use `uro` to dedupe monstrous crawler outputs feeding nuclei templates.\n4. Document pipeline ordering for reproducibility.\n\nCipherStrike mechanics: send a JSON POST from the Execute modal\u2014the agent drops empty optional scalars before invoking NyxStrike.\nPrefer explicit catalogue fields (`target`, `url`, `wordlist`, etc.) before `additional_args`; extra tokens must still match the packaged wrapper behaviour and your engagement rules.\nPer-field catalogue hints (first sentence of modal help):\n- `urls` (required): URL batch for qsreplace, uro, or anew dedup pipelines.\n- `whitelist` (optional): uro allow-regex keeping only approved URL patterns.\n- `blacklist` (optional): Regex/glob denylist trimming spider scope.\n- `additional_args` (optional): Shell tokens appended after mapped CLI arguments."
  },
  "vol": {
    "long_description": "Volatility3 (`vol`) rewrites memory forensics with pythonic plugin architecture differing from volatility2 semantics\u2014supply plugin slugs/templates.\n\nForensic tooling streams carved files, timelines, entropy maps, PCAP decodes, volatility tables, SQLite extractions.\n\nForensic artefacts may contain HIPAA/PII regulated content\u2014encrypt exports immediately.\n\nResource-heavy jobs need disk-bound IO quotas; carve only targeted ranges when multi-terabyte images are mounted.\n\nCipherStrike transmits the JSON envelope to `/api/tools/volatility`; NyxStrike performs final validation, then shells out or calls the bundled integration advertised for this catalogue id.",
    "safety": "Operate only inside authorized penetration tests or sanctioned lab networks. Respect stop conditions, lawful intercept rules, and internal change windows. Sensitive parameters are captured in CipherStrike tenant execution logs alongside NyxStrike response snippets \u2014 avoid pasting production secrets when alternatives exist. Evidence images may contain regulated data\u2014encrypt at rest immediately after extraction.",
    "usage": "CipherStrike `vol` executes against `/api/tools/volatility` via POST JSON envelopes assembled in the Execute modal.\n\n1. Specify `memory_file`, Volatility-style `profile`/`plugin`, and plugin lists per route.\n2. Validate profile selection\u2014incorrect profiles produce nonsense.\n3. Store plugin outputs encrypted; RAM captures hold secrets wholesale.\n4. Coordinate runtime with forensic leads before running destructive plugins.\n\nCipherStrike mechanics: send a JSON POST from the Execute modal\u2014the agent drops empty optional scalars before invoking NyxStrike.\nPrefer explicit catalogue fields (`target`, `url`, `wordlist`, etc.) before `additional_args`; extra tokens must still match the packaged wrapper behaviour and your engagement rules.\nPer-field catalogue hints (first sentence of modal help):\n- `memory_file` (required): RAM dump consumed by volatility `plugin` jobs.\n- `plugin` (required): Volatility module (`windows.pslist`, `linux_bash`, \u2026).\n- `profile` (optional): Volatility KDBG profile **or** named cloud CLI profile for Prowler\u2014check tool context.\n- `additional_args` (optional): Shell tokens appended after mapped CLI arguments."
  },
  "volatility": {
    "long_description": "Volatility 2-classic exposes hundreds of forensic plugins for parsing Windows/Linux/macOS RAM dumps conditioned on memory profiles matching KDBG signatures.\n\nForensic tooling streams carved files, timelines, entropy maps, PCAP decodes, volatility tables, SQLite extractions.\n\nForensic artefacts may contain HIPAA/PII regulated content\u2014encrypt exports immediately.\n\nResource-heavy jobs need disk-bound IO quotas; carve only targeted ranges when multi-terabyte images are mounted.\n\nCipherStrike transmits the JSON envelope to `/api/tools/volatility`; NyxStrike performs final validation, then shells out or calls the bundled integration advertised for this catalogue id.",
    "safety": "Operate only inside authorized penetration tests or sanctioned lab networks. Respect stop conditions, lawful intercept rules, and internal change windows. Sensitive parameters are captured in CipherStrike tenant execution logs alongside NyxStrike response snippets \u2014 avoid pasting production secrets when alternatives exist. Evidence images may contain regulated data\u2014encrypt at rest immediately after extraction.",
    "usage": "CipherStrike `volatility` executes against `/api/tools/volatility` via POST JSON envelopes assembled in the Execute modal.\n\n1. Specify `memory_file`, Volatility-style `profile`/`plugin`, and plugin lists per route.\n2. Validate profile selection\u2014incorrect profiles produce nonsense.\n3. Store plugin outputs encrypted; RAM captures hold secrets wholesale.\n4. Coordinate runtime with forensic leads before running destructive plugins.\n\nCipherStrike mechanics: send a JSON POST from the Execute modal\u2014the agent drops empty optional scalars before invoking NyxStrike.\nPrefer explicit catalogue fields (`target`, `url`, `wordlist`, etc.) before `additional_args`; extra tokens must still match the packaged wrapper behaviour and your engagement rules.\nPer-field catalogue hints (first sentence of modal help):\n- `memory_file` (required): RAM dump consumed by volatility `plugin` jobs.\n- `plugin` (required): Volatility module (`windows.pslist`, `linux_bash`, \u2026).\n- `profile` (optional): Volatility KDBG profile **or** named cloud CLI profile for Prowler\u2014check tool context.\n- `additional_args` (optional): Shell tokens appended after mapped CLI arguments."
  },
  "vulnx": {
    "long_description": "CVE-oriented vulnerability intelligence aggregator that correlates CVE metadata, exploits, references, and remediation notes for triage\u2014not a substitute for vendor PSIRT advisories.\n\nExpect structured vulnerability records pulled from aggregated feeds: identifiers, summaries, remediation pointers, and referencing links rather than exploitation proof.\n\nReach for vulnx-class routes when correlating disclosures during triage instead of brute-forcing local mirrors with searchsploit or hitting vendor APIs manually.\n\nIntel licensing varies by source; redistribution outside the tenancy may violate vendor terms.\n\nCipherStrike transmits the JSON envelope to `/api/vuln-intel/vulnx`; NyxStrike performs final validation, then shells out or calls the bundled integration advertised for this catalogue id.",
    "safety": "Operate only inside authorized penetration tests or sanctioned lab networks. Respect stop conditions, lawful intercept rules, and internal change windows. Sensitive parameters are captured in CipherStrike tenant execution logs alongside NyxStrike response snippets \u2014 avoid pasting production secrets when alternatives exist. Upstream intel feeds carry redistribution constraints\u2014respect vendor terms internally.",
    "usage": "CipherStrike `vulnx` executes against `/api/vuln-intel/vulnx` via POST JSON envelopes assembled in the Execute modal.\n\n1. Supply either `cve_id` for a focused pull or `search` for keyword discovery\u2014avoid blank queries that force broad downloads.\n2. When `auth_key` is required, load the vendor-approved secret; never append raw keys to `additional_args`.\n3. Map returned metadata to internal ticketing; do not treat CVE descriptions as exploit guarantees.\n4. Archive only what license terms allow\u2014some feeds forbid redistribution.\n\nCipherStrike mechanics: send a JSON POST from the Execute modal\u2014the agent drops empty optional scalars before invoking NyxStrike.\nPrefer explicit catalogue fields (`target`, `url`, `wordlist`, etc.) before `additional_args`; extra tokens must still match the packaged wrapper behaviour and your engagement rules.\nPer-field catalogue hints (first sentence of modal help):\n- `cve_id` (optional): CVE identifier for vulnx-style intel APIs.\n- `search` (optional): Keyword search for vulnx when CVE id unknown.\n- `auth_key` (optional): API key/bearer for premium vulnerability intel feeds."
  },
  "wafw00f": {
    "long_description": "wafw00f fingerprints web application firewall products by provoking characteristic block pages and heuristic signatures.\n\nOutput usually enumerates reachable paths, crawler graphs, fingerprints, robots hints, deduplicated URLs, or header snapshots.\n\nCombine directory bruteforce, spidering, and header probes sequentially: surface mapping before attempting injection payloads.\n\nMisconfigured crawlers crawl out of scope rapidly\u2014align seed URLs, blacklist regexes, and depth caps with authorised programs.\n\nCipherStrike transmits the JSON envelope to `/api/tools/wafw00f`; NyxStrike performs final validation, then shells out or calls the bundled integration advertised for this catalogue id.",
    "safety": "Operate only inside authorized penetration tests or sanctioned lab networks. Respect stop conditions, lawful intercept rules, and internal change windows. Sensitive parameters are captured in CipherStrike tenant execution logs alongside NyxStrike response snippets \u2014 avoid pasting production secrets when alternatives exist.",
    "usage": "CipherStrike `wafw00f` executes against `/api/tools/wafw00f` via POST JSON envelopes assembled in the Execute modal.\n\n1. Provide `url` for the protected application entry point.\n2. Run before heavy fuzzing to understand blocking behaviour.\n3. Compare output signatures with vendor documentation to tune bypass research ethically.\n4. Do not use identified weaknesses against systems outside scope.\n\nCipherStrike mechanics: send a JSON POST from the Execute modal\u2014the agent drops empty optional scalars before invoking NyxStrike.\nPrefer explicit catalogue fields (`target`, `url`, `wordlist`, etc.) before `additional_args`; extra tokens must still match the packaged wrapper behaviour and your engagement rules.\nPer-field catalogue hints (first sentence of modal help):\n- `url` (required): Fully qualified HTTP/S URL for crawlers, fuzzers, or vuln scanners (scheme required).\n- `additional_args` (optional): Shell tokens appended after mapped CLI arguments."
  },
  "waybackurls": {
    "long_description": "waybackurls (tomnommom) specialises in streaming Wayback Machine endpoints for archival URL mining.\n\nResults are textual lists, JSON artefacts, archive URLs, or passive reconnaissance breadcrumbs suitable for ingestion into trackers.\n\nOSINT tooling should precede intrusive scanning wherever policy demands minimal touch; escalate to DNS brute or crawler stages only after validating scope wording.\n\nPassive APIs throttle aggressively and may log lookups under shared keys\u2014credential handling matters.\n\nCipherStrike transmits the JSON envelope to `/api/tools/waybackurls`; NyxStrike performs final validation, then shells out or calls the bundled integration advertised for this catalogue id.",
    "safety": "Operate only inside authorized penetration tests or sanctioned lab networks. Respect stop conditions, lawful intercept rules, and internal change windows. Sensitive parameters are captured in CipherStrike tenant execution logs alongside NyxStrike response snippets \u2014 avoid pasting production secrets when alternatives exist.",
    "usage": "CipherStrike `waybackurls` executes against `/api/tools/waybackurls` via POST JSON envelopes assembled in the Execute modal.\n\n1. Provide `domains`/`domain`/`input` equivalents per GAU/WaybackURL connector mapping.\n2. Filter archives to scope hostnames programmatically afterwards.\n3. Archives may expose retired credentials\u2014handle carefully.\n4. Rate-limit concurrency to obey remote archive policies.\n\nCipherStrike mechanics: send a JSON POST from the Execute modal\u2014the agent drops empty optional scalars before invoking NyxStrike.\nPrefer explicit catalogue fields (`target`, `url`, `wordlist`, etc.) before `additional_args`; extra tokens must still match the packaged wrapper behaviour and your engagement rules.\nPer-field catalogue hints (first sentence of modal help):\n- `domain` (required): Authorised DNS apex fed to Amass/subfinder/GAU style OSINT connectors.\n- `additional_args` (optional): Shell tokens appended after mapped CLI arguments."
  },
  "waymore": {
    "long_description": "waymore automates crawling multiple web archives (including Wayback and partner mirrors) to resurrect historical URLs and HTTP responses tied to OSINT pivots.\n\nResults are textual lists, JSON artefacts, archive URLs, or passive reconnaissance breadcrumbs suitable for ingestion into trackers.\n\nOSINT tooling should precede intrusive scanning wherever policy demands minimal touch; escalate to DNS brute or crawler stages only after validating scope wording.\n\nPassive APIs throttle aggressively and may log lookups under shared keys\u2014credential handling matters.\n\nCipherStrike transmits the JSON envelope to `/api/tools/waymore`; NyxStrike performs final validation, then shells out or calls the bundled integration advertised for this catalogue id.",
    "safety": "Operate only inside authorized penetration tests or sanctioned lab networks. Respect stop conditions, lawful intercept rules, and internal change windows. Sensitive parameters are captured in CipherStrike tenant execution logs alongside NyxStrike response snippets \u2014 avoid pasting production secrets when alternatives exist.",
    "usage": "CipherStrike `waymore` executes against `/api/tools/waymore` via POST JSON envelopes assembled in the Execute modal.\n\n1. Provide `input` using the domain, scope seed, or archive token expected by the packaged waymore profile.\n2. Choose `mode` and output toggles (`output_urls`, `output_responses`) based on whether you need bare URLs or stored HTTP bodies.\n3. Expect large artefacts when response capture is enabled\u2014stage disk on the agent host accordingly.\n4. Historical captures may include personal data; scrub before exporting.\n\nCipherStrike mechanics: send a JSON POST from the Execute modal\u2014the agent drops empty optional scalars before invoking NyxStrike.\nPrefer explicit catalogue fields (`target`, `url`, `wordlist`, etc.) before `additional_args`; extra tokens must still match the packaged wrapper behaviour and your engagement rules.\nPer-field catalogue hints (first sentence of modal help):\n- `input` (required): Generic text blob for waymore/hurl data transforms.\n- `mode` (optional): High-level tool mode (Amass `enum`, gobuster `dir`, shuffleDNS profile, etc.).\n- `output_urls` (optional): Persist URL-only lines from waymore-style archive mining.\n- `output_responses` (optional): Persist raw HTTP bodies from waymore-style mining."
  },
  "wfuzz": {
    "long_description": "Wfuzz multiplexes payloads into HTTP verbs/headers/parameters with advanced filters analogous to Burp Intruder ergonomics.\n\nOutput usually enumerates reachable paths, crawler graphs, fingerprints, robots hints, deduplicated URLs, or header snapshots.\n\nCombine directory bruteforce, spidering, and header probes sequentially: surface mapping before attempting injection payloads.\n\nMisconfigured crawlers crawl out of scope rapidly\u2014align seed URLs, blacklist regexes, and depth caps with authorised programs.\n\nCipherStrike transmits the JSON envelope to `/api/tools/wfuzz`; NyxStrike performs final validation, then shells out or calls the bundled integration advertised for this catalogue id.",
    "safety": "Operate only inside authorized penetration tests or sanctioned lab networks. Respect stop conditions, lawful intercept rules, and internal change windows. Sensitive parameters are captured in CipherStrike tenant execution logs alongside NyxStrike response snippets \u2014 avoid pasting production secrets when alternatives exist.",
    "usage": "CipherStrike `wfuzz` executes against `/api/tools/wfuzz` via POST JSON envelopes assembled in the Execute modal.\n\n1. Define the `FUZZ`/`FFUF` keyword placement in `url`, headers, or data fields per this connector\u2019s template.\n2. Load wordlists and configure matchers/filters to ignore static 404 pages.\n3. Keep concurrency aligned with WAF rate limits; use `delay` options when present.\n4. Log successful fuzz slots for replay in Burp or ZAP if deeper manual testing is required.\n\nCipherStrike mechanics: send a JSON POST from the Execute modal\u2014the agent drops empty optional scalars before invoking NyxStrike.\nPrefer explicit catalogue fields (`target`, `url`, `wordlist`, etc.) before `additional_args`; extra tokens must still match the packaged wrapper behaviour and your engagement rules.\nPer-field catalogue hints (first sentence of modal help):\n- `url` (required): Fully qualified HTTP/S URL for crawlers, fuzzers, or vuln scanners (scheme required).\n- `wordlist` (optional): Agent-resident newline wordlist for content/DNS/password tooling.\n- `additional_args` (optional): Shell tokens appended after mapped CLI arguments."
  },
  "whatweb": {
    "long_description": "WhatWeb applies thousands of fingerprints to HTTP stacks discovering CMS/CDN/email addresses quickly.\n\nOutputs highlight technology stacks inferred from artefacts you already harvested from HTML, TLS metadata, HTTP responses, and similar signals.\n\nUse fingerprints to prioritise vuln hunts that actually apply to surfaced components instead of blindly launching every template pack.\n\nInference is probabilistic\u2014verify findings manually before escalating to intrusive checks.\n\nCipherStrike transmits the JSON envelope to `/api/tools/web_recon/whatweb`; NyxStrike performs final validation, then shells out or calls the bundled integration advertised for this catalogue id.",
    "safety": "Operate only inside authorized penetration tests or sanctioned lab networks. Respect stop conditions, lawful intercept rules, and internal change windows. Sensitive parameters are captured in CipherStrike tenant execution logs alongside NyxStrike response snippets \u2014 avoid pasting production secrets when alternatives exist. Fingerprinting correlates artefacts\u2014sanitize exports when customer metadata is classified.",
    "usage": "CipherStrike `whatweb` executes against `/api/tools/web_recon/whatweb` via POST JSON envelopes assembled in the Execute modal.\n\n1. Supply newline hosts/toggle aggression fields per WhatWeb wrappers.\n2. Useful for coarse stack discovery ahead of scanners.\n3. False positives abound\u2014sanity-check with manual headers.\n4. Rate-limit concurrency on SaaS fronts.\n\nCipherStrike mechanics: send a JSON POST from the Execute modal\u2014the agent drops empty optional scalars before invoking NyxStrike.\nPrefer explicit catalogue fields (`target`, `url`, `wordlist`, etc.) before `additional_args`; extra tokens must still match the packaged wrapper behaviour and your engagement rules.\nPer-field catalogue hints (first sentence of modal help):\n- `url` (required): Fully qualified HTTP/S URL for crawlers, fuzzers, or vuln scanners (scheme required)."
  },
  "whois": {
    "long_description": "whois retrieves registrar/registry data revealing abuse contacts, name servers, and routing artefacts for IPs or domains depending on registrar coverage.\n\nResults are textual lists, JSON artefacts, archive URLs, or passive reconnaissance breadcrumbs suitable for ingestion into trackers.\n\nOSINT tooling should precede intrusive scanning wherever policy demands minimal touch; escalate to DNS brute or crawler stages only after validating scope wording.\n\nPassive APIs throttle aggressively and may log lookups under shared keys\u2014credential handling matters.\n\nCipherStrike transmits the JSON envelope to `/api/tools/whois`; NyxStrike performs final validation, then shells out or calls the bundled integration advertised for this catalogue id.",
    "safety": "Operate only inside authorized penetration tests or sanctioned lab networks. Respect stop conditions, lawful intercept rules, and internal change windows. Sensitive parameters are captured in CipherStrike tenant execution logs alongside NyxStrike response snippets \u2014 avoid pasting production secrets when alternatives exist.",
    "usage": "CipherStrike `whois` executes against `/api/tools/whois` via POST JSON envelopes assembled in the Execute modal.\n\n1. Submit `domain` or IP handles via the structured WHOIS mapper.\n2. Respect registrar rate limits\u2014use official mirrors when possible.\n3. Archive abuse contacts for escalation paths.\n4. WHOIS masking may hide ownership; note limitations in reports.\n\nCipherStrike mechanics: send a JSON POST from the Execute modal\u2014the agent drops empty optional scalars before invoking NyxStrike.\nPrefer explicit catalogue fields (`target`, `url`, `wordlist`, etc.) before `additional_args`; extra tokens must still match the packaged wrapper behaviour and your engagement rules.\nPer-field catalogue hints (first sentence of modal help):\n- `target` (required): Primary IPv4/IPv6/hostname/CIDR or bulk newline list routed into network scanners and RPC clients."
  },
  "wifite": {
    "long_description": "wifite orchestrates WPA/WEP attack playbooks chaining airmon/aircrack/reaver bully flows with guided prompts\u2014still demands RF discipline.\n\n802.11 harnesses emit captures, WPA hash material, rogue AP scaffolding, injector acknowledgements, and audit logs keyed to channels/BSSIDs.\n\nLaboratory rigs should dedicate monitor-mode interfaces plus isolated spectrum; tethered enterprise APs rarely tolerate injection homework.\n\nJamming neighbours or intercepting unauthorised traffic violates law\u2014operate strictly inside sanctioned RF enclosures and licensed bands.\n\nCipherStrike transmits the JSON envelope to `/api/tools/wifi_pentest/wifite2`; NyxStrike performs final validation, then shells out or calls the bundled integration advertised for this catalogue id.",
    "safety": "Operate only inside authorized penetration tests or sanctioned lab networks. Respect stop conditions, lawful intercept rules, and internal change windows. Sensitive parameters are captured in CipherStrike tenant execution logs alongside NyxStrike response snippets \u2014 avoid pasting production secrets when alternatives exist. Wireless probing and injection are lawful only on spectrum and networks you control by license or contract.",
    "usage": "CipherStrike `wifite` executes against `/api/tools/wifi_pentest/wifite2` via POST JSON envelopes assembled in the Execute modal.\n\n1. Place adapters in monitor mode upstream when required (`airmon-ng` helper routes).\n2. Provide capture paths, ESSIDs/EAP configs, handshake targets, attack modes, regulatory paperwork references.\n3. Never point attack modes at civilian infrastructure\u2014hardware isolation only.\n4. Convert captures only within labs; distributing PMKIDs may violate telecom rules.\n\nCipherStrike mechanics: send a JSON POST from the Execute modal\u2014the agent drops empty optional scalars before invoking NyxStrike.\nPrefer explicit catalogue fields (`target`, `url`, `wordlist`, etc.) before `additional_args`; extra tokens must still match the packaged wrapper behaviour and your engagement rules.\nPer-field catalogue hints (first sentence of modal help):\n- `interface` (required): Network interface (`wlan0mon`, `eth0`) for Wi\u2011Fi or Responder jobs.\n- `target_essid` (optional): SSID string for rogue AP labs or WPA correlation workflows.\n- `target_bssid` (optional): 802.11 BSSID narrowed for Bettercap Wi\u2011Fi tooling.\n- `attack_pmkid` (optional): Prefer PMKID capture paths on supported chipsets.\n- `attack_handshake` (optional): Wi-Fi utilities bias toward WPA handshake harvesting when drivers allow.\n- `wordlist` (optional): Agent-resident newline wordlist for content/DNS/password tooling.\n- `timeout` (optional): Seconds for socket/DNS/CLI overall timeouts."
  },
  "wireshark": {
    "long_description": "Wireshark/tshark PCAP analysis supports capture filters/display filters scripted operations\u2014mind privacy.\n\nGUI Wireshark transcripts decode filters export packages\u2014normally lab only when headless watchers insufficient.\n\nGraphical decodes help humans correlate streams automated tshark better for pipelines.\n\nPCAPs expose credentials never share raw captures casually.\n\nCipherStrike transmits the JSON envelope to `/api/tools/wireshark`; NyxStrike performs final validation, then shells out or calls the bundled integration advertised for this catalogue id.",
    "safety": "Operate only inside authorized penetration tests or sanctioned lab networks. Respect stop conditions, lawful intercept rules, and internal change windows. Sensitive parameters are captured in CipherStrike tenant execution logs alongside NyxStrike response snippets \u2014 avoid pasting production secrets when alternatives exist. Wireless probing and injection are lawful only on spectrum and networks you control by license or contract. PCAP files may hold credentials or PII\u2014encrypt archives and limit distribution.",
    "usage": "CipherStrike `wireshark` executes against `/api/tools/wireshark` via POST JSON envelopes assembled in the Execute modal.\n\n1. Provide PCAP paths capture/display filters exporters stats toggles aligning with tcpdump/tshark/wireshark packaging.\n2. Expect sensitive payloads minimise distribution encrypt everything.\n3. Follow lawful intercept mandates when analysing customer traffic mirrors.\n4. GUI Wireshark stays human-driven headless watchers feed SIEM deltas.\n\nCipherStrike mechanics: send a JSON POST from the Execute modal\u2014the agent drops empty optional scalars before invoking NyxStrike.\nPrefer explicit catalogue fields (`target`, `url`, `wordlist`, etc.) before `additional_args`; extra tokens must still match the packaged wrapper behaviour and your engagement rules.\nPer-field catalogue hints (first sentence of modal help):\n- `interface` (required): Network interface (`wlan0mon`, `eth0`) for Wi\u2011Fi or Responder jobs.\n- `capture_filter` (optional): The workspace forwards `capture_filter` in the CipherStrike POST body unchanged after stripping empty optional fields.\n- `duration` (optional): Runtime cap for Responder poison loops or Falco capture windows.\n- `output_file` (optional): Explicit output path for logs/PCAPNG/JSON when CLIs support `-o`.\n- `additional_args` (optional): Shell tokens appended after mapped CLI arguments."
  },
  "wpscan": {
    "long_description": "WPScan targets WordPress installations by enumerating users, outdated plugins/themes, Interesting Files, CVE references, and brute-force safeguards aware of canonical WordPress internals.\n\nOutput usually enumerates reachable paths, crawler graphs, fingerprints, robots hints, deduplicated URLs, or header snapshots.\n\nCombine directory bruteforce, spidering, and header probes sequentially: surface mapping before attempting injection payloads.\n\nMisconfigured crawlers crawl out of scope rapidly\u2014align seed URLs, blacklist regexes, and depth caps with authorised programs.\n\nCipherStrike transmits the JSON envelope to `/api/tools/wpscan`; NyxStrike performs final validation, then shells out or calls the bundled integration advertised for this catalogue id.",
    "safety": "Operate only inside authorized penetration tests or sanctioned lab networks. Respect stop conditions, lawful intercept rules, and internal change windows. Sensitive parameters are captured in CipherStrike tenant execution logs alongside NyxStrike response snippets \u2014 avoid pasting production secrets when alternatives exist.",
    "usage": "CipherStrike `wpscan` executes against `/api/tools/wpscan` via POST JSON envelopes assembled in the Execute modal.\n\n1. Include `url`, optional API tokens, and enumeration toggles for users or plugins.\n2. Expect aggressive plugins list\u2014validate versions manually before claiming exploitability.\n3. Throttle brute-force modules; many CMS tools include login tests.\n4. Archive JSON output for regression testing after patching.\n\nCipherStrike mechanics: send a JSON POST from the Execute modal\u2014the agent drops empty optional scalars before invoking NyxStrike.\nPrefer explicit catalogue fields (`target`, `url`, `wordlist`, etc.) before `additional_args`; extra tokens must still match the packaged wrapper behaviour and your engagement rules.\nPer-field catalogue hints (first sentence of modal help):\n- `url` (required): Fully qualified HTTP/S URL for crawlers, fuzzers, or vuln scanners (scheme required).\n- `additional_args` (optional): Shell tokens appended after mapped CLI arguments."
  },
  "x8": {
    "long_description": "x8 diffs baseline vs candidate parameter responses to spotlight hidden reflective parameters with optional body/header contexts.\n\nOutput usually enumerates reachable paths, crawler graphs, fingerprints, robots hints, deduplicated URLs, or header snapshots.\n\nCombine directory bruteforce, spidering, and header probes sequentially: surface mapping before attempting injection payloads.\n\nMisconfigured crawlers crawl out of scope rapidly\u2014align seed URLs, blacklist regexes, and depth caps with authorised programs.\n\nCipherStrike transmits the JSON envelope to `/api/tools/x8`; NyxStrike performs final validation, then shells out or calls the bundled integration advertised for this catalogue id.",
    "safety": "Operate only inside authorized penetration tests or sanctioned lab networks. Respect stop conditions, lawful intercept rules, and internal change windows. Sensitive parameters are captured in CipherStrike tenant execution logs alongside NyxStrike response snippets \u2014 avoid pasting production secrets when alternatives exist.",
    "usage": "CipherStrike `x8` executes against `/api/tools/x8` via POST JSON envelopes assembled in the Execute modal.\n\n1. Submit baseline URLs payloads headers bodies per x8 template.\n2. Interpret diffs cautiously\u2014CDNs introduce noise.\n3. Pair results with authorised ffuf bursts.\n4. Keep concurrency sane on fragile APIs.\n\nCipherStrike mechanics: send a JSON POST from the Execute modal\u2014the agent drops empty optional scalars before invoking NyxStrike.\nPrefer explicit catalogue fields (`target`, `url`, `wordlist`, etc.) before `additional_args`; extra tokens must still match the packaged wrapper behaviour and your engagement rules.\nPer-field catalogue hints (first sentence of modal help):\n- `url` (required): Fully qualified HTTP/S URL for crawlers, fuzzers, or vuln scanners (scheme required).\n- `wordlist` (optional): Agent-resident newline wordlist for content/DNS/password tooling.\n- `method` (optional): HTTP verb (`GET`, `POST`, \u2026) or tool-specific dispatcher (ffuf mode selection, HTTP-framework action cues, Arjun transport).\n- `body` (optional): Raw HTTP body for x8-style hidden parameter fuzzing.\n- `headers` (optional): HTTP headers as JSON map or newline text for replay utilities.\n- `additional_args` (optional): Shell tokens appended after mapped CLI arguments."
  },
  "xsser": {
    "long_description": "XSSer assembles vectored XSS attacks with GUI/automation primitives, crawler hooks, statistical reporting, and heuristic payload sets aligned with OWASP classifications.\n\nOutput usually enumerates reachable paths, crawler graphs, fingerprints, robots hints, deduplicated URLs, or header snapshots.\n\nCombine directory bruteforce, spidering, and header probes sequentially: surface mapping before attempting injection payloads.\n\nMisconfigured crawlers crawl out of scope rapidly\u2014align seed URLs, blacklist regexes, and depth caps with authorised programs.\n\nCipherStrike transmits the JSON envelope to `/api/tools/xsser`; NyxStrike performs final validation, then shells out or calls the bundled integration advertised for this catalogue id.",
    "safety": "Operate only inside authorized penetration tests or sanctioned lab networks. Respect stop conditions, lawful intercept rules, and internal change windows. Sensitive parameters are captured in CipherStrike tenant execution logs alongside NyxStrike response snippets \u2014 avoid pasting production secrets when alternatives exist.",
    "usage": "CipherStrike `xsser` executes against `/api/tools/xsser` via POST JSON envelopes assembled in the Execute modal.\n\n1. Supply target URLs/parameters and optional crawler hooks/colaborator URLs.\n2. Start with benign payloads verifying reflection, then escalate using prepared polyglots.\n3. Capture proofs without impacting real users\u2014use labs or seeded accounts.\n4. Integrate DalFox/XSSer output with ticketing for developer reproduction.\n\nCipherStrike mechanics: send a JSON POST from the Execute modal\u2014the agent drops empty optional scalars before invoking NyxStrike.\nPrefer explicit catalogue fields (`target`, `url`, `wordlist`, etc.) before `additional_args`; extra tokens must still match the packaged wrapper behaviour and your engagement rules.\nPer-field catalogue hints (first sentence of modal help):\n- `url` (required): Fully qualified HTTP/S URL for crawlers, fuzzers, or vuln scanners (scheme required).\n- `additional_args` (optional): Shell tokens appended after mapped CLI arguments."
  },
  "xxd": {
    "long_description": "xxd hex-dumps or reverts dumps with offset/length controls mirroring util-linux behaviour.\n\nForensic tooling streams carved files, timelines, entropy maps, PCAP decodes, volatility tables, SQLite extractions.\n\nForensic artefacts may contain HIPAA/PII regulated content\u2014encrypt exports immediately.\n\nResource-heavy jobs need disk-bound IO quotas; carve only targeted ranges when multi-terabyte images are mounted.\n\nCipherStrike transmits the JSON envelope to `/api/tools/xxd`; NyxStrike performs final validation, then shells out or calls the bundled integration advertised for this catalogue id.",
    "safety": "Operate only inside authorized penetration tests or sanctioned lab networks. Respect stop conditions, lawful intercept rules, and internal change windows. Sensitive parameters are captured in CipherStrike tenant execution logs alongside NyxStrike response snippets \u2014 avoid pasting production secrets when alternatives exist. Evidence images may contain regulated data\u2014encrypt at rest immediately after extraction.",
    "usage": "CipherStrike `xxd` executes against `/api/tools/xxd` via POST JSON envelopes assembled in the Execute modal.\n\n1. Provide `binary`, `file_path`, or CLI-equivalent selectors for quick static insight.\n2. Use triage utilities before heavyweight Ghidra/autopsy timelines.\n3. Outputs are succinct\u2014combine with checklist reporting.\n4. Malware artefacts remain hazardous\u2014sandbox hosts accordingly.\n\nCipherStrike mechanics: send a JSON POST from the Execute modal\u2014the agent drops empty optional scalars before invoking NyxStrike.\nPrefer explicit catalogue fields (`target`, `url`, `wordlist`, etc.) before `additional_args`; extra tokens must still match the packaged wrapper behaviour and your engagement rules.\nPer-field catalogue hints (first sentence of modal help):\n- `file_path` (required): Readable path for xxd/exiftool style utilities.\n- `offset` (optional): The workspace forwards `offset` in the CipherStrike POST body unchanged after stripping empty optional fields.\n- `length` (optional): The workspace forwards `length` in the CipherStrike POST body unchanged after stripping empty optional fields.\n- `additional_args` (optional): Shell tokens appended after mapped CLI arguments."
  },
  "zaproxy": {
    "long_description": "OWASP ZAP automation configures baseline/full scans against `target`, API listener ports, keyed automation (`api_key`), `scan_type`, and exporters.\n\nStructured runs usually report vulnerable parameters, collaborator callbacks, payloads attempted, timings, tamper artefacts, and shell evidence when enabled.\n\nAutomation accelerates repeatable bug classes yet still requires manual corroboration; pair with benign accounts and isolated databases.\n\nExploit payloads can corrupt data stores or escalate privileges\u2014replay only artefacts provisioned explicitly for drills.\n\nCipherStrike transmits the JSON envelope to `/api/tools/zap`; NyxStrike performs final validation, then shells out or calls the bundled integration advertised for this catalogue id.",
    "safety": "Operate only inside authorized penetration tests or sanctioned lab networks. Respect stop conditions, lawful intercept rules, and internal change windows. Sensitive parameters are captured in CipherStrike tenant execution logs alongside NyxStrike response snippets \u2014 avoid pasting production secrets when alternatives exist. Active payloads may corrupt data stores\u2014replay only seeded test accounts created for the drill. Automated attacks may submit forms or reset sessions\u2014run against disposable tenants.",
    "usage": "CipherStrike `zaproxy` executes against `/api/tools/zap` via POST JSON envelopes assembled in the Execute modal.\n\n1. Configure `target`, API listener keys, concurrency pages scan policies per Burp/ZAP automation fa\u00e7ade.\n2. Headless scanners still mutate state validate CSRF safeguards.\n3. Export reports into encrypted evidence stores promptly.\n4. Provide unique API keys per engagement.\n\nCipherStrike mechanics: send a JSON POST from the Execute modal\u2014the agent drops empty optional scalars before invoking NyxStrike.\nPrefer explicit catalogue fields (`target`, `url`, `wordlist`, etc.) before `additional_args`; extra tokens must still match the packaged wrapper behaviour and your engagement rules.\nPer-field catalogue hints (first sentence of modal help):\n- `target` (required): Primary IPv4/IPv6/hostname/CIDR or bulk newline list routed into network scanners and RPC clients.\n- `scan_type` (optional): Connector-specific profile: Nmap flags, Trivy scan class, Burp macro, or ZAP policy name.\n- `api_key` (optional): Privileged bearer (ZAP API, SaaS connectors)\u2014never ticket plain-text.\n- `port` (optional): Single TCP port (for example ZAP\u2019s local proxy) distinct from multi-port scans.\n- `output_format` (optional): Structured report format (`json`, `xml`, `sarif`, `html`).\n- `additional_args` (optional): Shell tokens appended after mapped CLI arguments."
  },
  "zsteg": {
    "long_description": "zsteg detects PNG/BMP/APNG anomalies (zlib/LSB/comment abuse) favoured in layered CTF payloads.\n\nForensic tooling streams carved files, timelines, entropy maps, PCAP decodes, volatility tables, SQLite extractions.\n\nForensic artefacts may contain HIPAA/PII regulated content\u2014encrypt exports immediately.\n\nResource-heavy jobs need disk-bound IO quotas; carve only targeted ranges when multi-terabyte images are mounted.\n\nCipherStrike transmits the JSON envelope to `/api/tools/zsteg`; NyxStrike performs final validation, then shells out or calls the bundled integration advertised for this catalogue id.",
    "safety": "Operate only inside authorized penetration tests or sanctioned lab networks. Respect stop conditions, lawful intercept rules, and internal change windows. Sensitive parameters are captured in CipherStrike tenant execution logs alongside NyxStrike response snippets \u2014 avoid pasting production secrets when alternatives exist. Evidence images may contain regulated data\u2014encrypt at rest immediately after extraction.",
    "usage": "CipherStrike `zsteg` executes against `/api/tools/zsteg` via POST JSON envelopes assembled in the Execute modal.\n\n1. Provide cover/embed files passphrase actions keyed to steghide/stegsolve/zsteg/outguess flows.\n2. Outputs may be ambiguous\u2014combine with entropy analysis elsewhere.\n3. Carriers might be copyrighted\u2014handle distribution carefully.\n4. Document attempted transforms for regulators.\n\nCipherStrike mechanics: send a JSON POST from the Execute modal\u2014the agent drops empty optional scalars before invoking NyxStrike.\nPrefer explicit catalogue fields (`target`, `url`, `wordlist`, etc.) before `additional_args`; extra tokens must still match the packaged wrapper behaviour and your engagement rules.\nPer-field catalogue hints (first sentence of modal help):\n- `file_path` (required): Readable path for xxd/exiftool style utilities.\n- `additional_args` (optional): Shell tokens appended after mapped CLI arguments."
  }
}
